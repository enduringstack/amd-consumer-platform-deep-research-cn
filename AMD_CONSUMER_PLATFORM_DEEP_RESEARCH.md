# AMD 消费端与完整产品体系深度研究

> 研究快照：2026-08-04。本文件由 `scripts/assemble_report.sh` 从 `research/` 源章节确定性生成，请勿直接编辑。

本报告覆盖 Ryzen 桌面/移动/APU、Ryzen AI/AI Max、Threadripper、Radeon、掌机、mini PC、半定制，以及 EPYC、Instinct、Embedded、Xilinx 自适应计算、Pensando DPU 和端到端软件体系。


---

## 编辑说明、研究口径与结论摘要

> 研究快照：2026-08-04。本文档中的“当前”“最新”均指该日期；报告不是投资建议，也不是不附条件的购买榜单。

### 一、先给结论

AMD 消费端产品的真正特征，不是简单的“CPU 加 Radeon”，而是把同一批可复用的核心资产放入不同封装和功耗边界：Zen CPU 核、RDNA 图形、XDNA NPU、内存控制器、I/O 与安全模块，再通过 Infinity Fabric、chiplet 和软件栈组成桌面、笔记本、工作站、掌机、游戏机、服务器及嵌入式产品。Ryzen 9、Ryzen AI Max、Threadripper PRO、EPYC 和 Instinct 不是一条由低到高的刻度，而是不同内存容量、延迟、扩展、可靠性和软件合同的解。[A001][A002][A004]

消费端的优势集中在三处。第一，AM4 到 AM5 的平台连续性、CCD/IOD 复用和 3D V-Cache，使 AMD 能把高频通用计算、超大末级缓存和多核扩展做成不同 SKU，而不必为每一细分市场重做整颗单片。第二，Ryzen AI Max 把较宽 LPDDR5X 接口、最多 40 个 RDNA 3.5 CU、Zen 5 CPU 与 XDNA 2 放入共享内存 SoC，形成介于传统轻薄 APU 和独立显卡移动工作站之间的新类别。第三，半定制能力让同源 IP 进入 PlayStation、Xbox 与掌机，积累能效、驱动和开发者工具的规模效应。[A006][A001][A029]

限制同样结构性存在。芯片峰值不等于整机持续性能；同一移动 SKU 在 15W 与 54W、单通道与双通道、低速与高速内存、安静与性能风扇曲线下会像不同产品。NPU 的 TOPS 不等于应用速度，必须同时满足模型算子、精度、内存布局、运行时和 OEM/ISV 启用。ROCm 已扩大到部分 Radeon 与 Ryzen APU，但支持矩阵按 SKU、操作系统和组件划分，不能把 HIP 的源代码可移植性等同于 CUDA 生态的二进制覆盖或应用成熟度。[A037][A038][A039]

### 二、研究对象的六层定义

**架构（architecture）** 是一组设计规则，例如 Zen 5、RDNA 4、CDNA 4 或 XDNA 2。它说明前端、执行资源、数据类型、缓存和调度方式如何变化，但不自动给出产品的核心数、频率与功耗。**裸片（die）** 是实际制造的硅，例如 CCD、IOD 或单片 APU；同一架构可以落在多种裸片，同一产品也可组合多颗裸片。**封装（package）** 决定裸片、缓存、HBM、基板和外部接口如何连接。**产品家族（family）** 是市场与兼容性集合，例如 Ryzen 9000、Ryzen AI 300、Radeon RX 9000。**SKU** 是具有确定核心数、频率、内存、接口和功耗范围的料号。**OEM 设备** 最终决定散热、固件、内存颗粒、屏幕、供电和驱动镜像。[A003][A012]

报告避免三个常见混淆。其一，代号不是产品名，工程代号只能辅助理解，不作为兼容性依据。其二，“4nm”“3nm”等节点名称是代际标签，并非可直接比较的几何尺寸或功耗承诺。其三，TDP、PPT、TBP 和整机墙上功耗不是同一量。桌面 CPU 的默认 TDP 用于平台热设计，实际插槽功率受 PPT、温度、负载类型和 BIOS 控制；GPU 的 TBP 接近整卡预算；移动处理器还受 OEM 可配置范围约束。[A007][A014]

### 三、证据等级

一级证据包括 AMD 产品规格、架构白皮书、开发者文档、驱动支持矩阵、SEC 申报和官方 GitHub。它们用于回答“产品是什么”“正式支持什么”和“公司披露什么”。二级证据包括微软、Sony、Valve、ASUS 等平台拥有者的正式规格，用于验证半定制芯片进入何种终端。第三方实验只适合回答特定机器在特定配置下怎样运行，不适合反推出整个家族；本版没有用无法完整保留环境的跑分填充结论。[A021][A073][A075]

厂商性能宣称保留脚注条件。若 AMD 写“最高 50 TOPS”，这里只把它记为指定 NPU 数据类型下的理论峰值；若写“快 X%”，需要同时记录比较对象、BIOS、内存、驱动、应用版本、功耗模式与统计方法。缺少任何关键条件时，正文只表述产品方向，不复述倍数。峰值浮点、带稀疏矩阵峰值、游戏帧率、编译时长、LLM token/s 和每瓦性能属于不同量纲，禁止做一列式排名。[A005][A034]

### 四、如何阅读规格与产品矩阵

`data/product-matrix.csv` 是代表性矩阵，不是所有地区、OEM 和嵌入式生命周期料号的穷举。字段中的架构、核心数、内存和 I/O 以具体行对应的 SKU 为准；空值表示来源未给出或该字段不适用，不表示零。对 APU，“显存”通常来自系统内存，容量由固件和操作系统策略决定；对独显与 Instinct，板载显存/HBM 是明确物理资源；对半定制 SoC，统一内存容量是整机配置而不是 AMD 零售 SKU 选项。[A034][A003]

选型应从任务反推约束。交互办公与轻量 AI 先看待机、媒体引擎、OEM 电池和软件启用；游戏看目标分辨率、帧时间、显存与显示链，不只看平均帧率；本地大模型看可用内存、内存带宽、量化、运行时和首 token 延迟；内容生产看应用认证、编解码和显存；编译、渲染、仿真看核心扩展、内存通道与 PCIe；数据中心看可靠性、集群互连、容器和生命周期。只要任务换了，所谓“旗舰”就可能不是最优解。

### 五、核心判断

1. **Zen 的价值已从核心 IPC 扩展到产品工程。** Zen 2 之后的 CCD/IOD 分工，使 AMD 可以跨桌面、工作站和服务器复用计算裸片；Zen 3 统一核心复合体改善核心间共享 L3；Zen 4 引入 DDR5/PCIe 5.0 平台；Zen 5 加宽前端和向量数据路径。收益最终仍由缓存命中、内存和功耗边界决定。[A030][A024][A023]
2. **X3D 是以容量换延迟与频率约束的工作负载优化。** 大 L3 能显著降低部分游戏和仿真的 DRAM 访问，却不能保证所有生产力任务提升；双 CCD 型号还涉及线程和缓存域调度。第二代 3D V-Cache 改善封装与热设计，但仍应按具体 SKU、BIOS 和调度器验证。[A009]
3. **RDNA、CDNA 和 XDNA 分工大于名称相似性。** RDNA 面向图形和通用媒体/计算，CDNA 面向高吞吐矩阵与 HPC，XDNA 面向低功耗、受编译器约束的端侧推理。三者可以共享工具链资产，却有不同指令、内存和部署目标。[A036][A041]
4. **统一内存不是无限显存。** Ryzen AI Max 的宽接口和共享物理内存降低 CPU/GPU 拷贝，并允许给 iGPU 分配较大空间，但带宽、时延、页面驻留和系统保留仍存在；它也不自动获得离散 GPU 的专用显存带宽、显卡可替换性或完整软件支持。[A004][A051]
5. **AMD 的完整护城河是 IP 组合与客户覆盖。** Xilinx 自适应计算、Pensando DPU、EPYC、Instinct、Ryzen 与半定制共同覆盖控制、标量、图形、矩阵、网络和可编程逻辑；挑战是把这些硬件整合成稳定、易部署、版本一致的软件体验。[A063][A067][A001]

### 六、更新与可复核规则

所有动态页面以访问日期为界。驱动、ROCm、Ryzen AI Software 或路线图发生变化时，应先更新来源账本，再修改章节与 CSV，最后重新装配。删除来源必须同时删除正文引用；新增来源必须至少支撑一个可定位判断。新闻稿中的计划时点不是出货证明，产品页出现 SKU 也不证明所有地区已有零售库存，OEM 宣布设备也不证明全部 AI 功能已启用。

仓库的脚本是最低质量门，而不是事实正确性的替代品。引用闭包只能验证正文 ID 已定义、来源被至少一篇正文使用、来源 URL 唯一，以及 CSV 的来源 ID 可解析；它不能验证某个来源是否真的支持紧邻句子的语义。链接状态只能证明请求时的可访问性，重复检查只能发现机械复制。每次发布仍必须人工逐句抽审事实—来源映射，确认口径一致、反例存在、因果没有越界，并把未知保留为未知。本版对 CPU 历史、MI300 发布框架、Helios 与年度节奏完成了专项人工复核。[A082][A083][A084][A085][A086][A001]

### 七、对比结论的有效期

硬件的物理容量和接口相对稳定，软件支持、驱动性能、OEM库存与价格却会迅速变化。读者复用结论时应先判断变量属于哪一类。CPU核心数、显存容量可以直接从料号复核；ROCm组件、NPU应用、游戏超分与云实例必须重新查当期矩阵；促销价、地区供货和厂商相对性能只对原日期有效。一次更新若只改动态变量，不应顺便重写没有新证据的架构历史。

跨厂商结论还要保持对称证据。不能用 AMD 默认功耗对竞争平台解除功耗的结果，也不能用 AMD 零售台式机对 Apple 整机或 Qualcomm 工程样机；不能给一方最新驱动、另一方旧驱动。若无法对齐，就把比较拆成能力、系统和软件三个小结，陈述各自已证实事实，而不强求总冠军。

结论应包含失效条件。例如“X3D适合目标游戏”在分辨率、显卡或引擎更新后可能失效；“Ryzen AI Max适合大共享内存推理”在模型后端不支持或独显容量扩大后需重评；“ROCm不支持某卡”在新版本矩阵加入后失效。写出失效条件，使报告成为可更新的判断系统，而不是冻结的品牌印象。

---

## 从 K8、Bulldozer 到 Zen 5：AMD CPU、GPU、NPU 架构演进

### 1. 架构史应该回答什么

架构演进不是把代号按年份排成海报，而是解释每一代为什么改变、改变落在什么层级、产品为何仍可能表现不同。处理器性能近似受指令数、每指令周期和频率共同约束，但真实任务还会遭遇分支预测失败、缓存未命中、内存带宽、同步、操作系统调度、功耗和温度。GPU 也不能只数 CU：前端、纹理、光栅、光追、矩阵数据路径、缓存、显存和驱动都会改变有效吞吐。NPU 更依赖编译器能否把图划分为受支持算子。

因此本章把变化拆成五条线：CPU 核心、计算裸片与 I/O、缓存和内存、图形/计算 GPU、端侧 NPU。每条线都把微架构与具体产品分开。Zen 5 是 CPU 架构，Ryzen 9000 是产品家族，9950X 是 SKU，CCD 是裸片类型，AM5 是平台；把它们混为“一代”会产生错误的兼容性结论。[A003][A023]

### 2. K8：集成内存控制器与可扩展互连

K8 时代最值得保留的思想不是某一频率，而是把内存控制器从北桥移入处理器，并以点到点 HyperTransport 连接系统。这样做把核心访问本地内存的路径缩短，也让多插槽形成 NUMA：每颗处理器有更近的本地内存，访问另一插槽则跨互连。消费端感受到的是更直接的内存时延，服务器软件必须学习线程与页面亲和性。后来 Infinity Fabric、chiplet IOD 与多路 EPYC 仍沿用“数据位置决定代价”的原则。

K8 的 x86-64 扩展使 64 位通用计算进入大众 PC。它并不是简单增加寄存器位宽：更多通用寄存器、更大地址空间与新的调用约定一起改变编译器和操作系统。架构成功也提醒我们，ISA 兼容只保证程序语义，不保证相同微架构速度；老程序可以运行在新 Zen 上，却会因前端、缓存、分支和向量化差异展现完全不同的瓶颈。AMD 针对 Family 10h/12h 的软件优化指南仍把缓存、预取、内存访问和分支作为核心议题。[A082]

早期多核通过在同一封装集成核心，逐渐把共享缓存、一致性和内存控制复杂度带进消费端。此后每一次核心数增长都要回答三问：线程是否足够并行；工作集是否能被缓存；内存与互连能否供数。只看“核心翻倍”而不回答这三问，会高估桌面交互和游戏、低估编译与渲染，也会忽略许可证按核心计费的成本。

### 3. Bulldozer：共享模块的教训

Bulldozer 的核心组织试图在吞吐、面积和频率之间取新平衡。一个模块包含两个整数集群，却共享取指、解码以及部分浮点资源。营销上的“核心”计数与软件可获得的完整资源并非一一对应：两个线程竞争共享前端或浮点单元时，扩展不像两个独立大核；单线程又承受较长流水线和分支失败代价。这不是说共享资源必然错误，而是共享粒度必须匹配目标负载。

Family 15h 优化指南要求软件理解模块资源、调度、缓存和指令选择，这恰好说明“兼容 x86”不等于无需优化。[A083] Windows 调度器是否把两个繁重线程放进同一模块，会影响性能；编译器发出何种向量指令也会改变共享浮点路径压力。Bulldozer 后续 Piledriver、Steamroller 与 Excavator 逐步改良前端、预测和能效，却没能消除基本取舍给高性能桌面带来的印象。

这段历史对今天仍有两个意义。第一，核心数必须结合资源拓扑解释。Zen 5c 是面积优化核心，但不是 Bulldozer 式共享模块；Ryzen AI 300 某些 SKU 混合 Zen 5 与 Zen 5c 时，两类核心仍实现相同 ISA 语义，却在缓存、面积和频率目标上不同。第二，峰值频率只有在供电、温度和关键路径允许时出现，长流水线不能凭频率自动赢得每瓦性能。

### 4. Zen 1：重新建立独立核心与可组合单元

Zen 的战略转折是回到更强的独立 SMT 核心，并把四核 Core Complex 作为可组合单元。前端更强、分支预测和微操作缓存降低取指瓶颈，整数与浮点资源更均衡，SMT 让一个核心在部分流水线空闲时容纳第二线程。第一代 Ryzen 把高核心数重新带到主流桌面，但跨 CCX 访问的延迟、内存频率与 Fabric 时钟关系会显著影响游戏和延迟敏感负载。

第一代 Zen 也是平台学习期。内存训练、主板 BIOS、调度器和应用优化会让首发体验与成熟期不同。这形成一个贯穿 AMD 产品的规律：新硅只能定义能力上限，AGESA、BIOS、操作系统、电源计划和应用更新决定能力何时变为用户体验。评测必须注明固件日期；用首发旧 BIOS 代表整个生命周期，或用多年后调校代表首发状态，都不严谨。

Zen 的缓存层次和预取需要软件保持局部性。Family 17h 优化指南建议开发者关注数据对齐、分支、缓存冲突、预取和 NUMA，说明硬件并不能消除数据布局的重要性。[A084] 对消费者来说，“更快内存”的收益不是固定百分比：当任务被 GPU、存储、网络或纯计算限制时，内存变化可能很小；当核心频繁跨复合体或访问 DRAM 时，时序和 Fabric 才更关键。

### 5. Zen+ 与 Zen 2：制程改良到 chiplet 分离

Zen+ 主要通过工艺、延迟和频率管理改良第一代设计。Precision Boost 更细粒度地利用电流、温度与负载余量，揭示现代处理器并无单一“运行频率”：不同核心数、指令类型和散热条件会形成动态频率曲线。购买者应区分最大 boost 的短时单核条件与全核持续频率。

Zen 2 的决定性变化是把 CPU 计算核心放入 7nm CCD，把内存控制器、PCIe 与其他 I/O 留在单独 I/O Die。chiplet 让小计算裸片提高晶圆利用率，又可用不同数量覆盖 Ryzen、Threadripper 与 EPYC。AMD 的 chiplet 方法不是把裸片随意拼接；一致性、时钟、电源、测试、封装走线、固件和错误隔离都必须共同设计。[A030]

CCD/IOD 分离也引入物理代价。核心访问 DRAM 必须经过 Fabric 到 IOD；多 CCD 之间共享数据也跨封装互连。对高吞吐多线程，这种代价常被更多核心和缓存掩盖；对延迟敏感、小工作集频繁共享的任务，拓扑可能决定性能。Zen 2 每个四核 CCX 共享一部分 L3，同一 CCD 内仍有两个缓存域，线程放置影响游戏与实时负载。

chiplet 的经济价值来自复用。高成本先进节点用于密集计算，成熟节点用于模拟/I/O，服务器可以增加 CCD 而桌面保留较少 CCD。产品分档还能利用部分核心或缓存有缺陷但其他功能良好的裸片。不过，外部观察者不能从家族名推断每颗 SKU 的所有裸片：APU 常用单片设计以降低移动功耗和封装占用，桌面 Ryzen 则多用 CCD+IOD。

### 6. Zen 3：统一八核缓存域

Zen 3 把一个 CCD 内八个核心组织为共享 32MB L3 的核心复合体，减少相邻核心访问另一半 L3 的边界。这对线程相互通信、游戏主线程与工作线程共享数据尤其重要。官方 Zen 技术入口把代际改进归入分支、执行和缓存，但实际产品收益还取决于频率、内存和软件。[A024]

统一缓存并不等于无代价的共享内存。L3 容量有限，多个线程仍会互相驱逐；跨 CCD 仍比同 CCD 更远；缓存一致性会消耗带宽。高核心数 Ryzen 9 和 Threadripper 的调度需要兼顾负载均衡与局部性。把线程固定到单 CCD 有时改善延迟，有时却浪费核心，因此不应把一种绑核技巧写成普遍优化。

Zen 3 也奠定 3D V-Cache 的软件基础：如果整个八核域可访问大容量 L3，堆叠缓存可让游戏工作集更长时间停留片上。首代 X3D 产品以频率与超频限制换缓存容量，说明架构优化总有预算约束。缓存容量增加不会提高 DRAM 物理带宽，只是减少去 DRAM 的次数。

### 7. Zen 4：DDR5、PCIe 5.0 与平台转换

Zen 4 把桌面平台转向 AM5、DDR5 和 PCIe 5.0，并为消费产品普及 AVX-512 能力。这里的 AVX-512 不能只按指令名判断实现宽度和吞吐；软件应通过特性检测和实测选择路径。更宽向量可以减少指令数，却增加功耗密度，并非所有持续任务都能保持最高 boost。

AM5 平台把插槽寿命、主板供电、I/O 和内存训练置于产品价值中心。CPU 支持 PCIe 5.0 不保证每块主板把同样通道接到显卡和多个 M.2；芯片组、布线、重定时器和固件共同决定可用拓扑。DDR5 标称传输率也不等于应用带宽，双 DIMM 每通道、不同容量颗粒和 EXPO 超频都会改变训练与稳定性。

Zen 4c 用更紧凑布局追求同面积更多核心，主要进入云服务器，随后也影响移动异构组合。它保留软件可见的一致 ISA，调度器不需要像 Arm 大小核那样在不同指令集间切换，但频率、缓存和功耗曲线不同。高密度核心适合大量并发，小量延迟敏感线程仍可能偏好全尺寸核。

### 8. Zen 5：加宽而不是单一参数翻倍

Zen 5 的官方描述强调更强分支预测、更宽流水线和向量路径、更大的乱序窗口；Ryzen 9000 产品页给出的代际 IPC 是厂商选定工作负载的几何平均，不能直接套到每个应用。[A002][A023] 更宽前端可以每周期把更多工作送进后端，更多执行端口和调度能力提高指令级并行，但前提是分支正确、数据已在近端缓存、指令之间有足够独立性。

Zen 5 的双管线取指/解码思想减少单一路径阻塞，改进预测降低错误路径浪费；更宽浮点和向量资源有利于科学、媒体与 AI 前后处理。与此同时，复杂后端提高面积和功耗，移动产品需要与 Zen 5c 混合，服务器则可用不同 CCD 面积密度服务高频与高并发。架构名相同不意味着桌面 CCD、移动单片与服务器 CCD 有相同缓存和频率。

Ryzen 9 9950X 是两个 CCD 加 IOD 的 16 核具体实现，Ryzen 7 9700X 是单 CCD 8 核 65W 档，二者都叫 Ryzen 9000，却面对完全不同的散热、线程和成本边界。[A007][A008] 9950X 更适合可扩展生产力；9700X 可能在轻负载、能耗和平台成本上更合理。最大 boost 是单核突发上限，不能和 170W/65W TDP 直接换算性能。

正式后续披露必须保持克制。AMD 若在 Financial Analyst Day 展示后续 Zen 名称、平台或时间范围，只能记录台上已发布信息。[A065] 没有数据表时，不推断核心数、缓存、节点、插槽或最终 SKU；合作伙伴路线图也不能代替 AMD 的产品可用性声明。

### 9. 3D V-Cache：把封装变成架构变量

3D V-Cache 通过垂直互连把额外 SRAM 堆叠到计算裸片，增加 L3 而不扩大 CCD 平面面积。[A029] 它的价值来自降低未命中率：大型游戏世界、模拟、EDA 和部分数据库若工作集处于几十到上百 MB，更多 L3 可避免高延迟 DRAM。若任务流式扫描远大于缓存的数据，或主要受计算单元限制，容量收益有限。

堆叠会改变热路径和频率预算。第一代产品把缓存放在核心上方，热量更难散出；第二代封装调整堆叠关系，提高频率和超频灵活性。不能因此断言所有 X3D 都比非 X3D 更节能：高命中任务会减少外部访问，低命中或高频生产力则可能由频率、核心数和功耗主导。

双 CCD X3D 还带来不对称缓存域。一个 CCD 有额外缓存，另一个可能保持高频。操作系统与驱动需要识别游戏和生产力偏好，将关键线程放到合适 CCD。调度错误会让理论优势转化为跨 CCD 通信。评价 9950X3D 一类产品必须注明 Windows 版本、芯片组驱动、Game Mode、BIOS 和后台负载。

### 10. 从 GCN 到 RDNA：图形效率路线

GCN 采用适合图形与通用计算的宽波前执行模型，长期覆盖 Radeon 与早期 Instinct。它在异步计算和通用性上有优势，但消费图形的每瓦、前端和缓存需要重新聚焦。RDNA 1 以新的 Workgroup Processor、Wave32 选项和缓存层次提高游戏效率，同时保留必要计算能力。软件波大小影响寄存器占用、分支发散和 SIMD 利用，不能只由 CU 数解释。

RDNA 2 引入硬件光线求交、Infinity Cache，并进入 Radeon RX 6000、游戏机和多代 APU。Infinity Cache 用较大片上缓存降低对外部 GDDR6 带宽的压力，作用类似“减少昂贵远端访问”，但高分辨率、光追和大纹理仍可能穿透缓存。Xbox Velocity Architecture 把定制 SoC、NVMe、解压与 API 串成数据路径，说明终端性能来自平台协同而非 GPU 单项。[A074]

RDNA 3 把高端 GPU 做成 Graphics Compute Die 与 Memory Cache Die 的 chiplet 组合。RX 7900 XTX 的产品规格应按整卡理解：GCD 执行图形，MCD 承担缓存与内存接口，GDDR6 容量、Infinity Cache、板卡功耗和显示引擎共同决定体验。[A015] GPU chiplet 的互连带宽与延迟需求不同于 CPU，不能把 CCD/IOD 经验直接复制。

RDNA 3.5 是面向 APU 的演进，重点是能效、媒体和集成环境；Ryzen AI 300 与 Ryzen AI Max 的 CU 在共享系统内存上运行。40 CU 的 Radeon 8060S 在规模上接近独显计算阵列，却没有独立 GDDR6，性能高度依赖 256-bit LPDDR5X、功耗分配与散热。[A006][A051] 同一 SoC 的 CPU 和 GPU 会争用封装功率与内存，因此 CPU 满载加 GPU 满载的持续结果不能由各自峰值相加。

RDNA 4 强化光追与 AI 路径，并与 FSR 4 的机器学习超分结合。RX 9070 系列的官方资料给出 4nm、PCIe 5.0、DisplayPort 2.1a 等产品字段，但厂商相对帧率仍依赖游戏、画质和驱动。[A014] RDNA 4 的“AI 加速器”与 XDNA NPU不相同：前者位于 GPU，适合图形和高吞吐计算；后者以低功耗、受支持图为目标。

### 11. CDNA：从图形分叉的数据中心计算

CDNA 删除或弱化消费图形所需固定功能，把晶体管和功耗集中在矩阵、向量、缓存、高带宽内存和多 GPU 互连。它服务 HPC 与训练/推理，不是“没有显示输出的 Radeon”。软件使用 ROCm、HIP、数学库和通信库，部署单位往往是 OAM 模组、八 GPU 平台乃至机柜。[A036]

CDNA 2 的 MI200 采用多计算裸片和 HBM，面向 FP64 HPC 与矩阵；CDNA 3 的 MI300 将 CPU/GPU chiplet、IOD 与 HBM 的组合推得更深。MI300A 把 Zen CPU 和 GPU 放入统一封装用于加速计算，MI300X 则将资源集中给 GPU，提供 192GB HBM3，适合大模型容量需求。[A035] 两者名字接近，内存一致性和主机角色却不同。

CDNA 4 的 MI350 系列增加低精度格式和 288GB HBM3E、最高 8TB/s 理论带宽。[A034] “10.1 PFLOPS”之类数字只有在指定 MXFP/FP8、矩阵、稀疏与 boost 条件下成立；HPC FP64、训练 BF16 和推理低精度不可互换。大 HBM 也不消除通信：模型超出单卡、做数据并行或专家并行时，GPU 间与节点间网络仍决定扩展效率。

### 12. XDNA：端侧推理的新执行域

XDNA 源自 Xilinx 的 AI Engine 思路，由可编程计算阵列、片上存储和数据流互连组成。NPU 擅长持续、规则、可编译的张量图，以较低功耗处理摄像头效果、语音、背景分割和小模型。它不是可以任意运行 CPU 程序的通用核心，也不是大显存训练 GPU。

第一代 Ryzen AI 已进入 7040/8040 部分产品，XDNA 2 在 Ryzen AI 300/Max 提高 NPU 峰值至最多 50 TOPS并支持更丰富精度。[A005] 但“50 TOPS”只描述一条理论计算路径。实际图如果含未支持算子，会被切到 CPU/GPU；切分和张量拷贝可能吞掉收益；模型还需量化、编译和缓存。Ryzen AI Software 文档与 GitHub 示例才是判断模型可部署性的直接依据。[A041][A042]

Windows 应用可能通过 Windows ML/ONNX Runtime、DirectML 或厂商执行提供者选择 NPU/GPU；Linux 则依赖 XDNA 驱动与用户态组件。AMD 的开源 XDNA 驱动显示设备与队列接口正在进入 Linux，但源码存在不等于发行版、应用和所有 SKU 已支持。[A045] 端侧 AI 的体验应分四层验收：设备枚举、模型编译、正确性、持续功耗；只看到任务管理器的 NPU 图表不足以证明应用全程运行在 NPU。

### 13. Infinity Fabric：共同语言，不是无限带宽

Infinity Architecture 是 AMD 连接 CPU、GPU、缓存、内存控制器和加速器的技术集合。[A030] 在 Ryzen chiplet 中，Fabric 承担 CCD 与 IOD 的数据和控制；在服务器中，它扩展到更多 CCD、内存通道和插槽；在 Instinct 中还有 GPU 间链路。名称相同不表示各产品带宽、协议或一致性完全一样。

互连必须在延迟、带宽、功耗和封装走线间平衡。小数据、指针追踪关注往返延迟；流式访问关注持续带宽；一致性流量还会消耗链路。提高 Fabric 时钟可能改善某些内存延迟，却增加功耗并受硅和内存训练限制。对 Zen 2/3 的经验参数不能机械套到 DDR5 时代。

Infinity Fabric 也不等于软件看到一块无边界内存。多 CCD 共享地址空间，但缓存和访问距离不同；多 GPU 即便互连，显存通常仍由每个设备单独分配。程序需要显式选择设备、复制或用运行时管理统一虚拟地址。产品宣传中的“统一”必须继续追问物理位置、一致性、页面迁移和失败语义。

### 14. 从架构到实际持续性能

持续性能是一条闭环：工作负载产生指令和数据，硬件消耗电流，封装把热传到散热器，固件根据温度、电流、功率和可靠性调频，操作系统再调度下一批线程。任何环节达到限制，峰值都会下降。移动整机还共享 CPU/GPU/NPU 功耗预算，掌机更受皮肤温度、风扇噪声和电池容量约束。

验证 CPU 应至少记录单核、全核、内存和长时运行；GPU 记录平均帧率、低分位帧时间、显存、功耗、温度和时钟；NPU 记录编译成功率、端到端时延、CPU 占用和能量；混合任务记录资源争用。短跑测试可以发现峰值能力，却不能代表一小时渲染或长时间游戏。

架构升级带来的提升并非线性叠加。更强预测减少浪费，更多缓存减少 DRAM，chiplet 增加可扩展性，先进工艺提高密度；但每项都可能遇到新瓶颈。当 CPU 更快后，GPU 可能成为限制；GPU 更强后，共享内存或散热可能成为限制；NPU 更省电后，模型前后处理可能留在 CPU。深度分析的任务就是找出瓶颈迁移，而不是为每代写一个更大的百分数。

### 15. 架构判断清单

面对一个新 AMD 名称，依次问：它是微架构、裸片、封装、家族还是 SKU；CPU 核是 Zen 5 还是 Zen 5c，数量和缓存域怎样；GPU 是 RDNA、CDNA 还是没有；NPU 是否存在、属于哪代 XDNA；内存是可更换 DDR、焊接 LPDDR、独立 GDDR 还是 HBM；I/O 经 CPU 还是芯片组；功耗是默认、可配置范围还是峰值；软件支持矩阵是否点名该设备；OEM 是否实际启用；数据来自产品页、路线图还是测试。

这组问题能过滤多数营销误读。它也解释 AMD 的产品体系为何同时统一与复杂：统一的是可复用 IP 和软件方向，复杂的是每个市场对延迟、容量、带宽、扩展、生命周期、可靠性和成本的不同约束。

---

## AMD 消费端产品：桌面、移动、工作站、显卡、掌机与半定制

### 1. 消费产品不是一条性能阶梯

AMD 的消费端至少包含六种购买逻辑。桌面 Ryzen 追求可升级平台和插槽功耗；移动 Ryzen 在电池、厚度与共享功耗内平衡；Ryzen AI Max 用宽内存和大 iGPU 创建紧凑工作站；Threadripper 用内存通道与 PCIe 扩展换取成本和功耗；Radeon 独显提供专用显存和可更换图形；Ryzen Z 与半定制 SoC 则为固定终端重做功耗、内存和 I/O。型号数字接近不代表定位接近。

购买应先确定“终端合同”。桌面盒装 CPU 通常允许用户选主板、内存、散热和显卡；笔记本 APU 的内存与功耗由 OEM 固化；掌机把屏幕、控制器和电池纳入体验；游戏机规格在多年周期内固定，开发者可深度优化；工作站需要 ECC、管理、认证和大量扩展。对比处理器前必须先对齐这些合同。

### 2. 桌面 Ryzen：AM5 上的分层

Ryzen 9000 基于 Zen 5，主流层从 6 核到 16 核，以一个或两个 CCD 配合 IOD。Ryzen 5 9600/9600X 面向游戏和日常；Ryzen 7 9700X 提供 8 核单 CCD；Ryzen 9 9900X/9950X 增加第二 CCD，用于编译、渲染与多任务。AMD 桌面产品页列出核心、boost 和默认 TDP，但这些字段不能直接预测整机性能。[A002]

9950X 的 16 核/32 线程、最高 5.7GHz 和 170W 默认 TDP描述了 SKU 边界，而不是同时发生的稳态。[A007] 最高 boost 是单个核心在突发轻线程、温度和电流有余量时的上限；全核 AVX、渲染或编译的频率更低。主板可能用 PBO、厂商增强或过高电压改变默认行为，评测必须重置 BIOS 并说明 EXPO、PBO 与散热。

9700X 的 8 核/16 线程和 65W 默认 TDP适合展示能效分档。[A008] 它与 9950X 使用相同 Zen 5 核，并不意味着一半核心就恰好一半性能：单线程接近，多线程取决于可并行比例；单 CCD 避免部分跨 CCD 通信；较低功耗更易维持安静。对只用少量核心的游戏，预算投入显卡可能比第二 CCD 更有效。

Ryzen 9000 仍带基础集成 Radeon 图形的部分 SKU，主要用于显示输出、诊断和轻量媒体，不应与 8000G 或 Ryzen AI Max 的大 iGPU 混淆。带 `F` 的具体 SKU 可能要求独显，必须查规格数据库而非依赖家族印象。[A003] 同理，盒装是否含散热器、ECC 是否得到主板验证、可用 PCIe 通道如何分配，都要落到 SKU 与主板。

AM5 的长期价值来自平台而不只 CPU。相同插槽可以跨多代升级，但 BIOS 容量、供电、内存 QVL 和厂商支持决定具体主板能否稳定运行新 CPU。PCIe 5.0 通道可能分给显卡或 M.2，芯片组下游设备共享上行带宽。对多 NVMe、采集卡和高速网卡的用户，主板框图比“X870/B850”标签更重要。

### 3. X3D：游戏优化不是万能旗舰

X3D 型号通过 3D V-Cache 提高 L3。9800X3D 的单 CCD 八核共享大缓存，避免双 CCD 的缓存不对称，适合大量游戏；官方 SKU 页提供核心、频率和缓存字段。[A009] 其优势来自工作集命中：引擎对象、物理状态、绘制调用和脚本数据若留在 L3，就能降低 DRAM 延迟和流量。

缓存并不提高 GPU 着色速度。4K 最高画质常由显卡限制，CPU 差异会缩小；竞技游戏在低画质高帧率下更容易体现 X3D。生产力也分化：编译和部分仿真可能受益，纯向量计算或长时间渲染更看核心数、频率和内存。购买前应选自己的分辨率、显卡与游戏，而不是引用 1080p 顶级显卡平均值。

双 CCD 的 9900X3D/9950X3D 同时服务游戏和重生产力。调度器要把游戏关键线程放到带大缓存 CCD，后台与可扩展任务可用另一 CCD。驱动、Windows Game Mode 和游戏识别失败时，手工绑核可能暂时改善，却会损害后台吞吐。最佳实践是先更新 BIOS 与芯片组驱动，再用每 CCD 计数器验证，而不是永久禁用一半核心。

第二代堆叠缓存改善散热路径和频率余量，使 X3D 与高频之间的取舍缩小，但不消失。PBO 和 Curve Optimizer 改变稳定性；数据损坏风险不能用一轮跑分排除。工作站长期任务优先默认稳定性，游戏调校也应通过内存测试、向量负载和多小时真实游戏共同验证。

### 4. Ryzen 8000G 与桌面 APU

桌面 APU 把较强 Radeon iGPU 与 CPU、媒体引擎和内存控制器放在单封装，为不装独显的小机箱提供完整能力。它适合办公、家庭服务器、轻量游戏与以后再加独显的入门系统，但与同名纯 CPU 可能有不同 L3、PCIe 通道和裸片来源。Ryzen 8000G 不能仅按“8000 比 7000 新”判断 CPU 性能。

iGPU 没有独立显存，DDR5 带宽和双通道配置决定性能下限。单条内存会砍掉并行通道，高延迟低速套条会让 GPU 更快遇到带宽墙。UMA 帧缓冲预留过大会减少操作系统内存，过小又可能让应用误判；现代驱动可动态借用系统内存，固定预留并非越大越快。

桌面 APU 的持续性能通常比超薄笔记本稳定，因为散热和插座供电宽裕；但小型机也可能用低矮散热器、受限 VRM 和封闭机箱。mini PC 厂商有时允许 45W、65W 或更高模式，同一处理器的噪声和性能差别明显。规格矩阵把“处理器能力”与“整机行为”分栏，避免把某一台机器代表整个 APU。

### 5. 移动 Ryzen：名字必须和功耗曲线一起读

移动产品覆盖超轻薄、主流、游戏本和移动工作站。后缀 U、HS、HX 传达定位，却不保证固定瓦数；OEM 可在官方范围内配置长期功耗和温度。AMD 移动产品入口列出家族与合作设备，但最终 RAM 通道、屏幕、无线、风扇和电池来自 OEM。[A011]

评估移动 CPU 要至少做冷启动突发、十分钟持续和电池三种状态。插电首轮跑分可能利用较高短时功耗；机身热浸后频率降低；拔电后 OEM 策略又会限制 CPU/GPU。把三个状态混成一个“性能分”会误导移动使用。续航也依赖屏幕面板、刷新率、后台软件、无线与电池，处理器制程只是因素之一。

Phoenix/Hawk Point 类 APU 把 Zen 4、RDNA 3 与 XDNA 放入轻薄功耗；Ryzen AI 300 转向 Zen 5/Zen 5c、RDNA 3.5、XDNA 2。异构 CPU 核并非不同 ISA，调度难点更多是哪个线程需要高频、哪个适合密集核。游戏本若配独显，还要处理 MUX、混合显示、独显唤醒和共享散热。

内存类型形成不可升级性取舍。LPDDR5X 可提供较高带宽和低待机功耗，通常焊接；DDR5 SO-DIMM 可升级，但占空间且信号/功耗不同。购买本地 AI 或 iGPU 机器时，容量应按未来模型和共享需求预留，因为 16GB 焊接内存在生命周期内无法补救。

### 6. Ryzen AI 300 与 AI 400：能力、启用、可用

Ryzen AI 300 把最多 12 个 Zen 5/Zen 5c CPU 核、RDNA 3.5 iGPU 和最高 50 TOPS XDNA 2 NPU 集成，面向 Copilot+ 与本地 AI PC。[A005] Ryzen AI 400 是后续家族扩展，官方消费页在快照日列出 AI Max、AI 400、AI 300；部分 SKU 覆盖笔记本与桌面形态。[A004] 名称更新不代表每个子模块都换代，必须查具体核心、GPU、NPU 和内存。

必须区分三层时间。**芯片能力**从 AMD 数据表确认，例如 NPU 峰值、GPU CU 和支持内存；**OEM 启用**需要 BIOS、驱动、散热和具体内存配置；**终端功能可用**还取决于 Windows 更新、应用版本、地区和 ISV 模型。AMD 自己在 Ryzen AI 页脚写明 OEM 与 ISV enablement 必需，某些功能可能尚未优化。[A004]

50 TOPS 不是整颗 SoC 的所有 AI 性能，也不是可和 GPU FP16 简单相加的“总 AI”。NPU 的 INT8/块浮点峰值适合受支持推理；GPU 适合更大模型和图形并行；CPU 处理控制流、tokenization、未支持算子和小批量。实际应用可在三者间切分，跨设备张量转换会增加开销。

Copilot+ 的门槛确认平台具备一组硬件和系统能力，却不保证每个第三方应用使用 NPU。验证方法是使用系统跟踪或运行时日志，确认节点分配；测量端到端时延和能耗，而不是只看 NPU 利用率。离线隐私也不是硬件自动保证：应用仍可能把提示或遥测发送云端，应检查网络和隐私策略。

Ryzen AI 400 在 2026-03-02 的官方发布中扩展到桌面/工作站形态，继续组合 Zen 5、RDNA 3.5、XDNA 2、最高 50 TOPS。[A081] 其意义是让可插电、散热更强的平台获得 NPU，而不是证明 NPU 架构发生根本改变。OEM 小工作站可能给更高持续功耗，但 NPU 支持仍按具体 SKU 和软件矩阵。

### 7. Ryzen AI Max：宽共享内存的新类别

Ryzen AI Max+ 395 以 16 核 Zen 5、40 CU Radeon 8060S、XDNA 2 与 256-bit LPDDR5X 为代表，默认 55W、可配置范围由具体产品定义。[A051] 它不是传统“CPU 加小核显”，也不是可更换显卡；设计目标是用高集成度、宽共享内存和较大容量在紧凑机器里同时服务 CPU、图形与 AI。

对本地大模型，最大优势是 GPU 可访问比典型移动独显显存更大的系统内存区域，减少模型因显存容量被迫分层到 CPU 的情况。优势不是“统一内存等于零拷贝且无限快”：操作系统保留、UMA 分配、页面管理和带宽仍限制；模型框架必须支持 gfx1151；量化与 KV cache 会继续占空间。

对游戏，40 CU 与宽 LPDDR5X 提供强 iGPU 基础，但 OEM 功耗和内存速率决定实际帧率。与独显比较必须对齐整机功耗和价格：独显有 GDDR6 与独立功率预算，Max 共享封装功率；Max 则减少板级芯片、体积与复制。外接独显可能经过有限 PCIe 通道，具体机器要查拓扑。

对创作者，媒体引擎、共享内存和 16 核 CPU 可在剪辑、渲染、图像与本地推理间灵活分配。专业应用认证和插件支持仍可能偏好 Radeon PRO 或竞争平台。购买者要先列出必须运行的软件和版本，再查供应商认证，不应把硬件资源推断成完整应用兼容性。

Max 产品也暴露维修与升级取舍。大容量 LPDDR 通常焊接，购买时必须一次选够；整机主板故障会连带内存与处理器；但集成减少插槽和显卡接点，移动可靠性可能受益。mini PC 可用更大散热获得长时性能，笔记本则提供电池和屏幕，两者即便同 SKU 也不是同一体验。

### 8. Threadripper：HEDT 与工作站的真正分界

Threadripper 9000 面向高端桌面，最高 64 核；Threadripper PRO 9000 WX 最高 96 核，并提供企业管理和更多平台资源。[A010] 二者核心架构同为 Zen 5，但价值不在“比 Ryzen 多核”这么简单，而在内存通道、PCIe 通道、ECC、平台验证和长任务吞吐。

TRX50 支持四通道内存和最高 80 条 PCIe 通道的定位，既可运行非 PRO 也可运行部分 PRO；WRX90 提供八通道和更大 PCIe 5.0 扩展，并有 PRO 管理。用户要按 GPU、NVMe、网卡、采集卡和加速器总拓扑选平台。插槽物理有空位不代表 CPU 提供足够直连通道，主板可能共享或关闭接口。

Threadripper 的 350W 级 TDP要求工作站级散热和供电。短时跑分不代表长时 Blender、编译或科学计算；VRM 温度、机箱进风和内存散热都会影响稳定。高核心数任务还可能受内存带宽限制：96 核共享八通道与 64 核共享四通道的每核带宽不同，核心数增加后并非线性扩展。

PRO 的价值来自可管理性、内存容量、ECC 和 OEM 验证，而不是所有单机应用都更快。企业为远程管理、固件生命周期、ISV 认证和故障响应付费；个人创作者若只需少数 GPU 和中等内存，非 PRO TRX50 可能更合理。反之，多 GPU AI、虚拟制作和大量 NVMe 会迅速消耗 Ryzen 平台通道，Threadripper 才避免交换芯片瓶颈。

### 9. Radeon RX：按显存、光栅、光追和软件读卡

Radeon RX 7000 采用 RDNA 3，高端 Navi 31 用 GCD+MCD。RX 7900 XTX 的 24GB GDDR6、总线、Infinity Cache 和板卡功耗使其适合 4K 光栅、内容与部分本地计算。[A015] 24GB 是物理容量优势，但应用能否使用取决于 API、驱动与内核；CUDA 专用软件不能因显存足够就直接运行。

RX 9000 转向 RDNA 4，首发 9070 XT/9070，后续覆盖 9060 级。官方速查表给出 CU、显存、TBP、显示与编码能力，应按具体板卡使用，不把 9000 家族都视为同一裸片。[A014] AIB 非公版会改变频率、供电、尺寸和噪声；厂商 OC 的收益常小于散热和价格差。

光栅、光追和机器学习超分是三条不同路径。传统光栅依赖着色、纹理和像素；光追增加 BVH 遍历和求交；FSR 4 使用机器学习提高重建质量。一个游戏是否支持具体 FSR 版本、是否有帧生成、基础帧率和延迟控制，都会改变体验。[A048] 不能用支持列表外的推测代表已交付功能。

显存选型应看峰值占用、资产缓存和未来分辨率，而不是平均帧率。8GB 可能满足电竞与中等纹理，却在高分辨率材质、光追和创作场景出现换页；16GB 提供余量但不会自动提高核心算力。PCIe 代际与链路宽度影响显存溢出后的系统内存访问，真正解决仍是降低纹理/光追或选择更大显存。

显示和媒体同样重要。DisplayPort/HDMI 版本、DSC、色深、AV1 编解码与多屏组合由 GPU 和板卡输出共同决定；接口版本支持不保证任意分辨率、刷新率、色深组合无需压缩。直播者应测试编码质量、软件插件与音画同步，不只查“支持 AV1”。

Adrenalin 把驱动、录制、调优、延迟与游戏配置集中，功能版本会随驱动变化。[A049] 更新可能修复游戏也可能引入回归，生产机器宜保留可回滚包、关闭自动更新并记录稳定版本。Linux 的内核 amdgpu 与用户态 Mesa/ROCm 版本组合不同于 Windows，不能用一个系统的结论覆盖另一个。

### 10. Radeon PRO：硬件接近，合同不同

Radeon PRO 面向 CAD、DCC、媒体与企业部署，卖点包括大显存、专业驱动、应用认证、远程工作和生命周期。[A016] 某些裸片与 Radeon RX 同源，但固件、ECC/错误报告、显示接口、功耗、驱动分支和支持合同不同。消费卡跑得动应用不等于获得厂商认证。

专业用户应看失败成本。一次渲染慢 5% 可能可接受，驱动崩溃导致项目损坏则不可接受；认证驱动价值在于特定应用版本的回归测试。反过来，个人开发者若软件使用标准 API、预算敏感，RX 的性价比可能更高。二者不是只按 TFLOPS 选。

ROCm 在快照日支持部分 Radeon PRO 和 RX，但 Windows “Runtime 支持”与“HIP SDK 支持”不同，且有库仅 Linux 提供。专业卡也必须逐 SKU 查矩阵，不能因名称含 PRO 就假定完整 ROCm。[A039]

### 11. 掌机：APU 只是系统的一部分

Ryzen Z 系列把 APU 配置和支持面向 Windows 掌机，Z1 Extreme、Z2 家族覆盖不同 CPU/GPU 规模。[A003] 但掌机体验来自 7–9 英寸屏幕分辨率、VRR、内存速率、电池、散热、控制器、休眠和前端。15W 下快不代表 30W 下每瓦更好，插电模式也不代表便携体验。

Valve Steam Deck 的定制 AMD APU 使用 Zen 2 CPU 与 RDNA 2 GPU，整机规格固定，SteamOS、着色器缓存和统一目标让开发者优化。[A075] 它说明较旧核心也能通过平台协同获得稳定体验。屏幕 800p 降低 GPU 压力，固定硬件减少组合爆炸，休眠和输入整合提高实际可用性。

ROG Ally X 使用 Ryzen Z1 Extreme，并把 24GB LPDDR5X、电池、VRR 屏幕和散热作为整机差异。[A076] 相同 Z1 Extreme 在不同掌机的内存、TDP、BIOS 和风扇下结果不同。Windows 兼容库广，但桌面 UI、休眠和后台进程可能消耗电池；OEM 控制中心质量与芯片性能同样重要。

掌机本地 AI 的实用性受内存和功耗制约。可运行小模型不等于适合持续生成；GPU 占用会与游戏竞争；NPU 只存在于特定 APU。最现实的 AI 用途往往是语音、字幕、降噪或轻量助手，前提是应用支持离线执行。

### 12. mini PC：Ryzen AI Max 的天然试验场

mini PC 没有电池限制，可给 Ryzen AI Max 更高持续功耗和更大的散热，同时保留共享内存和小体积。它适合本地 LLM、开发、媒体与紧凑工作站。价值判断要与带 16–24GB 独显的台式机比较整机价格、噪声、内存容量、扩展和软件，而非只比处理器。

mini PC 常见风险包括焊接内存、专用电源、有限 BIOS 更新、较少 PCIe 和高密度散热。厂商可能宣称 USB4/eGPU，却因链路和驱动损失性能；多个高速端口可能共享控制器；双 10GbE、NVMe 与 APU 同时满载会提高内部温度。购买前需要框图、拆机散热和长期固件承诺。

### 13. 半定制：游戏机不是零售 APU

AMD 半定制业务与 Sony、Microsoft 等客户共同定义 SoC，把 Zen CPU、RDNA 图形、媒体、安全和定制 I/O 集成。终端厂商决定内存、存储和软件 API，AMD 不能单方面把零售驱动或新架构功能添加到已出货游戏机。10-K 将半定制收入归入 Client and Gaming，说明它既是技术复用也是独立商业周期。[A001]

Xbox Series X 官方规格和 Velocity Architecture 展示 8 核 Zen 2、RDNA 2、16GB GDDR6 与定制存储路径的协同。[A021][A074] GDDR6 被 CPU/GPU 共享，却有不同带宽区域和系统保留；开发者按固定预算优化。把它称作“16GB 显存”或“16GB 系统内存”都不完整。

PlayStation 5 Pro 的官方发布强调更大 GPU、改进光追与机器学习重建 PSSR，代表半定制能在相同平台周期内用特定图形目标迭代。[A073] 但其 GPU 不能简单映射到某张 Radeon SKU：CU 配置、时钟、缓存、内存和 API 都为终端定制。

半定制的优势是量大、生命周期长和软硬协同，风险是客户集中、周期波动和毛利结构。游戏机换代前库存与需求会起伏，新机出货又带来爬坡。消费 GPU 与主机共享 RDNA 技术可以促进游戏引擎优化，但主机优化不会自动使 Windows 驱动或每张显卡等比例受益。

### 14. AMD Advantage 与 OEM 责任

AMD Advantage 是 AMD CPU、Radeon GPU 与 OEM 设计协同计划，定义一定显示、散热、电池或软件体验目标。[A017] 它有助于减少组合变量，却不使所有认证机器相同。用户仍应查屏幕色域、亮度、内存是否可升级、MUX、USB4、SSD、噪声和保修。

Framework Laptop 13 的 Ryzen AI 300 版本展示可维修主板与模块化端口的另一种平台取舍。[A077] 可维修不代表 CPU/RAM 都可插拔，具体代际可能使用焊接内存；其价值在于整机结构、替换件和升级路径。OEM 产品页是验证这些终端细节的一手来源，AMD 规格页只能证明处理器能力。

### 15. 消费端选型决策树

若目标是纯游戏桌面，先确定显卡与分辨率，再在普通 Ryzen 与 X3D 间按 CPU 限制程度选择；若要重生产力，优先核心数和内存，超出双通道/通道数就转 Threadripper。若要轻薄续航，比较具体整机的电池、屏幕和低负载功耗；若要强 iGPU 或大共享内存，检查 Ryzen AI Max 的容量、功耗和框架支持；若要专业认证，考虑 Radeon PRO/Threadripper PRO；若要便携游戏，按 15W 帧时间、屏幕和电池选掌机。

任何选型都要最后做软件清单：操作系统、必须应用、插件、驱动版本、虚拟化、AI 框架、编解码和外设。硬件纸面领先但缺关键软件，就是不可用；软件支持成熟但容量不足，也无法靠优化创造物理内存。AMD 消费体系的最大灵活性来自选择多，最大风险也来自选择多造成的支持矩阵复杂度。

---

## AMD 产品名称、规格字段与对比口径速查

### 1. 名称只能用于导航

AMD 名称先帮助定位市场，不能代替规格。Ryzen表示客户端 CPU/APU，Threadripper表示 HEDT/工作站，EPYC表示服务器，Radeon RX表示消费显卡，Radeon PRO表示专业显卡，Instinct表示数据中心加速器，Ryzen Embedded/EPYC Embedded强调生命周期，Versal/FPGA属于自适应计算，Pensando属于网络基础设施。最终均回到精确料号与产品页。[A003]

系列数字可能表达产品世代、市场层级或组合，不能假设每位都固定。移动系列历史上可能在同一大系列混入不同 CPU架构；Ryzen AI新命名又按 AI PC重组。零售商标题常省略后缀，采购单必须写全名、OPN和 OEM型号。

### 2. Ryzen 3/5/7/9 与代际

Ryzen 3/5/7/9是市场层级，核心数随代际和桌面/移动变化。Ryzen 9通常更多核，Ryzen 7不保证永远8核，移动异构核心还需查 Zen 5/Zen 5c组合。数字9000/8000/7000不保证所有子系列同裸片或相同 PCIe。

桌面 Ryzen 9000的 X后缀常表示较高功耗/频率，非 X可为65W，F表示具体 SKU需独显，G通常强调较强集显，X3D表示 3D V-Cache。实际规则以规格页，不能给后缀赋未公布含义。[A002][A003]

`PRO`表示商用安全、管理和生命周期组合，性能基础可能接近非 PRO，OEM是否启用企业功能另查。`GE`等低功耗嵌入/桌面后缀也要查具体 TDP。盒装/Tray/MPK有不同产品 ID与保修渠道。

### 3. Ryzen AI 名称

Ryzen AI是 CPU、Radeon GPU和专用 XDNA NPU组合的品牌定义，不是只有 NPU。[A004] `AI 300`、`AI 400`、`AI Max`是家族；`Max+ 395`、`AI 9 HX 370`等尾部数字/层级定位具体 SKU。Max强调更大 iGPU和宽共享内存，普通 AI不应由名称推断同等位宽。

`HX`在 Ryzen AI名称中表示高性能定位，但 OEM仍可在可配置功耗内设计；不能把桌面 HX传统经验机械套入。`PRO`增加商用合同。处理器页面的“最多50 TOPS”通常指 NPU，整颗平台的 CPU/GPU计算不能混成相同口径。

AI 400在快照日正式扩展桌面与工作站，但仍须通过 OEM设备获得。[A081] 网页出现“laptops, desktops”不证明每个 SKU以插槽零售，更不证明任何 AM5主板可安装。

### 4. Threadripper 名称

Threadripper 9000面向高端桌面，`PRO 9000 WX`面向工作站。最高64核与96核是系列上限，具体9955WX/9975WX/9995WX核心数不同。所有9000均350W级定位也要查具体表。[A010]

平台名 TRX50/WRX90不是 CPU。TRX50四通道、较少 PCIe与 HEDT/部分 PRO兼容；WRX90八通道、更多 PCIe与 PRO管理。CPU可插不等于获得另一平台的通道。

### 5. Radeon RX 名称

RX 9000表示 RDNA 4消费家族，RX 9070 XT高于9070，9060级面向更主流。`XT`一般表示同级更高配置，`GRE`是特定市场/配置标签，不能按字母推核心。显存容量可能同一 GPU有多个版本。[A014]

板卡还带 AIB厂商后缀如 OC、Gaming、Nitro等，不是 AMD SKU。它们改变散热、频率、尺寸和保修。AMD“Game Clock”和“Boost Clock”定义不同：前者是典型游戏期望，后者是满足条件的最高，均非保证持续。

显卡规格中的 `Compute Units`跨 RDNA代际不能等性能；AI Accelerator、Ray Accelerator数量也需配架构。Stream Processor计数与 NVIDIA CUDA core不可一对一。TBP是整卡功耗口径，不是 GPU裸片 TDP。

### 6. Radeon PRO 名称

W7900/W7800等 W表示工作站，数字分层；`Dual Slot`描述板卡形态，可能影响功耗/频率。Radeon AI PRO R9700强调 AI/专业定位和 RDNA 4，仍要查显存、ECC、认证与 ROCm矩阵。[A016]

专业卡与消费卡即使同 gfx target，驱动、认证、显示和支持合同不同。ROCm表中的 Runtime/HIP SDK勾选是软件状态，不能由 PRO 名称推断。[A039]

### 7. Instinct 名称

MI300A的 A表示 CPU+GPU APU形态，MI300X是 GPU加速器；MI325X扩大 HBM，MI350X/MI355X属于 CDNA 4不同功耗/平台定位，MI350P为 PCIe卡。它们不能按数字大小当单一速度序列。[A034][A035]

`OAM`、`PCIe card`、平台/UBB是封装与系统形态。OAM峰值和功耗不能套给 PCIe；八 GPU平台互连也不是单卡规格。采购写 GPU型号、模组、HBM、TDP、主机和互连。

峰值表要附 FP64/FP32/TF32/FP16/BF16/FP8/FP6/FP4、matrix/vector、dense/sparse。把最小精度稀疏 PFLOPS与双精度 TFLOPS并列排名是口径错误。

### 8. EPYC 名称

EPYC 9005是 SP5 5th Gen家族，包含 Zen 5高性能与 Zen 5c密度 SKU。末尾字母可能区分单路、高频、缓存或电信等优化，规则随代际，必须查规格数据库。[A032][A033]

核心数不是全部：基础/boost、L3、TDP、内存速度、插槽数和 CXL同样重要。单路专用 SKU不能双路，双路能力也不表示必须双路。云虚拟机暴露 vCPU不等于物理 EPYC型号。

### 9. Embedded 与自适应计算

Embedded系列名后同数字可能与消费产品共享架构，却增加供货、宽温、RAS或封装。生命周期承诺以正式产品文档和合同，不能从网页长期存在推断。[A019][A059]

Versal AI Edge/AI Core/Premium/Prime/HBM各自偏 AI、通用、网络收发或内存。Adaptive SoC不是 CPU+GPU APU同义，它含可编程逻辑/AI Engine和 NoC。FPGA系列还按逻辑容量、DSP、收发器和封装选。[A057][A058]

Pensando Pollara是 AI NIC，Salina是 DPU。端口 Gb/s不代表包处理、RDMA或集群吞吐，软件/交换生态必需。[A063]

### 10. CPU 规格字段

`Cores/Threads`是可见核心/SMT线程；`Base Clock`不是典型全核固定频率；`Max Boost`是单核突发条件；`Default TDP`是平台热设计；`cTDP`给 OEM范围。实际 PPT/功耗需测。

`L2/L3 Cache`是总量还是每核需读表；X3D把堆叠计入 L3。`Processor Technology`节点是制造标签，不用于跨代面积比较。`Unlocked`允许调校但超出规格可能影响保修。

`PCIe version`是控制器能力，通道数和主板布线另查。`System Memory Specification`是官方速率条件，DIMM数量/rank可能降低。ECC support需要 CPU、主板、BIOS和 DIMM闭环。

### 11. GPU 规格字段

`CU`包含 SIMDs和纹理/相关资源，跨架构吞吐不同。`Game/Boost clock`随功耗温度，AIB可改。`Peak fill-rate/TFLOPS`理论且不含利用。`Infinity Cache`减少外部访问，不添加显存容量。

`Memory size/type/interface/speed`共同决定理论带宽；压缩与缓存影响有效。显示端口版本与最大组合要看脚注。媒体编码格式还看色深、profile和应用。

`Recommended PSU`是典型系统建议，不替代整机功耗计算；连接器和瞬态也查。卡长、高、槽厚决定安装。散热设计不是 GPU芯片规格。

### 12. NPU 与 AI 规格字段

TOPS=每秒万亿操作，必须有数据类型和计数规则。不同厂商是否把 MAC算一或两操作、是否用稀疏会不同。Copilot+门槛是平台资格，不是模型速度线性预测。

NPU支持精度、片上存储、带宽和编译覆盖决定有效。GPU AI TOPS与 NPU TOPS不能相加成应用值。CPU也可 AI，但向量指令和库口径不同。正确比较同模型、同质量、端到端能量。[A041]

### 13. 内存字段

DDR5双通道、Threadripper四/八通道、EPYC更多通道；LPDDR的256-bit是封装接口；Radeon GDDR6是独显；Instinct HBM3E在封装。它们的“GB”都表示容量，却具有完全不同的可升级、一致性与带宽。[A034]

共享内存 APU没有固定“显存”物理池，BIOS预留和动态借用；报告必须写总量与 GPU可用。独显显存物理固定，系统内存不能无损替代。CXL内存是远端层，不等同 DIMM本地。

### 14. 互连字段

PCIe `Gen x lanes`说明链路；物理插槽可能电气更少。USB4端口可能隧穿 PCIe/DP但 OEM实现。Infinity Fabric是内部架构族，不给所有产品同带宽。CXL需协议/设备/固件/OS。[A030][A052][A053]

Resizable BAR/SAM是地址窗口，不是带宽翻倍。[A054] GPU P2P、RDMA和多卡互连按平台。数据中心 GPU的 Infinity Fabric Link与桌面 CCD Fabric不应合并。

### 15. 功耗字段

CPU TDP、GPU TBP、APU cTDP、墙上功率和任务能量分开。移动 OEM可调整长期/短期，桌面 BIOS可启 PBO，显卡 AIB可提高 power limit。比较时恢复默认或明确。

每瓦性能=完成同任务的性能/功耗，任务能量=平均功耗×时间；两者回答不同。空闲/待机对移动和服务器 TCO重要。散热器标称瓦数无统一标准。

### 16. 上市和可用字段

Announcement date、launch date、shipping、OEM availability、channel stock和 software enablement是不同日期。路线图“预计”是未来条件。AMD Newsroom提供公告索引，但网页更新需存档。[A080]

产品规格出现日期可证明料号信息，不证明当地可买；OEM设备发布不证明所有配置；驱动列出设备不证明每个应用。报告的快照日期必须与动态事实并列。

### 17. 最小公平比较表

CPU：核心/线程、架构、缓存、内存、平台、默认功耗、持续频率、任务。GPU：架构、显存、TBP、驱动、游戏/API、画质/分辨率、帧时间。NPU：模型、精度、覆盖、端到端时延/能量。工作站：内存通道、PCIe、认证、服务。AI GPU：模组、HBM、精度/稀疏、互连、软件和集群。

所有表再加价格日期、地区和整机。缺字段留空，不用家族平均补。这样名称只作为索引，架构、die、package、family、SKU和 OEM终于不会混在同一列。

---

## 从芯片到整机：AMD 消费系统设计与采购审计

### 1. 为什么必须审计整机

处理器数据表描述硅的边界，用户购买的是由主板、供电、内存、散热、屏幕、存储、网络、固件和操作系统组成的系统。两个都写 Ryzen AI 9 HX 的笔记本，可以因 LPDDR 速率、长期功耗、风扇曲线和屏幕相差数十个百分点；两个 RX 9070 XT 可因散热器、VBIOS和机箱进风表现不同。系统审计的目的，是把“这颗芯片能做什么”与“这台机器长期交付什么”分开。

审计不等于拆机评分。它从任务开始：在哪运行、持续多久、允许多大噪声、需要多少本地数据、哪些软件不能替代、故障损失多高。然后把需求映射到 CPU、GPU、NPU、内存、I/O 和服务。规格更高但在关键软件中回退，属于不满足；性能略低但稳定、可维护，可能有更低总成本。

### 2. 桌面主板：插槽之外的真实平台

AM5 主板的 CPU support list 是第一入口。物理可插不代表 BIOS 已有微码；刷新 BIOS 又可能需要旧 CPU 或 Flashback。采购多台时应锁定主板 revision，因为同名板不同修订可能换网卡、VRM或 BIOS。升级前备份 BitLocker恢复密钥，固件更新可能改变 TPM测量。

供电不能只数相数。控制器、功率级额定、散热片接触、PCB和气流决定持续能力。65W Ryzen 对多数板轻松，170W Ryzen 9加 PBO会提高电流，Threadripper则需要专用平台。VRM过热可能降频但 CPU温度看起来正常，应读取主板传感器或用热成像。

主板 I/O框图必须逐条阅读。CPU直连显卡与 M.2延迟低，芯片组提供更多 USB/SATA/PCIe却共享上行。第二个 M.2可能使显卡从 x16变 x8，某插槽占用会关闭 SATA，USB4控制器也会占 lane。厂商规格页脚比正面“PCIe 5.0”徽标更重要。[A052]

网络控制器影响稳定与 CPU占用。2.5GbE、5GbE、10GbE需要匹配交换机和线材，Wi-Fi天线位置影响吞吐。家庭 NAS传输慢时先确认协商速率、丢包和存储，不把全部责任归 CPU。开发工作站若需 RDMA或多 VLAN，应选择经过 OS验证的 NIC，而非板载消费网卡。

音频、USB和传感器看似次要，却决定日常体验。高 DPC 延迟会影响实时音频，USB控制器共享会使采集卡掉帧，睡眠唤醒问题常来自外围固件。专业用途要按完整外设同时运行测试，不只跑裸机 CPU benchmark。

### 3. 桌面内存审计

先定容量，再定速度和时序。32GB适合常规游戏办公，创作、虚拟机和大型工程常需64GB以上，本地 AI还要给模型和系统余量。内存不足触发换页后，任何 EXPO收益都无关。长期项目应以两年峰值容量采购，尤其当以后补 DIMM会降低频率。

检查主板 QVL只是起点。QVL说明某套条在某 BIOS被测试，不保证所有 CPU内存控制器相同。两条套装不要与另一套相同型号混装，因为颗粒批次可能不同。开启 EXPO后跑冷启动、睡眠、MemTest、长时编译和 GPU共享内存，不以进入系统为稳定标准。

内存错误可能表现为应用随机退出、压缩校验失败、文件损坏或 GPU driver timeout。诊断先关闭 PBO/Curve Optimizer和 EXPO，使用默认 JEDEC；若恢复稳定，再逐项添加。同时修改三个参数会失去因果。

### 4. 散热与供电：峰值和稳态之间

CPU散热器的标称“支持多少瓦”没有统一测试。接触面、热管方向、风扇、机箱进气、室温和硅热密度共同决定。Zen小 CCD的热点面积小，即便平均功耗不高也可能快速触温度目标；温度接近设计上限不自动表示危险，但可能限制 boost。

一体水冷有较大热容量，首几分钟温度上升慢，最终受冷排和室温。泵故障与寿命是新增风险。塔式风冷结构简单、可换风扇，重型散热需注意运输和内存/机箱空间。工作站选择应看持续负载噪声和可维修，不看空闲灯效。

PSU要按整机瞬态、效率和接口选。高端 GPU瞬时功率、CPU全核和多 NVMe同时运行可能超过平均。余量过小导致重启，过大又不保证低负载效率。使用原生线缆，模组线不能跨品牌/型号混用；接头未插到底会发热。

机箱气流是风量与路径。前进后出、减少回流，GPU与 CPU都要新鲜空气。正压可降低缝隙吸尘但滤网增加阻力。测试关闭侧板：若温度显著下降，问题是机箱而不是换更大 CPU散热器。

### 5. Radeon 板卡与显示链

同一 GPU SKU 的 AIB 卡可有不同 TBP、VRM、散热器、风扇停转和尺寸。出厂超频通常小，散热噪声和保修更有价值。检查机箱长度、厚度、支撑和电源接口；三槽卡会遮挡其他 PCIe，对多卡/采集卡系统尤甚。

GPU hotspot高于 edge温度是正常梯度，但异常大差值可能来自接触或导热材料。判断应结合厂商限值、时钟、风扇和稳定性，不只凭一个温度。二手卡先跑显存错误、长时负载和端口测试，避免只看外观。

显示链包含 GPU端口、线缆、显示器输入、EDID、DSC和 OS。DisplayPort 2.1a标签不保证线缆或显示器全带宽；高刷新、10-bit和 HDR组合需计算并实际验证。黑屏可由线缆、固件、VRR和睡眠，不一定是 GPU算力。

多屏会改变显存时钟和空闲功耗，尤其分辨率/刷新率不一致。用真正使用的多屏组合测待机和视频。创作要校色、色彩管理和应用输出，显卡支持高色深不等于整条链准确。

### 6. 笔记本：同 SKU 的巨大方差

移动采购先查处理器官方可配置功耗，再查 OEM实际模式。安静、平衡、性能模式可改变长期功耗，且 AC/电池不同。厂商只公布一次 Cinebench时，无法推断一小时编译。实测要等待热稳定，记录室温和风扇。

散热通常由 CPU与独显共享热管/均热板。CPU单独满载很好，不表示游戏中同样；GPU加入后 CPU可降频。反之，轻薄无独显的 Ryzen AI Max让 CPU/iGPU共享封装功耗。混合负载比单项峰值更能预测创作和游戏。

屏幕是续航和体验最大变量之一。OLED、mini-LED、高刷新和高亮会增加功耗；低功耗 IPS可能续航更好。厂商“最长 X小时”依测试亮度、视频/办公和无线，不能跨机型。实际比较统一亮度、刷新、网络和应用。

内存确认容量、通道、速率和可升级。LPDDR通常焊接，SO-DIMM可升级但部分机型只有一个槽或混合板载。Ryzen AI/iGPU需要带宽，错误的单通道配置会明显限制。SSD也可能因功耗和温度降速，双面高性能盘未必适合薄机。

### 7. MUX、混合显卡与外接显示

有独显笔记本通常让 iGPU驱动内屏以节能，独显渲染后把帧复制到 iGPU；MUX可让独显直连内屏，减少复制和部分延迟，但待机更耗电。Advanced Optimus/动态切换试图兼顾，兼容和黑屏需验证。

外接端口可能直连独显或 iGPU，不同端口带宽/VRR/HDR不同。用 HDMI/USB-C连接外屏前查框图。创作应用若在独显计算、画面经 iGPU显示，会有额外拷贝但通常可接受；高帧电竞可能受影响。

混合驱动升级顺序重要，OEM电源管理和热键服务可能依特定包。完全删除 OEM组件再装通用驱动可丢失模式。建立恢复点与驱动备份，确认睡眠、亮度和外屏后再部署。

### 8. 电池、待机与移动能效

电池容量以 Wh表示，续航约等于容量除平均功耗；处理器 TDP只描述负载热设计，不是整机平均。屏幕、无线、SSD、内存、自放电和后台共同决定。空闲多 1–2W 对长续航影响巨大，峰值性能差异反而次要。

Modern Standby可能保持网络和任务，背包中异常唤醒会发热耗电。审计睡眠报告、唤醒源和一夜电量，测试插/拔扩展坞。休眠更省电但恢复慢，可作为旅行策略。BIOS和驱动更新可能改善或破坏待机。

电池性能模式通常限制 SoC功耗和独显。应测试目标任务每瓦和完成时间：限制功耗可能让编译慢但总能耗更低，也可能因运行更久反而耗更多。掌机用固定 10/15/25W点测帧时间，找到效率甜点。

### 9. Ryzen AI Max 系统专项审计

首先确认具体 SKU：395、390、385等 CPU核心和 iGPU CU不同，不能把 Max家族统一成16核/40CU。其次确认内存位宽、容量、速率与 BIOS可分配 UMA。第三确认持续功耗和散热。第四确认 ROCm/Ryzen AI Software是否点名 gfx1151/XDNA设备。[A004][A051]

本地 LLM 应按可用而非总内存规划。128GB系统可能被 OS、应用和 GPU共享，模型权重、KV cache与运行时各占空间。测试大模型时记录物理驻留、换页、首 token和稳态；若系统开始 swap，所谓“大显存”优势会急剧下降。

mini PC检查 NVMe和网卡在满载时的内部热，笔记本检查电池与共享散热。Max SoC同时满载 CPU/GPU会发生功耗分配，独立测试峰值不可相加。用真实混合任务，例如编译同时 GPU推理或剪辑导出。

外部扩展要看 USB4和 PCIe通道。eGPU可以增加独立显存，但跨外链传输；多 NVMe/高速 NIC可能共享。若计划作为服务器，确认无人值守启动、断电恢复、ECC状态、Linux驱动和远程管理。

### 10. Ryzen AI/NPU 系统专项审计

任务管理器显示 NPU只证明设备枚举。使用 Ryzen AI Software的编译报告确认算子，运行时日志确认执行，功率/计数器确认持续。若应用宣传 AI功能，询问模型是否本地、使用哪个设备、离线是否工作、地区和版本。[A041]

OEM BIOS可能默认关闭或保留 NPU内存，Windows更新和驱动才能启用 Copilot+功能。处理器发布、设备出货和功能推送时间不同。企业镜像还可能禁用商店/云功能，采购前在自己的镜像测试。

NPU长时负载会占封装功耗但通常低于 GPU。测视频会议一小时的电池、CPU占用和画质，比单次 TOPS更有意义。若 CPU fallback增加，能耗可能比不用特效更差。自动设备选择应有可观察性。

### 11. 掌机系统审计

屏幕原生分辨率决定 GPU负载，VRR范围影响低帧平滑。800p与1080p/1600p不能直接比 FPS。掌机常用 FSR或动态分辨率，记录内部渲染和输出。低分位帧时间比平均值更能反映着色器编译和内存压力。

统一内存容量同时服务游戏和 Windows/SteamOS。16GB可能在现代大作紧张，24/32GB有余量，但更大不自动更快。检查内存速率和 UMA设置。SSD更换要注意物理尺寸、功耗、热和恢复镜像。

控制器死区、陀螺、触控板、振动和前端决定可玩性。Windows掌机的桌面弹窗、反作弊、休眠可能影响；Steam Deck固定平台和缓存有优势。[A075][A076] 比较应包含从按电源到继续游戏的完整流程。

电池测试固定亮度、音量、Wi-Fi和 TDP。报告平均功耗与 Wh，而非只报时长；不同电池容量的机器时长不可直接代表 SoC能效。风扇噪声与握持温度也是性能边界。

### 12. 工作站主板与多 GPU

Threadripper TRX50/WRX90采购先列出每个 PCIe设备：GPU数量和宽度、NVMe、NIC、采集卡、音频/FPGA。将它们映射到 CPU直连通道、交换芯片和插槽间距。多张三槽 GPU可能物理放不下，即使有通道。[A010]

四/八通道内存应对称填充，NUMA与每核带宽随核心数变化。ECC RDIMM选主板 QVL，混插容量和 rank谨慎。96核编译可能受内存和文件系统限制，先测扩展曲线再购买更多核心。

多 GPU AI需验证 P2P和框架。PCIe x16不等于有 GPU专用互连，跨卡 all-reduce可能受根复合体。NIC应靠近主要 GPU，进程绑定对应 NUMA。消费 Radeon多卡游戏支持与计算多卡不是同一回事。

工作站电源可能超过普通插座持续能力。计算墙上功耗、断路器、UPS和热排放。长时 350W CPU加多 GPU要有机箱风道和房间冷却。UPS容量按 VA/W和运行时间，不只看标称。

### 13. 存储设计

系统盘、项目盘、scratch和备份角色不同。高顺序 NVMe适合大文件，编译/数据库看随机延迟和队列，游戏加载还受 CPU解压。多个 M.2可能共享芯片组上行，RAID峰值受限。持续写会因 SLC缓存耗尽和温度下降。

本地 AI模型文件大，下载、校验和多版本占空间。保留模型 hash与许可证，使用文件系统 reflink/去重需验证。模型加载频繁时页缓存有用，内存不足时却与推理争用。把模型和系统 swap放同盘会放大抖动。

备份遵守 3-2-1，RAID不是备份。工作站 checkpoint要测试恢复，而非只测试写入。SSD SMART、温度和固件纳入监控。消费盘在长写和断电保护上不同于企业盘。

### 14. 网络与远程工作

本地 AI服务需要低延迟、有认证的 API；10GbE理论 1.25GB/s，实际扣协议和存储。Wi-Fi吞吐随距离和干扰。大模型首次分发可由网络限制，运行后主要本地。远程桌面还受编码、色彩和输入延迟。

工作站多用户要隔离 GPU资源和数据。消费 Radeon通常缺少数据中心级分区，多个进程会争显存；调度器可限并发但不是硬隔离。敏感项目不应仅依应用进程边界。

Wake-on-LAN、断电恢复和远程 KVM在无人值守 mini PC很实用，消费主板支持不一。BIOS更新失败若无远程控制会停机。企业部署应选择带 BMC/专业管理的平台。

### 15. Windows 安装与 OEM 镜像

新机先导出 OEM驱动和恢复镜像，记录 BIOS。纯净安装可减少预装后台，却可能缺电源、音频、热键、MUX和 NPU组件。比较前确保 Windows版本、芯片组驱动、GPU/NPU驱动相同。

VBS、内存完整性、BitLocker和虚拟化影响性能与安全。企业不应为跑分关闭安全；评测可以分别测但注明。游戏反作弊与虚拟化有兼容问题，按实际策略。

Windows电源模式、OEM控制中心和 AMD驱动可能同时控制功耗。找到主控制源，避免“性能模式”互相覆盖。更新后重新验证。系统还原不保证回滚 BIOS，固件要单独管理。

### 16. Linux 安装与双系统

Linux硬件支持看内核、固件、Mesa和 ROCm，不看发行版名称 alone。新 Ryzen/Radeon可能需较新内核，企业发行版用 backport。安装前从 live环境测试 Wi-Fi、睡眠、音频和显示。

双系统共享时钟、Secure Boot、BitLocker和磁盘会产生问题。固件更新可重排启动项，Windows Fast Startup会让共享分区不安全。关闭快速启动、备份恢复密钥，分开数据和系统。

ROCm安装遵循官方矩阵，容器仍需宿主设备和驱动。[A038] 普通桌面图形不要无故替换 Mesa。XDNA Linux支持需要官方驱动/用户态，社区编译成功与生产支持分开。

### 17. 噪声与人因

分贝是对数，测量距离和背景必须注明。风扇频繁升降比稳定稍大声更扰人，可用滞后曲线，但 OEM BIOS未必开放。coil whine与负载/电源有关，不代表即将故障，却影响体验。

工作场所还要看键盘、触控板、摄像头、麦克风和端口位置。AI视频会议硬件强但摄像头/麦克风差，功能价值有限。迷你主机外置电源和线缆会占空间，桌面维护便利可能更重要。

### 18. 可维修性与生命周期

桌面标准件最易替换，笔记本和 mini PC可能焊接 RAM/SoC，掌机屏幕/摇杆可换性不同。可维修性评估包括拆装步骤、螺丝、胶、零件供应、文档和保修。Framework展示模块化路径，但具体 AMD代际仍要查部件。[A077]

BIOS/驱动支持期限与硬件寿命同样重要。企业采购向 OEM索取生命周期、关键漏洞响应和备件。消费新特性若两年后驱动停止更新，残值下降。开放 Linux驱动有助长期，但固件和用户态仍可能结束支持。

升级能力应按最可能瓶颈：桌面显卡/存储容易，CPU受插槽；笔记本 SSD可能可换、RAM不可；Ryzen AI Max大内存买错无法补。为永远不用的扩展付费也浪费，按任务增长情景。

### 19. 到货验收

记录包装、序列、SKU、BIOS、内存颗粒、SSD和屏幕面板。先恢复默认，更新到计划版本，跑 CPU、内存、GPU、NPU、存储、网络和睡眠。检查坏点、端口、风扇、扬声器与电池健康。

稳定性测试包含空闲、突发、混合长负载和多次冷启动。监控 WHEA/AER、内存错误、GPU reset和温度。任何超频在默认验收通过后再做。退换期内优先暴露硬件问题。

性能与同 SKU合理范围比较，显著偏低先看功耗、内存通道、驱动和后台。不要立即超频掩盖。将基线保存，未来更新可检测回归。

### 20. 采购评分卡

评分建议：任务正确性25%，持续性能15%，内存/扩展15%，软件兼容15%，能效/噪声10%，可靠与服务10%，价格/TCO10%。不同用户可改权重，但硬约束使用淘汰而不是平均。例如必须 CUDA-only 的应用会淘汰 Radeon，不应由便宜分数补偿。

每个分数附证据：官方规格、支持矩阵、自己的 PoC、保修条款。未知记为未知或零，不用宣传填补。对 OEM倍数保留测试条件；对未上市路线不给性能分。

最终选择保留一个备选和退出方案。AMD平台在某任务可能最佳，也可能因软件不适合。可复核的采购不是证明品牌正确，而是证明具体系统在明确周期内满足任务，并知道何时需要重评。

---

## OEM、内存与软件兼容案例簿

### 1. 案例簿为何独立成章

同一 AMD 芯片进入不同终端后，用户最常遇到的差异并非微架构，而是厂商如何配置内存、功耗、固件和软件。本章用若干可重复的案例展示推理方法。案例不指定未验证机型，不把假设包装成实测；每个案例给出需要收集的证据、可能结果和停止条件。

这种方法能避免两个极端：看到一台散热差的笔记本就否定整代处理器，或看到 AMD 工程样机跑分就认为全部 OEM 都能复制。产品页证明芯片上限，OEM 数据表证明配置，实际测试证明该台机器在指定环境的行为，三层证据不能互相替代。[A003][A004]

### 2. 案例一：相同 Ryzen AI SKU 的 16GB 与 32GB 机型

设两台轻薄本使用同一 Ryzen AI 处理器，一台 16GB LPDDR5X，另一台 32GB。CPU 核、NPU TOPS和 iGPU CU相同，但多任务和图形结果会不同。16GB中 Windows、浏览器和后台先占一部分，iGPU借用共享内存后余量更少；32GB不直接增加计算单元，却降低换页和应用退出概率。

测试应同时运行视频会议效果、二十个网页、办公文档和轻量图像模型，记录物理内存、压缩、page faults、SSD写和响应。若16GB没有换页，差异可能很小；若工作集超过，尾延迟会突然扩大。不能只跑清洁系统的单项 benchmark。

焊接内存使结论具有生命周期。今天任务占12GB不代表两年后仍足够，NPU应用也需要输入缓冲。采购溢价应与整机替换成本比较；若机器可换 SO-DIMM，则先买较小容量的策略可能合理。产品名称本身不告诉可升级性，必须查 OEM维修手册。

### 3. 案例二：同一 APU 的单通道与双通道

桌面 APU或可更换内存笔记本可能因只装一条 DIMM而损失一半并行通道。CPU轻负载不一定明显，iGPU渲染和带宽型任务会更敏感。零售商写“16GB DDR5”不说明是一条还是两条，必须读配置或用工具核对。

实验固定容量时，可比较一条16GB和两条8GB，但颗粒/rank不同会引入变量；更好的做法是同一机器按支持方式改变，并记录频率。测内存读写、三个游戏帧时间和 GPU时钟。如果 GPU利用低、带宽满，通道是瓶颈；如果功耗/核心满，提升较小。

升级也有代价。两条占满插槽，以后扩容需替换；一条保留扩展但牺牲早期 iGPU。购买独显桌面时单通道仍影响 CPU但程度不同。决策要按当前任务和升级概率，不把“双通道永远快一倍”写成结论。

### 4. 案例三：Ryzen AI Max 的 64GB 与 128GB

Ryzen AI Max共享 LPDDR使大容量成为产品差异。64GB可以服务中等量化模型和创作，128GB能容纳更大权重或上下文；二者同 iGPU时，小模型速度可能近似。容量价值只在工作集跨过阈值后出现。[A051]

实验建立模型阶梯：从完全装入64GB且留足系统余量，到接近64GB，再超过。记录模型加载、系统剩余、swap、首 token、稳态和长上下文。若超过后部分权重频繁换页，速度可能断崖下降；128GB避免该问题。但大模型每 token读更多权重，即使装入也会比小模型慢。

还需确认 OEM给 GPU的可分配范围、内存速率和持续功耗。128GB机型若内存频率较低或散热更紧，不一定所有任务更快。购买本地 AI应以“必须模型集合”估容量，不因最大配置存在就认为小配置无价值。

### 5. 案例四：Ryzen AI 400 桌面与移动

官方发布把 Ryzen AI 400扩展到桌面/工作站和移动形态，硅组合仍包含 Zen 5、RDNA 3.5与 XDNA 2。[A081] 桌面 OEM可能提供更持续的散热、更多端口和可更换存储，移动机提供电池；两者不能按相同处理器表推体验。

对比需要固定处理器 SKU、内存和应用，分别测短时/30分钟 CPU、iGPU、NPU与混合。桌面长时可能更高，NPU本身若已在低功耗区则差距小。移动拔电会改变。终端功能还要确认 Windows版本和应用，不因桌面插电就自动启用更多 NPU算子。

“桌面”也可能是 BGA mini PC或一体机，不表示 AM5可升级。采购确认封装、主板、RAM和保修，避免把形态词推成插槽合同。

### 6. 案例五：OEM 功耗模式如何改变同名 CPU

一台笔记本可能提供静音、平衡、性能；每个模式设置短时和长期功率、温度和风扇。相同 Ryzen 9在性能模式首分钟领先，热浸后可能接近；静音模式慢但每瓦和声学更好。评测只用最高模式无法代表默认。

实验从冷机开始记录十分钟全核，再等温度恢复；重复混合 CPU/GPU。报告功耗曲线、完成时间和噪声。若性能模式增加30%功耗只快5%，移动用户可能选平衡；若编译每天多次，时间价值可能高。

OEM更新 BIOS会调整曲线。购买决策不能保证未来版本保持跑分，但可要求模式切换和稳定支持。企业统一机队应锁 BIOS并验证安全更新。

### 7. 案例六：RX 9000 的 8GB 与 16GB

同级或近级 Radeon若有不同显存，平均帧率在低纹理可能相近，高分辨率、光追和创作会分化。显存不足不是稳定降低10%，而可能触发资源换入、纹理降级或崩溃。官方图形规格数据库用于确认物理容量，游戏测试确认需求。[A012][A014]

测试逐级提高纹理和分辨率，记录分配、预算、PCIe流量与低分位帧时间。还要重启场景，避免缓存让某次有利。创作使用实际工程和插件。如果任务从未超过8GB，16GB价值是未来余量而非当前速度；若已接近上限，应优先容量。

PCIe 5.0不会把系统内存变成等价显存。溢出路径仍有数量级差异，Resizable BAR只改变映射窗口。降低纹理或选择更大显存才是直接解决。

### 8. 案例七：Adrenalin 新驱动带来的提升与回归

驱动 release notes可能声称新游戏优化，用户更新后旧应用也可能变化。正确 A/B需要同一 OS/游戏、清理着色器缓存策略一致、相同 GPU设置；不能把 Windows后台更新同时发生的差异归驱动。[A049]

测试核心游戏、媒体、睡眠、多屏和 AI计算。新驱动若平均快却出现帧尖峰，不能只报平均；若修复安全漏洞，微小性能下降可能接受。生产创作保留已知稳定包，先在副机器验证。

回滚要处理 Windows自动安装和配置残留。清洁安装是可控步骤，但不应每次更新都破坏所有缓存后只测新驱动冷启动。记录两种缓存状态。

### 9. 案例八：ROCm Runtime 支持不等于完整开发栈

假设 Windows矩阵对某 Radeon显示 Runtime勾选、HIP SDK未勾选。设备可能运行 HIP/OpenCL基本程序，却缺官方数学库或预编译框架组件。看到 GPU被 `rocminfo`识别就宣布“完整 ROCm支持”会误导。[A039]

验证分层：设备枚举、简单 kernel、hipBLAS、PyTorch wheel、自定义扩展、目标模型。每层记录官方或社区。若目标只需要 Vulkan推理，ROCm缺库未必重要；若要训练，库和框架是硬约束。

Linux矩阵可能更完整，但迁移 OS有应用、驱动和运维成本。决策对比整个工作流，不把免费 Linux假定零成本。版本升级重新核对，因为支持状态会变化。

### 10. 案例九：同一模型在 NPU、GPU 与 CPU

小图像模型在 NPU可能最低能耗，GPU批处理吞吐高，CPU冷启动快。结果由模型、batch、量化和前后处理决定。使用同一任务质量对齐，不能让 NPU用 INT8、CPU用 FP32而只比速度。

编译报告列出 NPU覆盖；若部分回退，端到端包含拷贝。测单请求、连续30分钟和前台响应。摄像头持续场景对能量敏感，大批离线转换对吞吐敏感。自动调度可按场景选择，不需要一个设备统治所有。[A041]

模型更新可能加入不支持算子，使 NPU路径回退。CI应以覆盖和性能阈值检测，应用向用户展示当前设备。

### 11. 案例十：游戏机定制 SoC 与 PC Radeon 的错误类比

游戏机官方写 Zen 2/RDNA 2或定制 Radeon，观察者常用 CU×时钟映射某张 PC卡。这忽略缓存、内存、CPU共享、固定 API、功耗和引擎优化。Xbox的统一 GDDR6还有不同带宽区域，PC独显则有专用显存和 DDR主存。[A021][A074]

合理比较是终端任务：同一游戏、相同视觉目标、实际帧率和功耗，仍需说明版本。架构相似能帮助移植，不产生一对一 SKU。PlayStation 5 Pro的 PSSR也是整机图形路径，不能由 PC卡有没有同名功能判断。[A073]

半定制价值在固定平台长期优化。PC价值在可升级和更广软件。二者的优势来自不同合同，不能用一张理论算力表裁决。

### 12. 案例十一：Threadripper 核心数没有线性扩展

32、64、96核在渲染可能良好扩展，在编译受依赖和存储，在 EDA受许可证，在内存任务受通道。WRX90八通道提供供数，TRX50四通道较少；每核带宽随配置。[A010]

实验从1到全核绘曲线，记录内存带宽和频率。若64到96只提升很小，寻找串行、内存或 I/O。选择高频较少核可能降低许可证和交互延迟。多核心还提高散热、电力和 NUMA复杂。

工作站采购用每天节省时间计算，不以核心单价。若任务偶发，可租云或排队；若工程师持续等待，高端平台回报。结果必须用真实项目。

### 13. 案例十二：迷你主机的端口共享

一台 mini PC宣传两个 USB4、双网口和双 M.2，但内部可能共享 PCIe/控制器。eGPU、外置 NVMe、10GbE同时运行时，总吞吐不等于各峰值相加。芯片有接口能力，OEM布线决定实际。

测试单设备峰值，再两两和全部并发；读取 PCIe树，观察 CPU/芯片组根。若外置 GPU与 SSD共享，模型加载时 GPU传输受影响，稳态推理可能恢复。温度也会让内部 SSD/NIC降速。

采购向厂商索取框图或技术手册；没有则把共享视为未知并实测。端口数量是功能，不是保证每个同时满速。

### 14. 案例十三：Linux 新内核支持与生产稳定

新 Ryzen/Radeon可能需要新内核/固件才能识别，最新内核又可能不在企业 ROCm验证矩阵。开发者面临硬件 enablement和软件认证的时间差。[A038][A050]

可在副环境用新发行版验证设备，生产等待矩阵；或选择较成熟 GPU。不要把自编内核成功当厂商支持。若业务必须新硬件，建立补丁、回滚和支持责任。

桌面 Mesa更新与 ROCm用户态也可能分开，混装专有包造成冲突。用容器隔离框架，但内核驱动仍共享。记录每层版本。

### 15. 案例十四：移动电池和性能的错位

两台相同 APU笔记本，一台80Wh高刷新 OLED，一台60Wh低功耗 IPS。即使后者处理器更省，续航不一定更长；屏幕和容量可能主导。厂商“多日续航”定义也依测试，AMD产品页脚提醒实际受系统配置和条件影响。[A004]

统一亮度和60Hz测办公，再按用户实际刷新/HDR测。记录平均 W而非只小时，这样区分电池容量与整机能效。拔电性能也测，某 OEM可能限制更多。

处理器比较要用相近屏幕或扣除显示基线。购买整机则无需剥离，直接按实际模式选择。研究问题不同，指标也不同。

### 16. 案例十五：从 OEM 宣布到功能可用

一个新 Ryzen AI设备通常经历处理器宣布、OEM宣布、地区预售、出货、Windows/驱动更新、ISV启用。任何节点都可能延迟。网站有产品卡不证明本地库存，包装有 NPU不证明应用默认使用。

建立状态表：SKU规格已发布；目标 OEM配置有公开料号；渠道可购买；驱动支持；系统功能可用；第三方应用验证。只为当前通过的状态付款，未来项视为期权。

企业采购还需镜像和策略验证。商店版应用可用而企业镜像禁商店时，功能仍不可部署。隐私/地区规则也会阻止云功能。本地 NPU价值应由离线任务单独证明。

### 17. 案例簿的共同结论

所有案例都遵循同一链：先找具体 SKU和物理资源，再找 OEM连接与功耗，再查软件支持，最后用真实任务热浸。若某层未知，就把结论限制在上一层。芯片能力不能跨过 OEM和 ISV自动成为体验，负面 OEM案例也不能倒推硅架构。

兼容性不是二元。“设备识别、运行时、完整 SDK、目标应用、官方支持、生产 SLA”是逐级合同。容量也不是连续性能；超过阈值才出现断崖。功耗模式不是附加开关；它定义同名移动产品的稳态。

这套案例方法应随产品更新而复用。更换 Ryzen/Radeon代际只需更新规格与支持矩阵，问题仍是数据从哪里来、在哪计算、经过何种内存/互连、热和功耗如何限制、失败时谁负责。只有回答这些问题，产品体系研究才落到可执行决策。

---

## AMD 端侧产品部署蓝图：十二类真实场景的配置、数据路径与验收

### 1. 蓝图的使用方法

以下不是固定购物单，而是从任务到系统的设计模板。每个蓝图都给出负载、瓶颈、合适产品层、反例、软件与验收。型号和价格会变，约束链更稳定。实施时用当前规格数据库替换代表 SKU，并核对地区 OEM、内存和驱动。[A003][A012]

同一个人可能跨蓝图，例如游戏兼视频创作、软件开发兼本地 AI。不要把所有峰值叠成一台昂贵机器；先识别能否错峰、是否可用远程/云资源、哪些数据必须本地。最贵资源应服务最高频或最敏感任务。

### 2. 蓝图 A：安静的主流游戏桌面

目标是 1440p、稳定帧时间、低噪声与后续显卡升级。CPU从单 CCD Ryzen 5/7或 X3D选择；GPU按目标游戏显存和光追；32GB双通道 DDR5；一块优质 NVMe；风道明确。AM5平台价值是以后更换 CPU，而不是首日买最多核心。[A002]

数据路径是 CPU准备游戏逻辑和绘制，资产从 SSD进内存/显存，GPU渲染后输出。若 GPU利用接近100%，换16核 CPU改善有限；若低画质高帧 CPU限制，X3D更有价值。用目标游戏的1% low验证。

反例：用户只玩4K重光追且显卡预算受限，优先 GPU；游戏同时直播/编译，更多核心和媒体引擎有用；CUDA创作不能由 Radeon游戏性价比替代。验收包含冷/热着色器、两小时热浸、多屏空闲和睡眠。

### 3. 蓝图 B：高刷新电竞系统

目标是1080p/1440p 240–500Hz和低输入延迟。Ryzen X3D可降低部分 CPU帧时间，GPU要保证基础帧率，内存调校以稳定为先。关闭不必要后台，但保留比赛实际反作弊/语音。显示器 VRR范围、扫描和输入设备同等重要。

平均 FPS不是门槛，关注99/99.9百分位帧时间、输入到光子和长局稳定。帧生成不适合用来声称竞技基础响应，因为插入帧不提高游戏逻辑。低延迟模式逐游戏测试。

高频内存超频若偶发错误或训练慢，比赛可靠性更差。建立默认配置和可恢复 BIOS。网络测本地抖动/丢包，不用测速网站峰值替代。音频 DPC也可能产生尖峰。

### 4. 蓝图 C：4K 单机与客厅 PC

4K最高画质通常 GPU限制，选择 Radeon时看16GB以上显存、光追和目标 FSR支持；CPU可用能效良好的 Ryzen 7。客厅要求低噪、HDMI能力、HDR、CEC替代方案和手柄唤醒。小机箱要验证 GPU尺寸与热。

资产高分辨率增加显存，若溢出会产生帧尖峰。测试实际纹理、光追和 mod，而非内置平均。电视可能限制色深/刷新，线缆认证和端口设置必需。AV接收器会加入链路变量。

休眠/唤醒、无线手柄、前端和流媒体比跑分影响家用。Windows更新或登录弹窗破坏沙发体验，可使用受控启动器但保留安全更新。若追求游戏机式一致，半定制主机是间接替代，PC优势是库和升级。

### 5. 蓝图 D：轻薄办公与 AI 会议

目标是8小时真实办公、低风扇、摄像头效果和传统 x86应用。选择 Ryzen AI 300/400的具体 OEM，不只处理器。优先低功耗屏、足够电池、32GB内存、良好待机和 Wi-Fi。NPU可承担背景/音频，需应用启用。[A004][A081]

数据路径包括摄像头→内存→NPU/GPU效果→编码→网络，同时 CPU运行浏览器。测一小时会议的电量、温度和系统响应；若效果回退 CPU，风扇/续航会变。关闭效果作为基线。

反例：需要 CUDA插件或重3D，带独显工作站更合适；需要长离线训练，轻薄散热不足；16GB焊接内存即便 NPU强也会限制多任务。验收待机一夜、视频会议、外接双屏和企业镜像。

### 6. 蓝图 E：移动创作本

照片、4K视频、音频和3D混合。Ryzen AI Max可提供 CPU核、大 iGPU和共享内存，独显本提供专用显存/应用后端。按 DaVinci/Adobe/Blender的实际支持选择，不以统一内存自动推断。[A051]

内存至少按项目峰值，焊接建议64GB或更多；屏幕色域、校准和 SSD持续写重要。CPU/GPU共同导出时共享散热，测30分钟以上。媒体编解码确认应用调用和格式。

移动现场还看电池下性能、读卡器、USB4、外接存储和备份。一个快内置 SSD不是数据安全；项目同时写第二介质。专业交付若容不得驱动回归，锁定版本并禁自动替换。

### 7. 蓝图 F：Ryzen AI Max 本地 LLM 工作站

目标是运行超过普通移动独显显存的大模型，交互开发、隐私和低体积。选择96/128GB级共享内存配置，确认 gfx1151在当前 ROCm/Radeon-Ryzen矩阵支持；Linux/Windows按框架选。[A039][A051]

容量预算包括 OS、模型权重、量化元数据、KV cache、运行时和其他应用。给系统留20%以上余量是起点，长上下文另算。模型若可完全驻留，顺序生成常受 LPDDR带宽；CPU tokenization和采样也测。

比较台式独显时对齐模型、量化、上下文和墙上功耗。独显24GB可能更快但装不下大模型，双 GPU增加通信和成本；Max容量大但软件/带宽不同。根据模型集合而不是一句“能跑70B”决策。

验收包括多模型、首 token、稳态、长上下文、两用户并发、一小时热浸和内存压力。若后端使用 Vulkan/社区补丁，记录为非官方。隐私用网络监控验证。

### 8. 蓝图 G：软件开发与虚拟机桌面

目标是大型编译、多容器、若干 VM和日常交互。Ryzen 9 16核通常提供高性价比，64–128GB DDR5和多 NVMe；若并行更大或需 PCIe设备，Threadripper。CPU核数只有在编译依赖图和内存跟上时扩展。

源码、构建缓存和 VM磁盘产生大量小 I/O，SSD随机/持续写比峰值顺序重要。把源码、构建和容器层合理分盘，避免内存不足导致 swap与编译争盘。ECC按错误成本考虑。

虚拟机 vCPU不全部过量分配，保持 NUMA；容器共享内核但需要内存上限。测试真实 clean/incremental build、IDE索引、测试套件和多 VM并发。若依赖特定 Intel指令/虚拟化或 macOS构建，AMD性能不能解决平台要求。

### 9. 蓝图 H：独立开发者的 GPU 计算机

目标是学习 HIP、PyTorch和中小模型。候选为官方支持的 RX 9000/7900或 Radeon PRO，主存建议不低于显存并给数据余量；Linux通常库覆盖更完整。[A039] 先选软件支持，再选显存与 GPU。

使用官方容器，验证简单 HIP、PyTorch、目标模型和自定义扩展。若项目 CUDA-only，评估移植工时，不用硬件价格掩盖。24GB 7900 XTX有容量，但新 RDNA 4可能有不同支持/性能，逐版本。

工作站保留显示稳定，长计算防 Windows TDR或用 Linux。模型数据和 checkpoint需要存储。报告正确性和总能量。社区项目可加速启用，但生产/课程应预留回退。

### 10. 蓝图 I：家庭实验室与本地服务

目标是虚拟化、NAS、媒体、家庭自动化和轻 AI。能效/空闲功耗、ECC、SATA/NVMe、2.5/10GbE、无人值守恢复比单核峰值更重要。Ryzen桌面可扩展，mini PC体积小，EPYC/Threadripper二手平台通道多但空闲功耗高。

先画服务资源：媒体转码是否用 iGPU，NAS需要多少直连盘，VM内存，AI是否需要 Radeon。主板消费 SATA经芯片组，HBA占 PCIe；IOMMU组决定直通。UPS和备份不可省。

无人值守验收断电恢复、Wake-on-LAN、远程控制、磁盘故障、容器重启和温度。消费平台无 BMC时加外部 KVM。把关键家庭服务和实验 GPU分开可减少驱动更新影响。

### 11. 蓝图 J：摄影与视频工作室桌面

Ryzen 9适合导出与多任务，Radeon选择取决于软件后端/显存，64–128GB RAM和快速 scratch。若多采集卡、多个 GPU或超大内存，Threadripper TRX50/WRX90。[A010]

显示链使用校色器和应用色彩管理。高端 Radeon端口能力不能替代面板。素材从读卡器到项目盘、scratch、备份形成流，USB控制器共享可能限制多路导入。

测试自己的 codec、特效和插件，记录硬件解码/编码。时间线流畅与导出速度分开。驱动选专业认证或已验证 Adrenalin，项目中途不升级。自动保存和 UPS降低失败成本。

### 12. 蓝图 K：CAD/EDA/仿真工作站

CAD交互偏单核和专业 GPU驱动，大型求解/编译偏多核/内存，EDA还可能按核许可。Threadripper PRO提供高频多核、八通道与 PCIe，Radeon PRO提供认证；具体软件供应商列表是硬约束。[A010][A016]

96核并非总更快，内存带宽、NUMA和许可证会限制。选择能降低总项目时间的核心数，给 ECC容量和 scratch。多 GPU仅在应用明确支持时，PCIe通道不创造软件扩展。

验收用真实装配体/网表/求解，测交互尾延迟、保存、求解和崩溃恢复。远程工作测编码与输入。企业管理、备件和驱动回滚纳入 TCO。

### 13. 蓝图 L：小型 AI 推理服务器

如果模型能装入单 Instinct/Radeon，优先单卡简化；超出再多 GPU。EPYC主机按数据前后处理、PCIe/NIC和内存选，非盲目最高核。Instinct MI350提供大 HBM和正式 ROCm企业路径，成本/功耗较高。[A034]

服务指标是 p50/p95/p99、吞吐、并发、质量和能量。batch提高吞吐却增加延迟，KV cache容量决定并发。加载、健康检查、滚动更新和模型切换均测。消费 GPU无企业 RAS/分区时风险更高。

网络入口与 GPU同 NUMA，TLS/tokenization可能占 CPU。模型服务应限流和隔离，避免一个长上下文耗尽内存。监控显存/HBM、温度、错误和队列。数据隐私与审计并行。

### 14. 蓝图 M：边缘视觉设备

工业视觉可选 Ryzen Embedded+iGPU、Versal、自适应 SoC或独立 Radeon。CPU方案开发容易，GPU适合并行模型，FPGA/Versal适合确定性传感器接口和低延迟流水。生命周期与宽温往往比峰值。[A019][A057]

数据路径从传感器、ISP、预处理、推理、决策到网络。每次格式转换和外存往返增加延迟。Versal可将协议/预处理放可编程逻辑，AI Engine执行固定图；Ryzen提供丰富 OS和应用。按开发团队能力选择。

验收最坏光照、温度、振动、网络断开和模型更新。平均准确率不足以覆盖安全场景，测漏检/误检和故障状态。长期供货、固件签名和远程回滚写入采购。

### 15. 蓝图 N：网络与存储设备

EPYC Embedded提供多核、内存和 PCIe，Pensando DPU卸载包处理/安全，FPGA处理定制协议。选择依据包率、规则规模、延迟、加密、存储和可编程，不以 CPU核数独占。[A059][A063]

小包 PPS比大包 Gb/s更难，测试双向、多流、不同 MTU和规则。DPU控制平面故障不能断业务，验证高可用、升级与旁路。NUMA把 NIC队列、CPU线程和内存对齐。

存储测试持续写、重建和故障，不只缓存峰值。CXL/PCIe设备支持需平台验证。网络安全规则更新、遥测和审计是软件成本，硬件卸载若运维无法使用就没有价值。

### 16. 跨蓝图：产品名称解码

第一步在 AMD规格页定位精确料号；第二步写 CPU架构和核心类型；第三步写 GPU架构/CU、NPU代际；第四步写内存类型、通道/位宽、容量；第五步写 PCIe/端口；第六步写默认/可配置功耗；第七步查软件支持；第八步查 OEM实现。

例如 Ryzen AI Max+ 395不是“395代”，而是家族内16核/40CU/50 TOPS代表；Ryzen 9 9950X与9950X3D共享家族但缓存/功耗不同；Threadripper PRO 9995WX需要 WRX90才能获得完整平台；RX 9070与9070 XT不是只差频率。

命名相似的 MI350X、MI355X、MI350P还有模组、功耗和平台差异。数据中心采购按完整部件号/BOM，不能写“八张 MI350”而省略。产品资源中心是数据表入口。[A003]

### 17. 跨蓝图：软件清单模板

列出 OS与版本、必须应用及插件、模型/框架、图形 API、编解码、虚拟化、驱动、外设和安全。每项标“官方支持、社区支持、未验证、不支持”。任何必须项不支持，候选淘汰或先做 PoC。

AI项目进一步列算子、精度、模型格式、自定义 kernel和部署后端。HIP可移植性按源码、库和性能三档。[A040] NPU按编译覆盖。Windows与 Linux分别列，不从一侧外推。

版本清单进入配置管理，升级有回滚和测试。个人也可保存 BIOS/驱动/模型信息，遇到问题不靠记忆。

### 18. 跨蓝图：成本模型

三年 TCO=硬件+内存/存储升级+软件许可+迁移工时+电力+停机/维护−残值。云/远程还加网络与数据费用。更快硬件若每天节省工程师30分钟，价值可能高；只偶尔跑一次则租用更好。

焊接内存需要首购足量，提高初始价却避免整机更换；可升级桌面可延后成本。Threadripper主板/内存/散热昂贵，通道可替代多台普通机。消费 GPU便宜但工程移植和停机可能超过卡价。

成本范围而非单点，给电价、利用率和故障概率敏感性。不要把未来未上市性能计入收益。官方 MSRP和实际含税价分开。

### 19. 跨蓝图：可持续性能预算

给每个系统设功耗、温度和噪声上限。桌面在墙上、移动在电池、机架按插座/冷却。先用默认测性能，再降压/Eco寻找甜点，超频作为可选而非基线。

CPU、GPU、NPU共享封装时，单项功耗相加会超预算，固件动态分配。混合任务测；若游戏加 NPU导致 GPU下降，需调整设备放置。独显系统也共享散热/PSU。

能量以任务焦耳，不用 TDP推断。空闲占比较大的办公系统优先待机，长满载优先每瓦。风扇噪声是可用性约束。

### 20. 跨蓝图：兼容性风险预算

把新硬件、社区驱动、非认证软件和超频各视为一个风险因子，关键系统不要同时堆四个。需要新 GPU时用稳定 OS；需要新 OS时保留成熟硬件；PoC可以激进，生产逐步收敛。

AMD消费 ROCm覆盖扩展很快，快照结论会过期。更新前查矩阵并保存旧容器。[A038][A039] Ryzen AI应用也可能逐步启用，购买时只为已用功能支付确定价值。

### 21. 跨蓝图：数据安全与隐私

本地 AI减少传输机会，不保证隐私。检查应用网络、模型来源、日志、临时文件和 crash dump。全盘加密、账户隔离和备份。企业使用 PRO/EPYC安全功能仍需正确配置。[A078]

显存/共享内存可能留数据，多用户消费 GPU隔离有限。远程 API使用 TLS、认证、限流。模型许可证和个人数据合规与硬件无关，但必须纳入部署。

### 22. 跨蓝图：升级路径

桌面预留 PSU、机箱和主板通道，升级 CPU前查 BIOS；移动/Max一次买够 RAM，SSD可换则记录尺寸；掌机可换 SSD但恢复系统；工作站通道按未来 GPU/NIC留物理空间。

软件升级与硬件分离。先在副环境升级 ROCm/驱动，跑冒烟/基准，再生产。固件回滚不总允许，更新前读公告。路线图不是升级保证。

### 23. 蓝图验收总表

所有系统都验收：准确识别 SKU；默认稳定；内存容量/通道；PCIe宽度；持续 CPU/GPU；目标应用正确；睡眠/重启；网络/存储；温度/噪声；更新/恢复。AI再加模型覆盖和质量，工作站加 ECC/认证，多 GPU加 P2P，移动加电池。

结果标为通过、条件通过、失败、未测。条件通过要写限制，例如“仅 Ubuntu某版本官方”“仅插电性能模式”“FSR 4仅支持列表游戏”。未知不能变成营销承诺。

### 24. 何时选择 AMD，何时不选

当任务重视 x86兼容、X3D游戏缓存、核心/价格、AM5升级、较大 Radeon显存、Ryzen AI Max共享容量、EPYC通道或开放 ROCm时，AMD可能形成独特组合。当必须 CUDA-only、特定认证/驱动、Apple生态、Arm原生能效或某 OEM服务时，竞争平台可能更合适。

正确结论不是品牌全胜，而是具体蓝图的约束被满足且风险可控。未来软件改善可能改变答案，所以保存复现清单和失效条件。产品体系越完整，越需要在“可组合”与“已验证组合”之间保持区别。

### 25. 最终交付模板

项目交付包含：需求和硬约束；选择的精确 BOM；官方来源；软件版本锁；原始测试；已知限制；恢复步骤；三年容量/升级；安全与备份；重新评估触发器。个人购买也可缩短成一页。

这份模板把 Ryzen、Radeon、Ryzen AI、Threadripper、EPYC、Instinct、Embedded和 Pensando从品牌地图转成决策。只有当数据路径、功耗、软件和终端共同闭环，硅架构的优势才成为真实产品价值。

---

## AMD 平台实验手册：持续性能、端侧 AI、兼容性与故障定位

### 1. 实验目标与最小可复现单元

实验不是为了制造一个总分，而是回答可执行问题：某台机器能否在规定噪声和功耗下完成任务；换内存、驱动或模型后差异来自哪里；连续运行是否稳定；升级能否回滚。每项实验写一个假设和反例，例如“模型受内存带宽限制”，反例是提高内存带宽后速度不变且算力利用上升。

最小记录包括设备精确 SKU、主板/整机型号和 revision、BIOS/AGESA、内存容量/通道/速率、GPU VBIOS与驱动、存储、OS build、内核、功耗模式、室温、应用/模型 commit、命令和原始输出。缺任何关键项，结果只能作为观察，不能跨系统推广。

重复至少三次，区分冷启动与热缓存。报告中位数、范围和异常原因，不只报最好一次。长时实验先热浸再取稳态。自动更新、杀毒、索引和遥测可能干扰，记录而非随意关闭；若关闭，结果代表实验环境而非默认用户体验。

### 2. 基线快照

硬件层在 Linux用 `lscpu`、`lspci -nnvv`、`dmidecode`、`numactl -H`、`rocminfo` 等，在 Windows用系统信息、设备管理器、GPU-Z/HWiNFO类可信工具和 PowerShell。保存文本，不只截图。核对核心/线程、缓存、NUMA、PCIe速率宽度、内存通道、gfx target与 NPU。

固件层记录 BIOS所有非默认项：EXPO、PBO、Curve Optimizer、Resizable BAR、Above 4G、IOMMU、虚拟化、CPPC、风扇。开始时加载 optimized defaults，再只启用任务必需项。OEM控制中心模式也保存。

软件层导出包和驱动版本。ROCm用 `rocminfo`/包管理器，框架打印 build和设备；Ryzen AI记录运行时、模型编译器；游戏记录 build、着色器缓存状态。官方支持矩阵是可用性基准。[A037][A039]

### 3. CPU 单线程实验

单线程测试覆盖前端、分支、整数、浮点和内存，不用一个综合分。选择真实任务：代码编译单文件、压缩单线程、浏览器脚本、音频或某科学 kernel。合成 microbenchmark只用于解释端口/缓存，不作为体验。

记录有效时钟而非请求时钟，查看核心是否迁移。Windows调度可能在 CCD间移动，Linux可用 taskset稳定但应同时测默认。固定核心能揭示硬件，默认调度代表用户。最大 boost是短时上限，报告全程频率分布。

Zen 5宽前端在分支可预测、ILP足够时获益；指针链仍受延迟。使用硬件计数器看 cycles、instructions、branches、misses、L1/L2/L3与 DRAM。IPC高低不能跨不同指令比较，完成时间是主指标。[A023]

### 4. CPU 多线程与扩展

从1、2、4、8线程逐步到全部物理核和 SMT，绘制速度提升与效率。理想线性很少，串行比例、锁、内存和频率下降会弯曲。超过物理核后 SMT收益依空闲执行资源，可能为正、零或负。

编译、渲染和压缩分别代表依赖图、浮点和内存。Threadripper测试要扩到不同 CCD/NUMA，检查线程是否跨域。高核心数若每核带宽不足，速度平台期；增加 DIMM通道或改善数据布局比更多线程有效。

记录封装功耗、温度和稳态频率。PBO能提高功耗换性能，Eco Mode降低功耗可能提高每瓦。比较默认与 Eco时以完成任务能量评估。对生产机器，稳定与噪声是约束，不以最高分自动获胜。

### 5. 缓存和 3D V-Cache 实验

用不同工作集大小做延迟/带宽阶梯，观察 L1/L2/L3/DRAM边界。随机指针链测延迟，顺序多流测带宽，两者不要混。预取会让顺序访问看起来快，这是实际能力而非作弊，但不代表随机。

比较 X3D与非 X3D须对齐核心、频率、内存和功耗，难以完全对齐时明确。游戏选择 CPU限制场景，同时测4K GPU限制作为反例。生产任务选择数据库、编译、仿真和渲染，避免只选有利样本。[A029]

双 CCD X3D记录线程落点。可用每 CCD计数器或限制 affinity做诊断，再测默认调度。若绑到缓存 CCD提升，问题可能是识别；若默认最好，手工优化无必要。升级 Windows/驱动后重测。

### 6. DDR/LPDDR 内存实验

带宽测试含读、写、复制和多线程；延迟含本地与跨 NUMA。内存超频比较保持 CPU功耗和任务相同，记录 UCLK/FCLK比例与时序。标称 MT/s提升但控制器分频时，延迟可能上升。

稳定性不能只跑短 MemTest。使用多种模式、接近满容量、CPU+GPU共享、睡眠和冷启动。错误计数哪怕很少也不可接受，尤其编译/压缩校验。EXPO属于超频，失败时回默认不是性能倒退而是正确诊断。

APU iGPU测试单双通道/不同 LPDDR速率，记录 GPU频率和功耗，区分带宽墙与功耗墙。若 GPU频率相同而内存带宽上升带来帧率，说明供数；若封装功耗满，CPU/GPU争用需单独测试。

### 7. GPU 光栅实验

选择至少三类游戏：电竞高帧、现代开放世界、重着色/高分辨率。固定版本、场景、画质、分辨率、API和着色器缓存。优先内置可复现 benchmark，再用固定路径采集；多人在线不适合精确对比。

报告平均、1% low、0.1% low或完整帧时间分布，注明采集长度。平均高但周期尖峰会体感卡顿。首次运行与热缓存分开，驱动更新后清缓存可测编译体验。CPU限制时记录 GPU利用和频率。

功耗测整卡/墙上，记录 TBP、热点、显存温度、风扇和噪声。AIB卡比较默认 BIOS，不混厂商 OC。降压要做多游戏和长时，单 benchmark稳定不够。

### 8. 光追、超分与帧生成

光追按相同 RT效果、分辨率和无超分基线比较，再分别开启 FSR/DLSS/XeSS。不同质量档内部渲染比例可能不同，应记录。截图用相同帧/运动场景，观察细线、透明、粒子、鬼影和闪烁。

帧生成报告基础 FPS、输出 FPS和输入延迟。若基础 30插到60，画面更新数增加但游戏逻辑和输入仍接近30，伪影概率高。使用延迟测量或高帧摄像，不能由输出 FPS推断响应。

FSR 4依游戏集成和支持硬件，官方页面确认技术范围，具体质量由版本。[A048] 驱动级覆盖若存在，也需区别原生。不要把一个游戏的结果推广全部 RX 9000。

### 9. 显存容量与 PCIe 溢出

逐级提高纹理、分辨率、光追和资产，记录显存占用、驻留、PCIe流量和帧时间。当分配接近容量时，观察是否突然尖峰。监控工具的“已分配”不等于物理使用，结合驱动预算。

比较8/16/24GB卡时尽量同 GPU，否则算力混淆。可用创作项目或模型控制容量。PCIe 4/5和 x8/x16差异在容量内可能小，溢出后增大。Resizable BAR分别开关并逐游戏验证。[A054]

APU共享内存没有独立显存硬边界，却可能把系统推向 swap。记录总 RAM、GPU分配和 page faults。把共享内存机器描述为“96GB显存”会忽略系统保留，报告“GPU可用共享内存上限与条件”。

### 10. 媒体实验

编码测试固定源视频、分辨率、帧率、色深、码率、GOP和预设。比较速度、VMAF/SSIM与人工画质，文件大小一致才公平。直播加入场景变化、文字和高速运动；测丢帧和音画同步。

解码测试4K/8K、AV1/H.265、HDR和多流，记录 CPU/GPU占用、掉帧与功耗。浏览器是否调用硬解由 codec和 DRM决定。外接屏色深/HDR链也验证。

剪辑导出同时测试 timeline交互、特效和最终输出。应用可能在不同效果用 CPU/GPU，日志或性能图确认。Radeon支持某编解码不保证插件调用。

### 11. NPU 模型编译与正确性

选择公开可重复模型，固定 ONNX opset、输入和权重 hash。先在 CPU高精度运行，保存输出和任务指标。按 Ryzen AI Software流程量化/编译，保存报告，列出 NPU子图、CPU fallback和不支持算子。[A041][A042]

分类模型测 top-1/任务数据集，检测测 mAP，语音测 WER，生成模型用任务指标与人工样本。不要只比单张图。量化校准集需代表生产分布，偏差会让硬件看似快但质量不可用。

性能分冷编译、冷启动、预热后单请求、批量和持续。NPU目标通常小批低功耗，强行大 batch追 TOPS可能不符合端侧。记录 CPU前后处理，端到端才是应用时延。

### 12. CPU/GPU/NPU 横向调度实验

同一模型在 CPU、GPU、NPU用相同输入与精度尽量对齐；若后端不支持相同精度，明确质量差异。测时延、吞吐、能量、峰值内存和前台响应。结果不是决定一个永久赢家，而是建立任务调度规则。

测混合负载：视频会议 NPU+浏览器 CPU、游戏 GPU+语音 NPU、本地 LLM GPU+编译 CPU。记录封装功耗和内存争用。单设备最快的后端在混合任务中可能伤害前台，应考虑优先级。

自动调度在驱动/应用升级后可能改变，使用 trace确认。若系统把模型回退 CPU，结果不能记作 NPU性能。应用应暴露后端选择与失败原因。

### 13. 本地 LLM 实验

记录模型全名、权重 hash、参数量、量化方法与位宽、上下文、batch、线程、GPU offload层、后端版本。提示分为短问答、长文摄入和多轮，分别报告 prompt processing与 generation token/s、首 token和峰值内存。

验证输出质量。不同量化用固定评测集或任务成功率，不能只展示一段看似合理文字。温度和采样参数固定。模型版本不同的速度/质量不可归因硬件。

Ryzen AI Max重点测试共享内存容量曲线：模型从完全驻留到接近上限，再到触发换页。观察 token/s和尾延迟。与独显比较同模型量化和功耗，独显容量不足时报告分层方案，不把失败排除。

持续一小时观察内存泄漏、热、频率和上下文增长。并发测1/2/4用户，吞吐和尾延迟权衡。交互助手更看首 token，批处理看总吞吐。

### 14. ROCm 安装与支持验证

先从矩阵选硬件/OS/版本，不从安装教程倒推支持。[A038] 保存发行版、内核、驱动和 ROCm包。安装后运行设备枚举、简单 HIP、库 GEMM、框架 tensor，再跑目标应用，逐层定位。

容器记录镜像 digest，映射 `/dev/kfd`和 render设备，验证用户组。容器看到 GPU不表示库 target包含该 gfx。检查 kernel code object。避免用不受支持环境变量覆盖 gfx作为长期方案，它可能产生错误结果。

Windows区分 Runtime与 HIP SDK，某些库 Linux-only。[A039] WSL和原生路径分别测。新消费 GPU社区启用可以探索，但报告标为非正式，生产建议等待官方。

### 15. HIP 移植实验

建立 CUDA基线的正确输出与性能。用 hipify转换，先求编译和正确，再分析热点。列出无法转换的库、PTX、warp假设和扩展。每项替换写测试，避免一次改大块后无法定位。

使用 HIP API的设备属性而不是硬编码 wave/warp，采用 cooperative groups或库原语。对 reduce/scan等优先 rocPRIM/hipCUB。GEMM用库并搜索调优。源可移植后，在 NVIDIA和 AMD CI同时运行。[A040]

性能调优观察 occupancy不是越高越好，寄存器/LDS减少可能增加访存。扫描 tile、工作组和向量宽度，保留每架构配置。对比相同精度和算法，不让一个后端启用 Tensor Core而另一个用标量。

### 16. 多 GPU 与通信实验

绘制 `lspci -tv`/rocm topology，标出 GPU、CPU、NIC。先做每对 GPU P2P可用、单向/双向带宽和延迟。再跑集合通信不同消息大小、rank顺序和线程。总线带宽口径注明。

跨节点先验证普通网络、MTU、丢包，再 RDMA/GPUDirect路径。NIC与 GPU绑同 NUMA，CPU通信线程也靠近。用单对流确认峰值，再全节点暴露拥塞。交换机缓冲、ECN/PFC或 IB配置由网络团队记录。

应用扩展效率等于单卡时间除 N卡时间再除 N。低效率可能来自通信、数据加载、负载不平衡或小 batch。Profiler分解，不直接怪 RCCL。失败注入拔掉进程/端口，验证超时和恢复，不让作业无限挂。

### 17. 存储与数据加载

NVMe测试顺序、随机、不同队列深度、持续写和温度。厂商峰值通常高 QD，桌面加载可能低 QD。文件系统、加密和压缩按实际开启。测试数据大于 RAM避免全由页缓存。

AI训练监控 GPU空闲等待 DataLoader。增加 worker、预取、固定内存和数据格式，观察 CPU与存储；过多 worker会争内存。图像解码可在 CPU/GPU，比较总能量。

游戏加载记录首次、重复、着色器编译和网络。SSD更快若 CPU解压限制，加载变化小。模型加载包括反序列化和量化转换，也不能只看磁盘读。

### 18. 续航和能效实验

电池先校准健康与实际容量。固定亮度（用亮度计）、刷新、无线、音量和电源模式。办公脚本应包含网页、视频、文档和空闲，记录每阶段；连续视频仅代表解码。

任务能量用 Wh或积分功率，报告完成时间。插电墙上功率减空闲基线可估增量；笔记本用电池放电更接近移动。充电时不要测放电，电池温度影响。

比较 Eco/平衡/性能模式，寻找性能/瓦甜点。掌机按多个 TDP点绘制 FPS/W，而不是只测最大。NPU与 CPU/GPU同任务也以质量相同的焦耳比较。

### 19. 热浸、噪声与可靠性

热浸至少到温度和时钟稳定，通常30–60分钟；大型机架更久。记录室温，温升比绝对温度可跨日比较。CPU/GPU混合比单项最能加热机箱。SSD、VRM和内存温度也看。

噪声在固定距离和背景测 dBA，同时记录频谱/主观音调。风扇周期波动可能平均分贝不高但恼人。修改曲线后验证温度和部件寿命，不让 VRM/SSD失去气流。

长时可靠性用重复实际任务、内存和 GPU压力，监控 WHEA、AER、MCE、amdgpu reset和应用校验。压力软件可能产生不现实功耗，只作为边界，实际任务决定稳定。

### 20. Windows/Linux 双平台兼容

同硬件双系统比较前对齐 BIOS和功耗，应用编译器/驱动差异如实保留。Windows游戏和专业插件可能更完整，Linux开发/ROCm可能更完整。不能把不同 API性能当纯 OS开销。

测试睡眠、外屏、音频、Wi-Fi、蓝牙和 USB，而非只算力。新 APU Linux enablement可能需新内核；Windows OEM驱动可能落后通用版。记录哪些路径正式支持，哪些社区修补。

文件共享注意 NTFS Fast Startup和权限，模型/数据 hash验证。双系统时间和 Secure Boot配置记录。系统更新后跑冒烟集合。

### 21. 虚拟化与容器

CPU虚拟机测试嵌套、IOMMU、NUMA和内存；vCPU不要跨太多 CCD。GPU直通需要独立 IOMMU组、VBIOS和 reset支持，消费 GPU不保证企业级 SR-IOV。成功一次重启不等于可长期重置。

容器隔离用户态但共享内核驱动。镜像 ROCm版本必须与宿主兼容。限制内存/CPU/GPU可见性，记录 cgroup。多租户消费 Radeon缺硬分区，不应用容器声称安全隔离。

WSL2是虚拟化路径，文件放 Linux虚拟磁盘通常比跨 `/mnt/c`大量小文件快。GPU由 Windows驱动映射，兼容按官方 WSL矩阵。性能差异需用相同文件位置和版本解释。

### 22. 安全功能实验

验证 Secure Boot状态、TPM、磁盘加密、固件密码和恢复。更新 BIOS后确认 BitLocker，不把关闭安全作为永久性能调优。企业管理测试远程资产、更新和擦除，具体 PRO/OEM支持为准。[A078]

虚拟化内存加密/机密计算在 EPYC上需 hypervisor与 guest支持，测启动、证明和性能。GPU/DPU是否进入信任边界需架构文档，不因 CPU加密就假定 DMA设备数据全保护。

模型供应链保存签名/hash、依赖 SBOM与容器扫描。离线推理用网络监控确认无外发。敏感提示日志和崩溃转储也要清理。

### 23. 故障定位树：无法启动

新装 AM5无启动：确认 CPU供电、内存插槽、BIOS支持和诊断灯；第一次 DDR5训练可较久，不反复断电。用单条 QVL内存、核显/最少设备、默认 BIOS。需要时用明确文件与步骤 Flashback。

升级后不开机：清 CMOS会删除 EXPO/PBO；保留 BitLocker密钥。若旧 CPU可用，确认 BIOS确实刷新。检查针脚/插槽和散热压力。不要在未知状态连续盲刷不同 BIOS。

GPU无显示：确认线插独显/核显、供电、插槽和显示器输入，尝试低速/另一端口。Above4G/ReBAR先默认。若系统已进 OS，用远程或核显查驱动。

### 24. 故障定位树：随机重启与驱动超时

先恢复 CPU/GPU/内存默认，检查 PSU接头、墙插和温度。读取 WHEA/AER/系统日志，区分电源瞬断、CPU机器检查、GPU reset和应用崩溃。没有蓝屏的关机常指供电，但仍需证据。

内存不稳可表现 GPU超时，因为 GPU从错误系统内存读取。运行内存校验；独显显存错误另测。更换驱动前保存日志。若只一个游戏失败，查游戏和着色器；所有 GPU负载失败再看硬件/驱动。

笔记本 OEM模式和 MUX切换会触重启/黑屏，使用支持路径。eGPU问题分热插拔、链路、线缆和电源。不要用提高电压作为第一修复。

### 25. 故障定位树：ROCm/NPU 不工作

ROCm：矩阵是否点名 GPU/OS；`lspci`与内核是否识别；用户是否有设备权限；`rocminfo`是否列 agent；框架是否为 ROCm构建；gfx target是否包含。逐层，勿一开始重装系统。

若简单 HIP工作而框架失败，问题在用户态版本/扩展；若框架基本 tensor工作而模型失败，列 unsupported op或 CUDA-only模块。性能低用 profiler确认 CPU fallback和传输。

NPU：设备管理器/xdna枚举、运行时版本、模型编译、算子覆盖、应用执行提供者。应用显示“AI”不保证 NPU。保存编译/运行日志，检查是否转 CPU。OEM固件和 Windows build按官方要求。

### 26. 结果解释的统计纪律

硬件 benchmark噪声来自温度、后台、随机调度和场景。三次很接近可报告中位，差异小于测量变异就写“无可辨差异”，不强行排序。必要时增加样本和置信区间。

多个游戏求几何平均可描述比例，但会掩盖兼容问题。附每游戏结果和异常，样本选择提前定义，避免看到结果后挑。厂商测试若选择不同游戏，不能直接对比平均。

回归阈值根据任务：性能下降3%可能在噪声内，正确性错误任何比例不可接受，尾延迟增加20%对交互严重。预先写门槛，减少确认偏差。

### 27. 报告模板

标题写问题；摘要写结论和适用边界；环境用表；方法给命令/场景；结果保存原始 CSV和图；分析用计数器解释；限制列未控制变量；附复现步骤和来源。不要截断不利结果。

产品宣称单独一栏，注明 AMD测试条件；自己的结果不写成“官方”。若引用产品规格，使用来源 ID。动态价格写日期和地区，不纳入长期技术结论。

数据文件用 ISO日期和 git commit，脚本版本控制。敏感序列号、用户名、token在公开前扫描。原始日志可压缩发布，个人信息脱敏。

### 28. 代表工作负载套件

消费桌面：浏览器、Office类、代码编译、压缩、目标游戏、视频转码、待机。创作：时间线、RAW、Blender、音频低延迟。AI PC：视频会议 NPU、图像/语音、LLM。掌机：多个 TDP、休眠、五款游戏。

工作站：全核渲染、编译扩展、内存带宽、多 NVMe、多 GPU、认证应用。服务器：NUMA、虚拟化、网络、数据库、功耗。AI：单 GPU kernel、模型训练/推理、多 GPU/节点、checkpoint和故障。

套件不需覆盖所有 benchmark，要覆盖购买任务和瓶颈类型。每季度保留相同核心集合评估驱动回归，新应用作为补充而非替换历史。

### 29. 从实验到决策

先检查硬约束是否全部满足，再对稳定结果加权。若两个平台性能接近，选择内存余量、噪声、服务和迁移成本更好者。若 AMD硬件领先但应用不支持，记录等待条件和替代后端，不用未来承诺补分。

为选择写失效条件：模型超过内存、软件停止支持、工作负载转 CUDA-only、功耗预算改变、OEM固件结束。达到条件就重评。这样购买不是品牌忠诚，而是可管理假设。

最终可复现证据应该允许另一个人指出错误。保留环境、原始数据和失败，才能区分 Zen/RDNA/XDNA的真实特征、OEM实现和偶然噪声。深度分析的完成标准不是最长报告，而是每个重要结论都有下一步验证方法。

---

## AMD 完整产品体系：从 Ryzen 到 EPYC、Instinct、Xilinx 与 Pensando

### 1. 为什么消费产品必须放回完整体系

AMD 的产品线看似横跨个人电脑、游戏机、服务器、AI 加速、工业、航空航天和网络，底层却共享研发与供应链资产：x86 核心、图形与计算架构、片上网络、内存控制、安全处理器、chiplet 封装、编译器和代工合作。消费端获得服务器验证过的 CCD、I/O 与安全思想；数据中心获得大规模 Ryzen 生态带来的 x86 软件基础；半定制和嵌入式又把相同 IP 放进更长生命周期。

这种协同不是“同一芯片改名”。EPYC 需要八到十二通道内存、海量 PCIe/CXL、RAS 与多插槽；Ryzen 需要低延迟、成本与主板兼容；Instinct 使用 HBM 和专用互连；Versal 带可编程逻辑；Pensando 把包处理和安全隔离移到 DPU。共享的是模块和工程方法，产品合同不同。

### 2. 业务分部与收入结构

AMD 2025 年 10-K 报告总营收 346.39 亿美元：Data Center 166.35 亿，Client and Gaming 145.50 亿，其中 Client 106.40 亿、Gaming 39.10 亿；Embedded 34.54 亿。[A001] 2025 年开始 Client 与 Gaming 合并为报告分部，历史数字重述。分析趋势时不能把新分部直接与早期未经重述口径相减。

数据中心同比增长由 5th Gen EPYC 与 Instinct MI350 需求推动；Client and Gaming 同比增长包含 Ryzen 出货与 ASP、半定制和 Radeon；Embedded 同比下降反映部分终端需求混合。收入说明商业权重，不说明毛利或技术优劣。数据中心 166 亿也不能全部归于 AI GPU，因为其中有 EPYC CPU。

2025 年研发费用约 80.91 亿美元，同比增加约 25%，10-K 将增长主要归因于 AI 战略相关人员成本。这个资源转移会加快 Instinct、ROCm 和网络整合，也形成执行风险：消费驱动、平台固件和嵌入式客户仍需要长期投入。完整产品体系的优势在复用，组织复杂度则可能稀释优先级。

AMD 是 fabless 公司，依赖外部晶圆制造、封装、HBM/内存和基板供应。先进节点产能可在 CPU、GPU 与加速器间竞争；chiplet 可以让不同节点分工，却不能消除先进封装和高速内存瓶颈。出口管制也会改变可售 SKU；2025 年 MI308 相关库存和费用表明政策风险已进入财务结果。

### 3. Client：Ryzen 与商用 PRO

Client 业务覆盖桌面与移动 Ryzen、Ryzen AI、Threadripper 的部分渠道以及商用 PRO。零售桌面提供可见 SKU，但移动收入主要通过 OEM。AMD 向 OEM 销售处理器，最终设备的内存、屏幕、定价和地区上架由合作伙伴决定，因此处理器“发布”与消费者“买到合适配置”存在时间差。

Ryzen PRO 在相同计算基础上增加企业管理、安全、镜像稳定和供货承诺。AMD PRO 技术页说明安全与可管理功能，但企业仍需验证具体平台是否支持远程管理、固件更新、TPM、磁盘加密和企业镜像。[A078] 某 SKU 标有 PRO 不等于所有 OEM 打开全部功能。

Client 的商业杠杆是设计赢单：一颗移动 APU 被多家 OEM、多个尺寸采用，量可以摊薄研发；风险是 OEM 配置把平台优点削弱，例如单通道内存、低容量焊接 RAM、差屏幕或限制功耗。AMD 需要通过参考设计、驱动和 Advantage 计划影响整机，却没有 Apple 那样对整机的完全控制。

### 4. Gaming：Radeon 与半定制双引擎

Gaming 包含 Radeon 消费 GPU 和游戏机半定制 SoC。两者周期不同：独显随 PC 升级、加密/AI 需求和竞争产品波动；半定制随主机世代、库存和大客户订单波动。合并收入掩盖两者此消彼长，分析时应查管理层解释，不从总数推断 Radeon 市占。

Radeon 的价值链包括 GPU 裸片、AIB 板卡、驱动、游戏合作和 GPUOpen。AMD 通常提供参考规格，合作伙伴决定非公版散热、供电和价格。零售渠道可能在建议价上下波动，官方发布价只能作为起点。显卡业务也为 APU 共享 RDNA、媒体和显示 IP，并为游戏机开发者形成相近着色器经验。

半定制则是共同工程和长期供货。客户可以要求定制 CPU/GPU 比例、I/O、安全和媒体，AMD 获得大批量但毛利、付款和库存受合同影响。产品不会以 AMD 零售料号出现，终端规格应从 Sony/Microsoft/Valve 等确认。消费者不能安装 Radeon 通用驱动给主机，也不能按主机理论算力预测 PC 卡。

### 5. EPYC：服务器 CPU 不只是更多 Ryzen

5th Gen EPYC 9005 基于 Zen 5 和 Zen 5c，覆盖高频少核、通用多核与最高 192 核密度 SKU。[A032] 它使用 SP5 平台、多通道 DDR5、大量 PCIe 5.0/CXL 与企业 RAS。CCD 与 IOD 的模块化类似 Ryzen，但服务器 IOD、内存通道、安全和验证不同。

EPYC 产品规格数据库按 SKU 给出核心、频率、缓存、TDP、内存和插槽能力，是配置服务器的依据。[A033] “最高 192 核”不能代表所有 SKU：高频数据库许可、EDA 或交易可能选择少核高频；虚拟化和云原生吞吐选择高密度；HPC 还要平衡每核内存带宽。

服务器的性能单位是系统，不是插槽。十二通道内存必须填充合适 DIMM 才获得带宽；双插槽会形成 NUMA；PCIe 设备分属不同 CPU 根复合体；虚拟机和容器要保持 CPU、内存、NIC、GPU 亲和。错误布局能让旗舰 EPYC 慢于正确配置的较小型号。

Zen 5/5c 架构白皮书记录内存、I/O、可信执行与 RAS，说明数据中心差异来自平台合同。[A023] 消费 Ryzen 有 ECC 能力不等于获得服务器级 DIMM、内存巡检、故障预测、热插拔和 OEM 认证。EPYC 的成本包含长期固件、验证和服务。

EPYC 还作为 GPU 主机。Instinct 平台需要 CPU 提供 PCIe、内存、存储和网络，GPU 训练并非 CPU 无关。数据加载、tokenization、通信进程和 checkpoint 会消耗主机资源。选择 EPYC 与 GPU 数量时应画出 NUMA/PCIe 拓扑，确保每张加速器接近 NIC 和内存。

### 6. Instinct：从单 GPU 到 AI 系统

Instinct 是数据中心 GPU 加速器家族，采用 CDNA，覆盖 MI200、MI300、MI325、MI350 等。产品单位可能是 PCIe 卡、OAM 模组、八 GPU UBB/平台或整机，规格不可混用。MI300X 的 192GB HBM3 和平台互连适合大模型，MI300A 将 CPU/GPU 组合用于 HPC APU。[A035] AMD 在 2023 年 Data Center and AI Technology Premiere 正式把 MI300、EPYC 与软件生态合作纳入同一发布框架，这为后续系统化路线提供了公开锚点。[A085]

MI350X/MI355X 基于 CDNA 4，官方页列出最多 256 CU、288GB HBM3E、8TB/s 峰值理论带宽和多种低精度。[A034] MI355X 更高功耗/冷却定位与 MI350X 不同；MI350P PCIe 卡又面向现有服务器部署。比较 NVIDIA B200 时必须对齐 OAM/SXM/PCIe、功耗、稀疏、数据类型和软件版本。

Instinct 产品价值分四层。硅层决定矩阵、向量、HBM 和互连；平台层决定 GPU 数、拓扑、CPU、NIC 与散热；软件层包括 ROCm、驱动、数学库、通信和框架；服务层决定容器、模型、监控与支持。任何一层缺失都会使峰值无意义。

训练需要高带宽 all-reduce，推理需要容量、内存带宽、调度和低延迟。288GB HBM 可减少模型切分，但批量、KV cache 和并发会继续消耗容量。低精度提高吞吐，前提是模型质量和量化工具接受；稀疏峰值要求结构满足规则，不能用于稠密工作负载预算。

### 7. Helios 与机架级方向

AMD 在 Advancing AI 2025 正式介绍以 Instinct、EPYC、Pensando 和 ROCm 组成的开放 AI 系统方向，并预览 Helios 机架级设计。[A086] “机架”意味着供电、液冷、管理网络、GPU 域和东西向网络共同设计，而不是把多台服务器装进柜。

Helios 的竞争目标是缩短客户集成时间。加速器、CPU、NIC、交换和软件由同一平台路线协调，可以减少 BIOS、固件和拓扑组合；开放标准又允许 ODM、云厂商和网络伙伴参与。风险在于开放组件的版本验证更复杂，需要 AMD 提供参考 BOM、固件矩阵和端到端支持。

发布路线图不等于量产系统。报告只记录 AMD 在正式活动披露的部件和预期窗口；真正可用要看到具体 OEM/云实例、规格、软件版本和交付。供应链中的 HBM、封装、液冷、光模块与机架电力都可能影响爬坡。

### 8. ROCm：横跨产品的共同软件层

ROCm 是 AMD GPU 计算平台，包含驱动、HIP、编译器、数学库、通信库、调试与分析工具。[A037] 其核心战略是让 Instinct 成为训练/HPC 平台，并把一部分能力延伸到 Radeon 与 Ryzen。开源仓库便于审计与贡献，但用户仍依赖官方二进制、验证矩阵和框架发行节奏。

HIP 允许 C++ 核函数在 AMD 与 NVIDIA 后端间移植，hipify 可转换部分 CUDA 代码。[A040] 可移植不是零成本：warp/wave 大小、共享内存、原子、库算法、内联 PTX、扩展和性能调优都可能不同。最可移植的是使用框架和标准库的应用，最难的是深度依赖 CUDA 私有内核和二进制插件。

ROCm 支持矩阵按版本、GPU、OS、内核、虚拟化与组件变化。[A038] 企业应该冻结“固件—驱动—ROCm—容器—框架—模型”清单，升级先在副本跑正确性和性能。消费用户更要查 Radeon/Ryzen 专用矩阵，不能把 Instinct 文档的支持推到所有显卡。

### 9. Embedded：生命周期与确定性优先

Embedded 产品涵盖 Ryzen Embedded、EPYC Embedded、Radeon Embedded、自适应 SoC 和 FPGA。[A059] 工业、医疗、视觉、网络与航空客户通常重视长期供货、温度范围、确定性 I/O、功能安全和可维护性，而不是每年最高峰值。

Ryzen Embedded 将 x86 CPU、Radeon 图形和媒体带入工业 PC、数字标牌、边缘服务器，产品页按系列展示核心和生命周期。[A019] 与消费 APU 同源不代表相同封装、温度或供货；嵌入式料号可能降低频率换宽温，提供 ECC 或长期可用，并通过不同主板生态销售。

EPYC Embedded 适合网络、安全、存储和工业服务器。9005 Embedded 可提供 Zen 5/5c 与最高 192 核的系列能力，但设备厂商通常选择较少 SKU并进行多年验证。固件变更需要严格回归，升级节奏会慢于云服务器。

嵌入式业务在 2025 年收入下降不意味着技术线无价值，它常有更长订单和库存周期。Xilinx 客户在通信、工业与国防的需求受宏观、库存消化和出口规则影响。市场分析要区分短期渠道库存与长期设计赢单。

### 10. Xilinx：FPGA 与自适应 SoC

AMD 2022 年完成 Xilinx 收购，把 FPGA、自适应 SoC、AI Engine 和通信客户纳入产品组合。[A067] 收购后的战略不是让 FPGA 变成 x86 附件，而是把可编程逻辑、硬核 CPU、DSP、网络和软件与 AMD 数据中心/边缘资产组合。

FPGA 通过配置逻辑、存储、DSP 和 I/O 实现定制数据路径，适合协议桥接、低延迟交易、测试测量、视频和逐步演进的标准。[A058] 它的优势是可重新配置和确定性，代价是开发门槛、工具编译时间、面积/频率与软件人才。不能用 CPU/GPU 的 TOPS 直接比较。

Versal 自适应 SoC 组合 Arm 处理系统、可编程逻辑、NoC、DSP 和部分 AI Engine，覆盖 AI Edge、Prime、Premium、HBM 等系列。[A057] 不同系列对高速收发器、视频、雷达、网络或 AI 做取舍。开发流程包括硬件平台、时序收敛、固件和应用，更新周期比普通 PC 软件长。

Adaptive SoC 产品入口把 Versal 与 Zynq 等归入一套组合。[A057] 对 AMD 的协同包括：AI Engine 思想进入 XDNA；数据中心可用 FPGA 做网络/存储加速；嵌入式渠道扩大。整合风险是工具链并存——Vitis、Vivado、ROCm、Ryzen AI Software 和企业管理需要更统一的开发体验。

### 11. Pensando：把基础设施服务移出 CPU

AMD 2022 年完成 Pensando 收购，获得可编程 DPU、交换/网络软件和分布式服务平台。[A001] DPU 的目标是卸载虚拟交换、防火墙、加密、存储和遥测，使主机 CPU 留给租户，同时在硬件边界隔离基础设施。

Pensando DPU 产品组合服务云、企业和 AI 网络。[A063] DPU 不是更快网卡的同义词：它有可编程处理流水线、管理 CPU、加速器和安全启动，运行基础设施策略。收益取决于卸载比例、控制平面整合和运维；如果工作负载不需要虚拟网络/安全，DPU 可能增加成本。

Salina DPU 面向高带宽基础设施卸载，产品页描述网络、存储和安全能力。[A063] 部署必须验证主机接口、端口速率、SR-IOV、虚拟化平台、固件和高可用。DPU 故障可能影响整台主机连接，应有双端口、旁路、升级回滚和遥测。

Pollara 400 是面向 AI 后端网络的以太网 NIC，强调 Ultra Ethernet 与可编程能力。[A063] AI 网络不是仅提高端口速率：拥塞、负载均衡、丢包恢复、集合通信和 GPU Direct 数据路径共同决定训练扩展。产品正式页面证明定位，实际集群结果仍需公开平台和软件版本。

AMD 数据中心网络组合把 Pensando 与 EPYC、Instinct 平台连接。[A063] 理想状态下，CPU、GPU、NIC/DPU 与 ROCm 通信库共享拓扑和遥测；现实中还要与第三方交换机、光模块和编排兼容。开放以太网降低锁定，却把互操作测试责任分给更多厂商。

### 12. 数据中心互连与平台分层

一个 AI 节点至少有 GPU 内部 HBM、GPU 间高速链路、CPU—GPU PCIe、节点间 NIC、前端管理/存储网络。不同流量不能混成“网络带宽”。GPU all-reduce 需要低延迟高吞吐；checkpoint 需要存储带宽；控制平面需要可靠隔离；推理入口关注尾延迟。

EPYC 提供 PCIe 5.0 与 CXL 根，Instinct 提供 GPU 互连，Pensando 负责以太网数据路径，ROCm/RCCL 选择通信拓扑。机架级系统要避免 NIC 和 GPU 跨 NUMA，配置大页、IOMMU 和 ACS，确保 DMA 路径。理论链路速率还要扣编码、协议、包头和拥塞。

CXL 可用于一致性加速器和内存扩展，但具体 EPYC 平台、设备类型、BIOS 和 OS 必须验证。标准支持不等于任意 CXL 内存可插即用；延迟高于本地 DDR，软件分层和页面放置决定收益。容量型工作负载可能接受，延迟敏感数据库需基准。

### 13. 产品协同：真正能复用什么

**核心 IP 复用。** Zen 同时进入 Ryzen、Threadripper、EPYC 和半定制；RDNA 进入独显、APU和游戏机；CDNA 专注 Instinct；XDNA 来自自适应计算积累。复用缩短开发并扩大软件投入回报。

**chiplet 与封装复用。** CCD 可服务多市场，IOD 按平台定制；3D V-Cache、HBM 和多裸片封装积累供应链经验。AMD chiplet 白皮书强调接口、测试、隔离和恢复，说明生态化仍需要规范与验证。[A030]

**软件复用。** LLVM/Clang、HIP、开源内核驱动和模型框架跨产品共享；GPUOpen 将图形工具开放给游戏开发者；Ryzen AI Software 服务 NPU。但不同产品仍有独立驱动和支持矩阵，统一品牌不能替代版本工程。

**客户复用。** 云厂商可能同时采购 EPYC、Instinct 和 Pensando；OEM 同时做 Ryzen 笔记本与 Radeon 工作站；工业客户采用 Ryzen Embedded 与 Versal。交叉销售降低进入成本，但客户也可能坚持多供应商以避免锁定。

### 14. 商业模式与竞争壁垒

AMD 主要出售芯片、模组和平台，部分软件开放免费，用硬件收入回收研发。它不拥有大规模消费操作系统或完整云服务，因此依赖 Microsoft、Linux 社区、OEM、ODM、框架厂商和云伙伴。这比垂直整合更开放，也更难保证端到端一致。

壁垒来自 x86 兼容、CPU/GPU 设计能力、chiplet/封装经验、大客户验证、软件生态和长期路线图。单项 IP 可被竞争，但同时交付 CPU、GPU、NPU、FPGA、DPU 的组织与供应链需要多年。相反，产品广度会增加驱动矩阵、文档、支持和销售复杂度。

客户切换成本因市场而异。桌面 CPU 受插槽/主板约束，显卡受 API 和应用插件约束，服务器受虚拟化、采购和验证约束，AI GPU 受模型内核与集群运维约束，FPGA 受 RTL/工具工程约束，DPU 受网络控制平面约束。AMD 的“开放”战略要真正降低这些迁移成本，不能只提供接口名。

### 15. 完整体系的风险清单

供应风险包括先进晶圆、HBM、CoWoS 类先进封装、基板与光模块；技术风险包括大 chiplet 良率、互连、功耗和固件；软件风险包括 ROCm 兼容、Windows 驱动、NPU 应用和版本碎片；商业风险包括大客户集中、游戏机周期、PC 库存和嵌入式长周期；政策风险包括出口限制和地区合规。

并购整合也需持续观察。Xilinx 和 Pensando 带来技术与客户，但 10-K 的收购无形资产摊销、组织整合和工具链重叠会影响盈利与执行。判断整合是否成功，应看共同产品、软件统一、客户交叉采用和支持质量，不只看品牌被改成 AMD。

### 16. 组合评价

AMD 的完整体系可概括为：Ryzen 提供规模和用户入口，Radeon/半定制维持图形与游戏生态，EPYC 提供数据中心主机，Instinct争夺 AI/HPC，Xilinx 覆盖确定性与自适应计算，Pensando连接和隔离基础设施，ROCm/GPUOpen/Ryzen AI Software把硬件变为开发平台。各线不是简单上下游，而是共享技术、争夺资源又相互验证。

最强的情形是客户按任务组合：EPYC+Instinct+Pensando 构成 AI 节点，Ryzen AI 在端侧运行压缩模型，Versal 做传感与预处理，开发者用开放工具迁移。最弱的情形是硬件各自优秀、软件和版本却无法形成统一体验。未来评价 AMD 不应只问下一颗芯片多快，而应问跨 CPU、GPU、NPU、DPU 和自适应计算的数据与开发流程是否真正闭环。

---

## 内存、缓存、封装与互连：从 Ryzen DDR5 到 Instinct HBM

### 1. 性能不是“算力除以内存”这么简单

现代 AMD 平台是一组距离不同的存储层：寄存器最快最小，L1/L2 近核心，L3 在核心复合体或 3D 堆叠，DDR/LPDDR/GDDR/HBM 在封装内外，NVMe 与网络更远。每一层有容量、延迟、带宽、并发、功耗和一致性属性。程序的有效性能取决于数据是否在正确层、访问是否连续、预取是否成功，以及多个核心/设备是否争用。

带宽是单位时间可传输数据，延迟是一次请求等待时间。提高带宽不能消除依赖链上的单次延迟，减少延迟也不能给流式矩阵足够吞吐。游戏主线程和数据库指针追踪偏延迟，视频、GPU 着色和大矩阵偏带宽，编译与通用桌面混合。规格比较必须先归类负载。

### 2. CPU 缓存：命中率比标称容量更重要

每个 Zen 核有私有 L1 指令/数据和 L2，多个核心在缓存域共享 L3。L1 服务每周期高频访问，容量小；L2 承接较大工作集；L3 减少去 IOD/DRAM。缓存行通常按固定粒度移动，即使程序只读一个字节也会带入整行，因此连续数据结构比散乱指针更友好。

多核一致性保证核心看到符合内存模型的值，但修改共享行会在核心间转移所有权。两个线程更新同一缓存行的不同变量可能产生 false sharing，逻辑互不相关却互相使缓存失效。增加核心数后，这类同步流量会成为扩展上限。优化应从性能计数器确认，而不是盲目加预取。

Zen 3 后八核共享更大连续 L3，减少同 CCD 内跨复合体访问；跨 CCD 仍经过 IOD/Fabric。操作系统调度与线程池若频繁迁移，热缓存会丢失。低延迟服务可以把线程、内存和设备绑定同 NUMA，但绑核需要给中断和后台留资源。

### 3. 3D V-Cache：容量、热与调度的三角

3D V-Cache 通过垂直混合键合堆叠 SRAM，增加 L3 而不扩大计算裸片平面。[A029] 它特别适合“比普通 L3 大、比 DRAM 小”的复用工作集。缓存命中率从 90% 到 95% 看似只增加五个百分点，未命中次数却减半，因此延迟收益可能很大。

容量不是唯一变量。堆叠改变热阻，电压/频率策略可能不同；额外缓存访问也耗能；双 CCD 型号存在一侧大缓存、一侧高频的拓扑。游戏驱动和调度器识别工作负载后放置线程，若识别错误，跨 CCD 延迟抵消收益。稳定评测需说明芯片组驱动和 Game Mode。

对生产系统，缓存收益应以缺失率、内存带宽和端到端时长验证。若 DRAM 带宽显著下降且任务变快，说明缓存有效；若核心已满算或数据只读一次，增加 L3 无法创造复用。将所有游戏均值归因于“缓存越大越快”会掩盖引擎差异。

### 4. DDR4 到 DDR5：通道、子通道与训练

DDR5 提高传输率、每 DIMM 内部子通道并改进电源管理，AM5 桌面和新移动/服务器平台采用。标称 DDR5-6000 表示数据传输率，不是内存控制器时钟，也不是应用必达带宽。真实带宽扣除命令、刷新、行切换和控制器效率，延迟还包括时序周期与时钟换算。

桌面 Ryzen 通常双通道。每通道插一条 DIMM 更容易达到高频；四条 DIMM 增加载荷，主板可能降低官方速率。EXPO 是超频配置，不保证每颗 CPU、主板和内存组合稳定。长时内存测试应覆盖冷启动、热机、休眠唤醒与大容量，因为训练成功不等于无静默错误。

容量优先级取决于任务。容量不足导致换页到 SSD，损失远大于内存频率差；容量足够后，低延迟与带宽才有意义。游戏主机 32GB 通常提供余量，本地模型、虚拟机、视频和大型工程可能需要 64–128GB。ECC 能检测/纠正部分错误，但消费平台是否真正报告、主板是否布线和固件是否启用，必须逐项验证。

### 5. LPDDR5X：移动能效与不可升级

LPDDR5X 通过更宽封装内接口和低功耗状态服务 APU。它常焊接在主板附近，走线短、待机能效好，也能为 iGPU 提供带宽。代价是购买后不能扩容、维修牵连主板。标称 MT/s 高不表示 CPU 延迟一定低，内存控制器和封装拓扑仍不同。

Ryzen AI Max 的 256-bit LPDDR5X 是其大 iGPU 和共享内存的基础。[A051] 与普通 128-bit APU 相比，理论带宽显著扩大，但 CPU、GPU、NPU、显示和 I/O 共同使用。运行本地 LLM 时，模型权重顺序读取接近带宽限制；同时录屏、编译或游戏会争用，token/s 不能只由内存规格预测。

OEM 可以选择不同容量和速率，同一处理器不保证都配最高内存。低容量 SKU 会削弱“大共享内存”卖点；高容量价格可能逼近独显工作站。采购时记录可由 GPU 使用的最大 UMA、系统保留和 BIOS 设置，而不是只看焊接总容量。

### 6. GDDR6：独显的专用带宽

Radeon RX 用 GDDR6 通过多控制器和较宽总线提供显存。理论带宽等于引脚速率乘总线宽度再除八；实际访问受控制器、页面、压缩和缓存影响。Infinity Cache 减少外部流量，使较窄总线在命中时表现好，但缓存穿透场景仍受 GDDR 带宽。

独立显存把图形资产和系统内存分开。CPU 上传纹理需要经过 PCIe，GPU 访问未驻留数据会出现传输或分页。Resizable BAR/SAM 允许 CPU 映射更大的显存地址窗口，减少小窗口重映射；它不是把 PCIe 变成显存总线，也不保证每个游戏提升。[A054]

显存容量压力呈离散而非平滑：容量内可能稳定，超过后突然换页和卡顿。平均 FPS 不能揭示，需看低分位帧时间、PCIe 流量和显存驻留。纹理质量、光追加速结构、帧生成缓冲和创作应用都占显存。8GB、16GB、24GB 的价值随分辨率和生命周期变化。

### 7. HBM：把带宽搬到封装旁

HBM 将多层 DRAM 通过 TSV 堆叠，并在硅中介层或先进封装上以极宽接口连接 GPU，降低每比特能耗并提供 TB/s 带宽。[A034] 它适合 Instinct 的矩阵和 HPC，却成本高、封装复杂、容量受堆叠数量限制，不适合普通可更换 DIMM。

MI300X 的 192GB HBM3 允许较大模型留在单 GPU，MI350 系列把容量提高到 288GB HBM3E、带宽最高 8TB/s。[A034][A035] 这些是 GPU 本地物理内存，仍需主机、存储和网络供数。模型载入与 checkpoint 可能由 PCIe/网络限制，稳态矩阵才更接近 HBM 带宽。

HBM 带宽是多个堆栈并行的总峰值。访问不合并、Bank 冲突、缓存行为和计算占用会降低有效值。大模型推理常受权重读取带宽，训练还受矩阵算力和通信。不能用 8TB/s 直接除模型大小得到实际 token/s，因为每 token 的 KV cache、激活、调度和并发不同。

### 8. 统一地址、共享物理内存与一致性

“统一内存”可能指三件事：CPU/GPU 共享物理 LPDDR；不同内存具有统一虚拟地址；运行时自动迁移页面。三者不能互换。Ryzen APU 的 CPU 与 iGPU 使用同一物理 DRAM，但设备缓存、一致性和分配策略仍存在；独显可有统一虚拟地址，物理页仍在 GDDR 或主存。

零拷贝也常被误用。共享缓冲可以避免完整显式复制，但 CPU 写后 GPU 读仍需同步、缓存维护和格式转换；若设备访问远端页，带宽/延迟可能更差。正确优化是测量数据生命周期，尽量让生产者和消费者在合适设备上连续处理，而不是只删除 `memcpy`。

Ryzen AI Max 的共享内存让 CPU 前处理、GPU 推理和显示减少容量碎片，并能把较大模型交给 iGPU；但系统内存一旦被 GPU 占用，其他程序可用空间减少。操作系统可能压缩或换页，引发尾延迟。部署应留出系统、运行时和 KV cache 余量。

### 9. Infinity Fabric：CCD、IOD 与数据位置

Infinity Fabric 是可扩展连接框架，承载数据、控制和一致性；具体实现随 Ryzen、EPYC、Instinct 改变。[A030] 桌面 Ryzen 中 CCD 通过 Fabric 到 IOD/内存，跨 CCD 也经过封装路径。延迟由核心、缓存命中、Fabric 时钟、IOD 和 DRAM 共同组成。

早期 Ryzen 常讨论 FCLK、UCLK 与 MCLK 比例；DDR5 平台的最佳比例和上限已不同。过度追求频率会导致控制器分频或训练失败，纸面带宽提高但延迟变差。调优需同时看读写复制、随机延迟、真实应用与错误计数。

EPYC 有更多 CCD 和内存通道。内存页面应靠近使用它的核心，NIC/GPU DMA 也应靠近相应 NUMA。Linux `numactl`、hwloc 和性能计数器能验证；虚拟化需要把 vCPU、内存和直通设备对齐。错误亲和是多插槽扩展失败的常见原因。

### 10. PCIe：端到端路径而非版本标签

PCIe 是分层点到点互连，代际提高每 lane 速率。[A052] 一条 x16 链路的峰值是方向性总和前的单向数字，实际要扣编码和事务层开销。设备可能物理 x16、电气 x8；主板分叉后多个插槽共享 CPU 通道；芯片组设备还共享上行链路。

桌面显卡通常不持续填满高代 PCIe，因为工作集留在显存；显存溢出、GPU 计算频繁主机交换、外接 GPU 才更敏感。NVMe 顺序读峰值也不代表模型加载全程：文件系统、解压、CPU、页缓存和小文件会限制。

PCIe 拓扑决定 P2P。两张 GPU 在同一根复合体可能直接 DMA，跨 CPU/芯片组或受 ACS/IOMMU 时可能绕主存。工作站多 GPU 应用必须验证实际 P2P 和带宽，不因 Threadripper 有很多 lane 就假定软件启用。

PCIe 原子和 IOMMU 是 ROCm/RDMA 等功能的基础。Windows ROCm 文档指出现代 Zen 与 Intel Haswell 后 CPU 支持所需 PCIe atomics，但具体主板/设备/OS 仍要在支持矩阵内。[A039] BIOS 开关、Above 4G Decoding 和 Resizable BAR也会影响设备映射。

### 11. CXL：协议能力不等于产品可用性

CXL 建立在 PCIe 物理层之上，定义 CXL.io、CXL.cache 和 CXL.mem，用于设备发现、缓存一致与内存访问。[A053] 它可以让加速器一致访问主机内存，或让服务器扩展/池化内存。延迟和带宽仍受链路、交换和介质限制。

EPYC 平台支持某代 CXL 只证明根端口协议能力。真实部署还需要 CXL 设备、BIOS 枚举、ACPI 表、OS 内核、内存分层和 RAS。Type-3 内存扩展可能增加容量，却比本地 DDR 更远；数据库、内存缓存和 AI offload 需要页面放置策略。

消费 AM5 的 PCIe 5.0 不等于支持 CXL 内存设备。主板固件、CPU 产品合同和 OS 均可能未开放。不要因为物理插槽相同就插入未知 CXL 卡。CXL 也不是 Ryzen AI Max 的共享内存机制，后者在 SoC 内集成控制器和 LPDDR。

### 12. Smart Access Memory / Resizable BAR

传统 PCIe BAR 映射显存窗口较小，CPU 访问大资源需重映射。Resizable BAR 允许协商较大窗口，AMD 将匹配平台功能称为 Smart Access Memory。[A054] 它可减少驱动开销、改善某些资源上传，收益由游戏引擎和显存管理决定。

启用需要 CPU、主板 BIOS、UEFI、显卡 VBIOS 和驱动共同支持，通常还要求 Above 4G Decoding。CSM/旧式启动可能冲突。系统显示已启用后仍应逐游戏测试，有些负载提升小或出现回归，驱动会维护配置。

SAM 不是 GPU 直接以 DDR 带宽访问 CPU 内存，也不是把系统 RAM 添加到显存。CPU 访问显存仍经 PCIe；GPU 超出显存仍可能换页。它优化地址窗口，不改变物理链路。

### 13. USB4、外接 GPU 与扩展坞

移动 Ryzen 平台可实现 USB4，但具体端口是否支持 40Gb/s、PCIe tunneling、DisplayPort、充电和唤醒由 OEM 决定。一个 Type-C 形状不能证明全部协议。扩展坞多设备共享上行，显示、网卡、SSD 同时使用会争带宽。

外接 GPU 经 USB4/Thunderbolt 隧穿 PCIe，链路远小于桌面 x16，并有协议与往返延迟。外屏直连 eGPU 通常比把画面回传内屏更好。高端显卡更容易被链路限制，实际收益与游戏数据交换模式有关。

Ryzen AI Max mini PC 是否值得外接显卡取决于任务：游戏可获独显着色与显存，CPU—GPU 传输受限；AI 若模型常驻显存影响较小，数据预处理/多 GPU 通信较差。还要验证电源、驱动热插拔与睡眠恢复。

### 14. GPU Direct 与数据中心 DMA

数据中心希望 NIC、存储和 GPU 直接 DMA，减少 CPU 拷贝与内存往返。实现需要设备 P2P、IOMMU/ACS、驱动、注册内存和通信库共同支持。所谓“直接”仍有控制路径、队列和同步，不是 CPU 完全不参与。

Instinct 集群中，单节点 GPU 间高速链路负责高频集合，跨节点经过 NIC 和交换。若 NIC 与 GPU 跨 NUMA，数据可能穿过 CPU I/O 链；若消息太小，协议延迟主导；若拥塞，端口峰值无意义。性能诊断应从拓扑、单链路、双向、多流、集合到应用逐层测。

Pensando AI NIC/DPU 的价值是把包处理、拥塞和安全能力移近网络。[A063] 但 GPUDirect 类路径是否支持要查整个平台兼容清单，不能从网卡速率推断。固件和 OFED/驱动版本也需冻结。

### 15. Infinity Cache 与图形数据路径

Infinity Cache 是 GPU 片上大缓存，用于提高数据复用、降低 GDDR 流量。它与 CPU L3 都利用局部性，但图形访问模式和缓存结构不同。分辨率提高会增加像素、渲染目标和光追数据，缓存命中可能下降；无损压缩可减少外部带宽，但依赖数据可压缩性。

RDNA 3 的 MCD 将缓存和内存控制器分布在 chiplet，GCD 通过封装互连访问。互连需要提供远高于 PCIe 的内部带宽，同时控制功耗。外部看到的是统一显存地址，物理上却有多个控制器和缓存切片；驱动负责映射和负载均衡。

APU iGPU 通常没有同级独立大 Infinity Cache，依赖共享 L3/系统内存设计和较低功耗。比较 CU 数时必须把内存系统放入：相同 20 CU 在 128-bit LPDDR 与 40 CU 在 256-bit LPDDR 的供数不同；离散卡又有 GDDR 与缓存。

### 16. 存储到 GPU 的完整链路

本地模型加载路径是 NVMe → PCIe/芯片组 → 主机内存/页缓存 → GPU 内存或共享内存。文件可能经过解压、反序列化和权重转换，CPU 与内存会成为限制。APU 共享内存可以省去独显上传，但 SSD 数据仍要进入 DRAM；若内存不足，系统可能边读边换页。

游戏资产路径还包括 DirectStorage 类 API、GPU 解压和着色器。Xbox 固定平台能共同设计 NVMe、硬件解压和内存；PC 必须适配多种 SSD、CPU、GPU和驱动。[A074] 因此同样 SSD 标称速度，游戏加载差异可能很小或很大。

数据中心 checkpoint 路径可能经过并行文件系统和网络。训练暂停时间由每节点数据量、写入并发、文件系统元数据与压缩决定。HBM 更大意味着 checkpoint 可能更大，不能只扩 GPU 而不扩存储。

### 17. 内存容量规划

容量预算从不可压缩部分开始：操作系统与后台、应用代码、数据、模型权重、优化器状态、激活、KV cache、图形资产和安全余量。推理量化降低权重，但 KV cache 随上下文、层数、头维度和并发增长；训练优化器状态可为权重多倍。宣传“可运行某参数模型”若未注明量化、上下文和速度，意义有限。

APU 应为系统保留至少足够内存，避免把全部容量分给 GPU。独显系统应同时配置主存和显存；AMD Radeon ROCm 指南建议复杂 AI/ML 工作负载考虑 64GB 主存与 24GB 显存，并指出最低需求随工作负载变化。[A039] 指南不是每个模型的保证，而是部署起点。

服务器要按 NUMA 分配 DIMM，填满通道优先于只追求单条大容量。混插频率、rank、容量可能降速或不受支持。CXL 内存可做容量层，但软件需识别慢层。数据库与内存缓存还需要持久化和恢复预算。

### 18. 持续性能与能效测量

能效应以完成任务的焦耳衡量，不只看瞬时瓦数。低功耗机器运行更久可能消耗更多总能量，高功耗机器快速完成后进入空闲可能更省；也可能因电压频率曲线相反。测量需含墙上功率、空闲基线、任务时间和 PSU 效率。

内存能耗在大模型和多核服务器占比显著。更多缓存可能减少 DRAM 能量，HBM 每比特能效较好，LPDDR 适合待机；但更大容量本身有静态功耗。工作负载优化数据复用常比单纯降频有效。

热浸测试至少 30–60 分钟，记录 CPU/GPU/NPU 时钟、封装功率、内存带宽、温度、风扇和错误。掌机还记录电池放电与皮肤温度，机架记录冷却水与泵。峰值之后的稳定平台才是生产能力。

### 19. 故障与一致性

内存错误可能来自颗粒、信号、超频、温度或电源。ECC 能检测/纠正一定错误，不覆盖软件 bug、链路所有错误或控制器故障。消费主板即便支持 ECC UDIMM，也可能不报告计数；工作站/服务器有更完整 RAS 和日志。

PCIe 链路降级、AER 错误和重训练会造成性能波动。排查顺序应为恢复默认、检查插槽速率/宽度、固件、线材/转接、温度和设备日志。把每次 GPU reset 归因于驱动会漏掉电源与链路。

chiplet 系统需要隔离和恢复。AMD chiplet 白皮书把测试、互操作与故障隔离列为生态要求。[A030] 多裸片增加连接点，但也可通过分仓和冗余提高良率/可服务性；可靠性不能凭“单片更简单”或“chiplet 更先进”先验判断。

### 20. 端到端诊断清单

第一步画拓扑：CPU 核/CCD、内存通道、GPU、NIC、NVMe、根端口和交换。第二步验证配置：BIOS、通道填充、PCIe 宽度、Resizable BAR、IOMMU 和驱动。第三步测单层：缓存/DRAM、PCIe、显存/HBM、网络、存储。第四步测组合：CPU+GPU、GPU+NIC、存储+GPU。第五步才跑应用并用计数器解释。

若算力利用率低，先看等待的是内存、通信还是串行 CPU；若带宽低，看访问是否连续和并发够不够；若延迟抖动，看温度、分页、NUMA、后台和中断；若容量报错，区分物理不足、碎片、运行时保留和地址空间。端到端分析的价值在于把“AMD 驱动慢”“内存不够快”这种模糊结论拆成可证伪问题。

---

## 软件与 AI 生态：ROCm、HIP、Ryzen AI Software、Windows 与 Linux

### 1. 硬件能力只有被软件命中才存在

AMD 平台同时有 CPU、RDNA GPU、CDNA GPU 和 XDNA NPU，开发者面对的不是一条统一执行路径。CPU 由成熟 x86 编译器和操作系统调度；Radeon 图形走 DirectX/Vulkan/OpenGL；GPU 计算走 ROCm/HIP、OpenCL 或框架后端；NPU 需要 Ryzen AI Software、ONNX 图编译与系统 API。一个模型可能把不同节点分到三种设备。

软件生态分析应分五层：内核驱动与固件；用户态运行时；数学/媒体/通信库；框架和模型；应用与运维。安装成功只证明前两层部分工作，跑出一次结果不证明数值正确、性能稳定或升级可维护。支持矩阵是产品合同，比论坛成功截图更可靠。

### 2. ROCm 的组件边界

ROCm 是开源 GPU 计算平台总称，包含 amdgpu 内核接口、KFD/HSA 运行时、HIP 编译与运行时、rocBLAS/hipBLASLt、MIOpen、RCCL、rocFFT、rocRAND、分析器和调试器等。[A037][A043] 不同发行版本并非所有组件同号，容器还会复用宿主内核驱动。

Instinct 是 ROCm 的首要验证对象。官方兼容矩阵按 MI 系列、Linux 发行版、内核、虚拟化和 ROCm 版本列支持。[A038] 生产部署应从矩阵选择组合，而不是先装最新包再找能否运行。操作系统小版本升级可能更换内核，影响 DKMS/驱动；容器不能绕过宿主驱动不兼容。

Radeon/Ryzen 有独立文档与矩阵，因为显示和计算共用 GPU，Windows/Linux 驱动模型也不同。[A039] 快照日 Windows 表将设备分为 Runtime 与 HIP SDK 支持，部分数学库仅 Linux 提供。某张卡能运行 HIP kernel，不等于 PyTorch、FlashAttention、量化库或专业应用都获得官方支持。

版本冻结建议使用机器可读清单：GPU PCI ID/gfx target、VBIOS、主板 BIOS、内核、amdgpu、ROCm、Python、框架、容器镜像、模型 commit 与编译参数。升级时保留旧环境，先跑单元正确性、确定性、性能和长时稳定，再切生产。

### 3. HIP：源代码可移植性的实际含义

HIP 是 C++ 运行时 API 与 kernel 语言，可在 AMD 平台由 HIP-Clang 编译，也可面向 NVIDIA 后端。[A040] 它提供与 CUDA 相近的线程、块、内存和流概念，使大量 kernel 可机械转换。hipify 工具能替换 API 名，但不能自动处理所有语义和性能。

迁移难点包括：CUDA 专有库、内联 PTX、warp=32 假设、未定义竞争、不同原子/同步、共享内存大小、寄存器压力、编译器扩展和驱动行为。AMD wavefront 可能是 32 或 64，算法若硬编码掩码会错误。正确做法是用 HIP 的可移植原语和库，运行跨后端测试。

性能可移植比功能可移植更难。一个 tile 尺寸在 NVIDIA SM 上最佳，可能在 AMD CU 上因 wave、LDS、寄存器和缓存不同而降低占用。库调用如 GEMM 应优先交给 hipBLASLt，而不是维护自写内核；自定义热点再按架构调优并保留通用路径。

二进制插件是更硬的边界。只发布 CUDA cubin 的 Python wheel 不能由 HIP 直接运行，必须有源码和构建系统。选择 AI 工具前查 wheel 是否包含 ROCm 版本、支持哪些 gfx target；“Python 项目”不表示底层没有 CUDA-only 扩展。

### 4. 框架：PyTorch、TensorFlow 与上层生态

PyTorch ROCm 构建尽量保持相同 Python API，设备字符串有时仍显示 `cuda` 以兼容现有代码。这只是 API 选择，不代表实际使用 NVIDIA。诊断需查看 PyTorch 版本、ROCm 构建标记和设备名称，而不是字符串。

框架发布节奏与 ROCm 不完全同步。官方容器通常锁定经过验证的组合，社区 nightly 可能支持新 GPU但回归风险更高。生产用户选择稳定镜像，消费新卡用户可能必须等待正式 wheel或用 TheRock/源码构建；应清楚标为社区可用而非官方支持。

模型生态的瓶颈常在自定义 attention、量化和推理引擎。主干算子由框架支持不保证扩展支持 AMD。部署前列出模型依赖的 Triton、FlashAttention、bitsandbytes、vLLM、llama.cpp 等具体后端，逐一核验版本。回退到通用 kernel 可能正确但速度显著下降。

数值正确性必须独立验证。FP16/BF16/FP8 舍入、融合顺序和原子非确定性会导致差异。设置容差前先比较任务指标，不以“能生成文字”代替 logits 或训练收敛验证。低精度峰值只有在模型缩放和校准保持质量时才有价值。

### 5. RCCL 与多 GPU

RCCL 提供 all-reduce、all-gather、reduce-scatter、broadcast 等集合通信，框架分布式训练通过后端调用。性能取决于 GPU 拓扑、PCIe/Infinity Fabric、NIC、消息大小、算法和 rank 放置。单 GPU kernel 快不能保证多 GPU 扩展。

节点内先验证 P2P 矩阵和每链路单向/双向带宽，再跑集合基准；跨节点先测 NIC 与 GPU 亲和、RDMA 和网络拥塞。总线带宽与算法带宽口径不同，不能直接与端口速率比较。小消息由启动延迟主导，大消息才接近带宽。

环境变量可用于诊断和选算法，但随机复制“性能魔法参数”会掩盖拓扑或驱动问题。每项变量记录原因、默认值、适用版本和回滚。超时应先查某 rank 崩溃、网络丢包或 collective 顺序不一致，而不是无限增大 timeout。

### 6. Windows GPU 计算

Windows 消费生态的优势是应用和驱动整合，限制是 ROCm/HIP 组件覆盖较 Linux 窄。快照日官方 Windows 表明确 RX 9000、部分 RX 7000、Radeon PRO 和 Ryzen AI Max 支持层级，也明确一些 RDNA 2 卡只有 Runtime 而没有完整 HIP SDK。[A039] 用户必须以当期表为准。

DirectML 是微软的低层机器学习 API，运行在 DirectX 12 设备上，允许 Windows 应用跨供应商 GPU。[A044] 它降低厂商专用部署门槛，却不保证所有模型都有同等优化。Windows ML/ONNX Runtime 执行提供者可能选择 DirectML、NPU 或 CPU，应用需暴露设备和回退信息。

WSL 提供 Linux 用户态体验，但 GPU 驱动通过 Windows 主机映射，不等于原生 Linux。文件系统跨边界、网络、容器和内核功能有差异。重训练或高 I/O 任务应比较原生 Linux，交互开发可用 WSL 简化工具。支持表必须点名 WSL 和 GPU。

Adrenalin 驱动同时服务显示、游戏、媒体和部分计算。[A049] 生产 AI 与每日游戏共用机器时，驱动升级可能造成冲突。可为稳定任务保留容器/环境和旧安装包，更新前导出配置。Windows Update 也可能替换驱动，应纳入变更控制。

### 7. Linux 图形与计算栈

Linux amdgpu 是内核驱动，文档覆盖显示、内存管理、调度、RAS 和调试接口。[A050] 用户态图形通常使用 Mesa，ROCm 使用自己的计算组件；发行版内核、Mesa 与 ROCm 可来自不同渠道。版本错配是黑屏、设备未枚举或计算失败的重要来源。

消费桌面优先发行版集成图形栈，除非 ROCm 文档要求特定驱动。随意混装 AMDGPU-PRO、发行版 Mesa 和 ROCm 包可能覆盖库。安装前用包管理器和容器规划，失败后不要用复制单个 `.so` 的方式永久修补。

权限包括 `/dev/kfd`、`render` 组和容器设备映射。把容器设为 privileged 虽能绕过问题，却扩大攻击面；应只映射需要的设备和组。多用户工作站还要限制性能计数器和显存访问，防止侧信道或数据残留。

Linux 新硬件 enablement 可能依赖新内核和固件。XDNA 驱动在 AMD GitHub 开源，发行版合入和用户态支持有时间差。[A045] 自己编译驱动适合开发验证，不应被描述为普通用户正式可用。

### 8. Ryzen AI Software：NPU 部署流水线

Ryzen AI Software 提供模型量化、编译、运行时和示例，用于 XDNA NPU。[A041] 典型流程是从 ONNX 模型开始，规范输入形状，量化到支持精度，编译为设备图，在运行时提交张量。每一步都可能因算子、动态形状或精度失败。

图划分是性能核心。NPU 支持的连续子图越大，设备切换越少；不支持节点回退 CPU/GPU会带来同步和内存转换。开发者应读取编译报告，列出落在 NPU 的节点比例，并用端到端时延而非子图 kernel 时间评价。

量化需要代表数据校准。图像、语音和语言模型对量化误差敏感度不同，单个样例正确不能证明质量。应保留 FP32/BF16 基线、任务指标和每层误差，必要时混合精度。NPU TOPS 的价值要乘以算子覆盖和利用率。

官方 RyzenAI-SW 仓库包含示例和发布信息，可用于锁定代码与 issue。[A042] GitHub main 分支不是稳定发布，生产应使用 tag/commit。示例支持的模型不代表任意变体，输入大小、opset 和权重仍需匹配。

### 9. CPU、GPU、NPU 的任务分配

CPU 适合控制流、低批量、tokenization、数据库和不规则算子；GPU 适合大并行、图形、训练与较大模型；NPU 适合受支持、持续、低功耗推理。最佳设备由端到端能量和时延决定，不由 TOPS 排名。

小模型一次推理可能在 CPU 更快，因为无需编译和设备提交；摄像头持续滤镜适合 NPU，因为长时间低功耗；大语言模型权重读取适合宽内存 GPU；训练通常用 GPU。混合执行要减少跨设备边界，让每段包含足够工作摊销。

调度还要考虑用户体验。前台游戏占 GPU 时，把助手推理放 NPU可避免帧率争用；视频会议占 NPU 时，LLM 可能转 GPU/CPU。系统应提供优先级、取消和功耗模式，不以所有单元同时 100% 为目标。

### 10. 本地大模型：容量、后端与可复现

本地 LLM 需要权重容量、KV cache、内存带宽和受支持 kernel。模型参数量乘每参数字节只估权重，不含量化表、运行时、缓存和系统。长上下文与并发会让 KV cache 成为主导。Ryzen AI Max 的大共享内存可容纳更大模型，性能仍由 LPDDR 带宽和后端决定。

llama.cpp 一类项目可能通过 HIP、Vulkan 或 CPU运行。不同后端的算子覆盖、量化格式和图卸载不同；报告 token/s 必须记录模型精确文件、量化、上下文、batch、提示长度、生成长度、后端 commit、线程和功耗。首 token 延迟与稳态生成速率分开。

“70B 可运行”不是产品结论：极低量化可能影响质量，全模型装入但只生成 1 token/s 也未必实用。对话助手应同时测首 token、20–50 token/s 目标、长上下文和系统响应；编码助手还要测 prompt ingest 与工具调用。

隐私要看应用数据流。本地权重可能仍调用在线认证、更新、遥测或搜索。敏感部署用防火墙/抓包验证，检查模型许可证和缓存位置。NPU/GPU 不自动提供机密计算。

### 11. 图形软件：DirectX、Vulkan 与 GPUOpen

Windows 游戏主要通过 DirectX 12，跨平台和 Linux/Android 使用 Vulkan，旧应用仍有 DX11/OpenGL。驱动要编译着色器、管理显存和调度队列；首次着色器编译可造成卡顿，游戏或驱动缓存能改善。评测应区分首次运行和热缓存。

GPUOpen 汇集 AMD 的开放图形 SDK、性能工具和文章。[A046] Radeon GPU Profiler、GPU Detective 等可分析波前、缓存、崩溃与帧。开放源码利于跨 GPU 采用，但开发者整合和测试决定游戏是否交付。

FidelityFX SDK 提供 CAS、SSSR、降噪、超分和其他组件。[A046] 组件是源代码/库，不是驱动全局自动优化。游戏选择版本、输入缓冲和 UI 处理；不同实现质量会不同。FSR 支持多厂商是覆盖优势，机器学习版本可能要求特定硬件路径。

FSR 的渲染比例、帧生成和延迟要分开。超分从低内部分辨率重建，帧生成插入中间帧，不提高游戏逻辑或输入采样；基础帧率过低时插帧画面更流畅但交互仍慢。比较 DLSS/XeSS 必须同输出分辨率、质量档、基础帧和锐化。[A048]

### 12. 媒体、创作与应用认证

Radeon/Ryzen 媒体引擎支持 H.264/H.265/AV1 等具体编解码路径，但编码质量、色深和应用接口随代际。直播测试要用相同码率、预设和场景比较画质；导出要确认应用真正调用硬件，而非 CPU fallback。

DaVinci Resolve、Adobe、Blender、CAD 等有各自后端和插件。硬件支持 OpenCL/HIP 不代表应用启用；专业版本、驱动认证和 OS 也会改变。选购前以软件供应商支持页和实际项目验证，避免仅依 AMD 峰值。

Radeon PRO 的认证驱动减少特定版本组合的风险，RX 驱动更新更快服务游戏。创作者可在 RX 获得高性价比，但生产稳定性要自行承担。混合使用显示和长计算时，Windows TDR、桌面响应与显存抢占需测试。

### 13. 编译、调试和性能分析

CPU 侧使用 GCC/Clang/MSVC 与 perf、uProf、VTune 类工具；GPU 侧用 rocprofiler、rocTracer、Omniperf/rocprof、Radeon GPU Profiler；NPU 用编译报告和运行时 trace。工具名称随版本变化，结论应保留版本。

性能优化流程是先建立正确基线，再定位热点，再一次改变一个变量。GPU 看 kernel 时间、占用、wave、LDS、寄存器、缓存和传输；CPU 看 IPC、分支、缓存、NUMA；NPU看图覆盖与设备边界。只看利用率百分比无法区分满算与等待。

编译缓存和 JIT 会污染首轮。报告冷启动与热运行；模型编译时间与部署推理分开。动态形状可能产生多个编译变体，占磁盘与启动时间。生产服务应预热并设缓存生命周期。

### 14. 驱动可靠性与回归管理

驱动故障可能来自内核、用户态、固件、应用、超频、电源或硬件。排查先恢复默认时钟和内存，检查系统日志/AER，最小化应用，再更换已知稳定驱动。清洁安装是工具，不是对所有问题的解释。

游戏驱动按新作快速更新，ROCm 稳定版按验证周期发布，OEM 笔记本又可能要求定制包。用户在 AMD 通用驱动与 OEM 驱动间选择时，应保留恢复介质；混合显卡/MUX、电源和热键可能依赖 OEM 组件。

回归测试集合应覆盖设备枚举、显示/休眠、典型游戏、媒体、AI 正确性、长时计算和多 GPU。只跑一个 benchmark 无法发现待机功耗或视频问题。记录 known-good 状态，升级失败能回滚。

### 15. 安全与隔离

GPU/NPU 可处理敏感提示、图像和模型。多用户环境需要设备权限、容器隔离、显存清理、固件签名和 IOMMU。把 `/dev/kfd` 暴露给容器相当于授予设备访问，应限制可信工作负载。

AMD PRO、EPYC 的安全功能包括安全启动、内存加密和虚拟化能力，但具体功能依 SKU/平台。[A078] 加密会有性能和运维取舍，密钥、证明和固件更新仍需平台方案。消费 Ryzen 的安全标签不能直接替代企业威胁模型。

AI 供应链包括模型文件、Python 包、容器和编译器。校验哈希、固定依赖、扫描镜像和最小权限比“完全离线”口号更重要。社区 wheel 可能包含预编译本机代码，应从可信发布源获取。

### 16. 开源战略的优势与边界

ROCm、GPUOpen 和 XDNA 驱动开放，使开发者能查看实现、提交补丁并支持 Linux。对云和研究客户，开放编译器和内核降低黑盒风险；对游戏开发者，跨厂商 SDK 扩大采用。

开源不等于成熟、完整或无需支持。issue 数、PR 合并速度、文档一致和二进制发行才决定体验。某组件开源而固件或验证闭源仍可形成依赖。企业需要 SLA 和责任边界，不能把社区热情当服务合同。

AMD 的挑战是减少多入口碎片：Instinct ROCm 文档、Radeon/Ryzen ROCm、Ryzen AI Software、GPUOpen、Vitis/Vivado 分属不同受众。统一安装、版本语义、硬件发现和诊断能显著降低采用成本，比再增加一个峰值指标更有价值。

### 17. 与 CUDA、oneAPI、Metal/MLX 和 Qualcomm 栈比较

NVIDIA CUDA 的优势是长期库、工具、应用和人才，硬件/软件垂直优化；代价是供应商锁定。AMD HIP 试图降低 CUDA 源迁移并以开放 ROCm竞争，硬件容量常有吸引力，但长尾扩展和 Windows 覆盖需逐项查。[A072]

Intel oneAPI/SYCL 强调跨 CPU/GPU/加速器，Core Ultra 也组合 CPU、GPU、NPU。[A069] AMD 与 Intel 都面对 x86 OEM 多样性和驱动矩阵；差异应落到具体应用、GPU 规模与软件版本，不能用厂商口号。

Apple 控制 M 系列 SoC、统一内存、macOS、Metal/Core ML/MLX 与整机，端到端一致性强，但内存购买后不可升级、平台封闭。[A070] Ryzen AI Max 在共享内存容量和 x86 软件上形成竞争，仍依赖多 OEM 与 Windows/Linux。

Qualcomm Snapdragon X 以 Arm、集成 NPU 和 Windows on Arm 争取能效。[A071] 原生 Arm 应用可表现良好，x86 转译和外设驱动是变量。AMD 的 x86 兼容降低迁移，却要在待机和整机协同上竞争。

### 18. 端侧 AI 验收方法

第一阶段验证可用性：驱动识别设备，目标模型能编译，所有算子有明确放置。第二阶段验证正确性：与 CPU 高精度基线比较任务指标。第三阶段验证性能：冷启动、首结果、稳态吞吐、长上下文和并发。第四阶段验证能效与体验：墙上/电池能量、温度、噪声、前台响应。

测试矩阵至少包含 CPU、GPU、NPU 三条路径以及自动调度。自动路径可能因版本变化，记录日志。模型名称必须带版本/哈希、量化、上下文和输入；硬件带 SKU、RAM、功耗；软件带 OS、BIOS、驱动和运行时。

若 NPU 只覆盖部分图，报告覆盖比例和 CPU fallback 时间；若 GPU 借用共享内存，报告系统剩余和带宽；若设备过热，报告稳定段而非首轮。没有这些条件的 TOPS/倍数只能放在产品规格，不进入体验结论。

### 19. 面向不同用户的软件建议

普通消费者优先 OEM 稳定驱动和应用功能，不为 NPU 手动安装开发栈。游戏玩家维护一个稳定 Adrenalin 版本，更新前查游戏修复；创作者查应用认证和媒体路径；本地 AI 爱好者选择官方矩阵点名的 GPU/APU，使用固定容器或 Python 环境。

开发者优先框架标准 API 和库，隔离厂商专用优化；CI 至少跑 CPU 与一条 AMD GPU/NPU 路径。企业冻结全栈、维护 SBOM 和回滚，使用官方支持；集群运维把通信、固件与容器一起升级。

真正的“生态成熟”不是 demo 数量，而是从安装、正确、性能、诊断、升级到恢复都可预测。AMD 已有覆盖 CPU、GPU、NPU 的主要组件，下一阶段价值取决于能否让支持矩阵更宽、更一致、更容易被应用默认选择。

---

## 市场、业务模式与竞争：AMD 如何在端侧与数据中心之间配置资源

### 1. 市场问题的正确定义

“AMD 市场有多大”不是可操作问题，因为 AMD 同时进入 PC CPU、独显、游戏机 SoC、服务器 CPU、AI 加速器、嵌入式、自适应计算和 DPU。每个市场的客户、价格、更新频率和供应约束不同。更有用的问题是：在某个时间、地区和任务中，有多少客户愿意为一个满足软件、容量、能效和生命周期条件的平台付费。

本报告不采用缺少样本与方法的第三方 TAM。市场事实以 10-K 分部收入、产品上市和官方客户披露为锚；竞争分析用技术合同和切换成本，不把厂商自选跑分当份额预测。公开收入是出货量×价格×组合的结果，不能只由单位性能解释。

### 2. 2025 财务快照

2025 年 AMD 总营收 346.39 亿美元，同比增长 34%；数据中心 166.35 亿，Client and Gaming 145.50 亿，Embedded 34.54 亿。[A001] 数据中心已成为最大分部，但消费/游戏仍接近其规模，证明 AMD 不是单一 AI 加速器公司。

Client 收入 106.40 亿、同比 51%，AMD 披露处理器单位出货增加 31%、ASP 增加 15%。这两个比例不能简单相乘复算收入，因为产品组合、时间和四舍五入；它们说明增长既来自数量也来自高端组合。Gaming 39.10 亿、同比 51%，由半定制与 Radeon 推动。

Embedded 34.54 亿、同比下降 3%，经营利润仍为 12.43 亿。嵌入式订单受客户库存和长周期影响，单年下降不代表设计赢单消失。数据中心经营利润增长慢于收入，部分因 MI308 出口管制相关约 4.4 亿美元库存/费用及研发投入。

研发 80.91 亿约占营收较大比例，反映 CPU、GPU、软件、网络和并购整合的资本强度。AMD 需要从多个产品回收共同 IP；若 AI 收益不足，投入压力会传给利润；若成功，ROCm、封装和网络也可反哺消费端。

### 3. PC CPU：与 Intel 的平台竞争

AMD 与 Intel 竞争不只在核心性能。桌面用户比较插槽寿命、主板价格、内存、I/O、空闲功耗、游戏和升级；移动 OEM 比较供货、参考设计、待机、驱动、企业管理和品牌补贴；商用客户看镜像、可管理性与多年采购。

AMD 的强项是 Zen chiplet 使桌面从 6 到 16 核复用，X3D 形成游戏差异，AM5 可升级性降低未来 CPU 切换成本。弱项是 OEM 设计数量和体验一致性受合作伙伴影响，移动待机与平台固件不能只靠核心解决。Intel Core Ultra 同样集成 CPU、GPU、NPU 并拥有深厚 OEM/企业渠道。[A069]

桌面份额竞争常被评测榜单放大。消费者实际预算含主板、DDR5、散热和显卡；一个 CPU 快 10% 但平台贵 30% 不一定赢。AMD 延长插槽支持能提高主板投资价值，也要求 BIOS 容纳更多代和主板厂持续测试。旧板支持新 CPU 若功能或内存训练退步，会损害承诺。

移动设计赢单比零售更慢。处理器发布后 OEM 需完成主板、散热、认证和渠道，消费者可能数月后才看到。Ryzen AI 400 进入桌面/工作站扩大形态，但每台设备的 NPU 功能仍依 OEM/ISV。[A081] 市场判断应统计可买配置、价格和地区，不只统计“宣布合作伙伴”。

### 4. AI PC：AMD、Intel、Qualcomm 与 Apple

AI PC 的供应方都把 CPU、GPU、NPU 放入 SoC，差异在 ISA、内存、软件和整机控制。NPU TOPS 是门槛，不是差异的充分条件。真正决定采用的是 Windows/应用默认执行提供者、模型覆盖、续航和不破坏既有工作流。

AMD 的 x86 兼容和 Radeon iGPU 利于传统 Windows 应用，XDNA 2 达到 Copilot+ 级 NPU，Ryzen AI Max 用宽共享内存服务本地模型。风险是产品命名复杂、OEM 配置差异和 Ryzen AI Software 仍在扩展。官方明确 AI 功能依赖 OEM/ISV enablement，市场预测必须把应用启用率纳入。[A004]

Intel 的优势是庞大 PC 渠道、企业平台和软件合作；不同 Core Ultra 代际的 GPU/NPU 与内存不同，不能用一个标签比较。Qualcomm Snapdragon X Elite 的 Arm 能效和 NPU 是卖点，但 x86 转译、驱动与企业应用是切换成本。[A071] 如果应用原生 Arm，差距可能小；若依赖旧插件，兼容风险提高。

Apple M4 由 Apple 同时控制芯片、统一内存、macOS、开发工具和整机，能快速让系统功能使用 Neural Engine。[A070] 它的封闭垂直整合带来一致性，代价是内存/存储不可升级和非 macOS 工作流。Ryzen AI Max 的机会是给需要 x86、Windows/Linux 和大共享内存的人替代，不是复制 Apple 全栈。

AI PC 需求应按工作细分：视频会议和系统特效需要低功耗持续 NPU；创意生成需要 GPU和应用；企业助手还需数据权限与模型；开发者需要内存和框架。若关键功能仍在云端，NPU 不直接产生付费意愿。市场价值来自可重复的本地任务，而不是“AI”贴纸。

### 5. 消费 GPU：与 NVIDIA 的生态竞争

Radeon 与 GeForce 竞争光栅、光追、超分/帧生成、媒体、显存、功耗、驱动和软件。NVIDIA GeForce RTX 50 系列结合 Blackwell、DLSS 和 CUDA 生态，是直接参照。[A072] AMD RX 9000 用 RDNA 4 和 FSR 4改进光追/机器学习重建，并常以显存或价格竞争。

GPU 购买高度依赖应用组合。纯游戏看具体游戏和区域价格；直播看编码；Blender/AI 看后端；专业软件看认证。Radeon 硬件理论性能若应用 CUDA-only 就无法兑现；GeForce 显存较小时，本地大模型又可能因容量受限。不存在脱离软件的统一赢家。

FSR 的跨厂商开放性可扩大游戏集成，但最新机器学习模式的硬件覆盖和质量需逐游戏验证。DLSS 的垂直工具和开发者采用形成网络效应。AMD 可通过 GPUOpen 降低集成成本，长期胜负取决于游戏默认质量、驱动首发和工具，而非一次发布会帧率。

独显市场还受 AIB、渠道和库存影响。建议价不是成交价，供应不足会扭曲性价比。AMD 可能选择不在每个最高端功耗档正面对抗，而聚焦大销量价格带；这是一种组合策略，不能从缺少某旗舰推断架构全部失败，也不能把中端成功外推到高端。

### 6. 掌机与游戏机：固定平台的规模效应

AMD 在 Xbox、PlayStation、Steam Deck 和 Windows 掌机中广泛出现，形成 Zen+RDNA 的固定平台规模。开发者针对统一内存、GPU 和存储优化，技术可反哺 PC 引擎。Ryzen Z 则让 OEM 更快推出不同屏幕和功耗设备。[A003][A001]

竞争包括 Nintendo/移动 Arm SoC、Intel 掌机芯片和云游戏。AMD 的优势是 x86 PC 游戏兼容与成熟 RDNA，限制是 Windows 掌机待机、UI 和电池由微软/OEM共同决定。Valve 用 SteamOS 垂直优化证明软件可以弥补较旧 APU；Windows OEM 数量多则带来组合与渠道优势。

半定制商业周期较长。主机首发可能大批量，成熟期降本，代际末库存下降；中期 Pro 型号延长周期。AMD 要平衡客户定制工程和 IP 复用。大客户集中使订单变动显著，但稳定长约能提供晶圆规划。

### 7. HEDT 与工作站

Threadripper 在消费 Ryzen 与双路服务器之间建立高价细分。Intel 工作站 Xeon W 是直接竞争，Apple Mac Studio/Mac Pro和 GPU 工作站是间接竞争。客户按软件许可、内存容量、PCIe 卡、认证和服务选择，不只按核心数。

AMD 的 96 核 PRO、八通道、PCIe 5.0 和 Radeon/第三方 GPU 扩展适合渲染、仿真、编译和 AI 开发。[A010] 高平台价和 350W 散热限制大众需求。若任务可租云 GPU或在普通 Ryzen 完成，Threadripper 的闲置成本高；若每天节省工程师时间，它可快速回本。

工作站市场的护城河是 ISV 认证和 OEM服务。DIY 跑分领先不保证企业部署；固件、ECC、远程管理和备件是采购条件。AMD 需同时维护 DIY HEDT 热度与 HP/Dell/Lenovo 等专业渠道，两者价格和更新节奏不同。

### 8. 服务器 CPU：EPYC 对 Xeon/Arm

EPYC 的 chiplet、高核心数、内存通道和 PCIe 给云和企业提供整合优势。竞争包括 Intel Xeon 的企业生态和加速器、AWS Graviton/Ampere/NVIDIA Grace 等 Arm。客户比较每机架吞吐、软件许可、功耗、虚拟机密度与迁移风险。

高核心数可减少服务器数量，但按核许可数据库可能成本更高；高频少核 SKU为此存在。Arm 可在云原生提供能效，x86 遗留软件让 EPYC迁移更容易。机架功率限制下，每瓦和内存带宽比单插槽峰值重要。

EPYC 与 Instinct/Pensando 的组合能增加每客户收入，客户也会坚持多供应商 NIC/GPU。开放标准和标准服务器形态降低采购风险；专有机架级系统可提高协同却增加锁定。AMD 的策略需要在二者间平衡。

### 9. AI 加速器：Instinct 对 NVIDIA 与自研 ASIC

NVIDIA 在 CUDA、NVLink/NVSwitch、网络和整机有强生态。AMD Instinct以 HBM 容量、开放 ROCm、标准以太网和客户定制争夺云/AI。另有 Google TPU、AWS Trainium/Inferentia、Microsoft Maia 等自研 ASIC，针对自家负载优化并减少外采依赖。

MI350 的 288GB HBM3E 有利于单卡容量和低精度推理，实际竞争要看可交付系统、ROCm 模型支持、集群可靠性和总拥有成本。[A034] 采购者不只算 GPU 单价，还算工程移植、利用率、网络、功耗、停机和退出成本。

超大云客户有能力维护两套后端并争取供应议价，普通企业更倾向成熟托管服务。AMD 需要云实例、OEM 系统、容器和模型“开箱”路径，把开放转化为较低运维成本。仅提供芯片会把集成责任留给客户。

### 10. Embedded 与 Adaptive Computing

Xilinx 使 AMD 在 FPGA/自适应 SoC 面对 Intel Altera、Lattice、Microchip 和定制 ASIC。FPGA 的市场规模不像 PC 按季度快速更新，设计验证和生命周期可达多年。切换工具和 RTL 的成本高，生态和工程师经验是壁垒。[A057][A058]

Versal 将硬核 CPU、AI Engine、NoC 与可编程逻辑结合，竞争 GPU边缘模块和 ASIC。[A057] 它适合协议变化、低延迟和传感器数据流，开发复杂度是成本。AMD 若能统一 Vitis 与 AI 软件体验，可把 XDNA、数据中心和边缘开发者连接。

Embedded 的需求来自工业、通信、医疗、汽车、航空和测试。宏观放缓会造成渠道库存，但产品长期供货。市场分析应看设计赢单、backlog 和客户库存，不用单季度收入判断架构竞争力。

### 11. DPU 与 AI 网络

Pensando 让 AMD 与 NVIDIA BlueField、Intel IPU/Broadcom Marvell 等竞争基础设施卸载。云厂商需要虚拟交换、安全、存储和遥测，AI 集群还需要低拥塞高吞吐网络。DPU 的客户不是普通消费者，却影响 Instinct 平台完整性。

Pollara 400 面向开放 Ultra Ethernet AI 网络，优势是以太网生态与可编程；挑战是新规范、交换机和集合通信需要共同成熟。[A063] NVIDIA 可提供端到端 InfiniBand/NVLink，AMD 倾向开放以太网伙伴。开放降低单一锁定，集成责任可能更高。

Salina DPU 卸载基础设施服务，价值以释放 CPU 核、隔离和运维自动化衡量，不以 TOPS。[A063] 客户若已有 SmartNIC 控制平面，切换 Pensando 需要策略、遥测和团队培训。软件是 DPU 市场的主壁垒。

### 12. 渠道、定价与产品分层

AMD 使用 OEM/ODM、分销、零售、电商、云厂商和系统集成商。盒装 CPU价格透明，移动 APU 藏在整机，EPYC/Instinct价格多为合同。把 MSRP 与服务器成交价比较没有意义。区域税、促销和整机搭配会改变消费性价比。

产品分层通过核心数、缓存、频率、内存、PCIe、管理和软件合同实现。禁用部分单元可提高晶圆利用率，形成 Ryzen 5/7/9、RX x600/x700等层级。分层也可能让名称复杂，消费者需要规格数据库。[A003][A012]

高端“光环”能影响品牌，销量常在主流。AMD 是否追求绝对旗舰取决于裸片成本、功耗、供应和可售价格。分析产品策略应看毛利和机会成本：同一先进产能可能用于更高价值数据中心产品。

### 13. 护城河与切换成本地图

CPU 护城河：x86 软件、插槽、主板、OEM 验证、编译器；GPU：API、驱动、游戏/创作插件、显存；NPU：模型编译、OS 调度、ISV；服务器：虚拟化、固件、认证；AI GPU：kernel、通信、运维；FPGA：RTL、工具、时序；DPU：控制平面和网络策略。

AMD 的开放策略尝试降低从竞争平台迁入成本：HIP 接近 CUDA、GPUOpen 跨厂商、标准 PCIe/以太网、x86 兼容。真正的低切换要有文档、自动转换、性能调优、长期 API和支持。源代码能编译只是第一阶段。

客户也担心从 AMD 退出。使用标准框架、容器、以太网和开放工具可以减少锁定；依赖特定 XDNA 编译、RCCL 调优或 Pensando 策略仍有成本。成熟平台应提供可迁移数据和标准接口，而非否认锁定存在。

### 14. 情景分析而非单点预测

**上行情景：** Ryzen AI Max/400 获得更多高质量 OEM，Windows/应用默认使用 NPU；RX 9000在主流价位建立份额；MI350/后续系统与 ROCm 扩展，Helios按时交付；EPYC、Pensando和 Xilinx交叉销售。共同结果是软件投入被更多硬件摊薄。

**基准情景：** PC 与游戏保持竞争，数据中心增长但生态迁移渐进，嵌入式随库存恢复。AMD 在多个市场保持第二供应商和部分技术领先，收入受产品周期波动。软件改善持续但 CUDA 长尾仍强。

**下行情景：** AI 需求集中少数客户或自研 ASIC，出口限制扩大；先进封装/HBM限制交付；ROCm回归拖慢采用；PC OEM启用不足，Radeon驱动/光追差距影响需求；并购工具链整合缓慢。多线投入会放大成本。

这些是条件性推断，不是财务预测。验证指标包括：正式 OEM/云实例而非宣布；支持矩阵覆盖与升级稳定；客户重复部署；分部收入/利润；库存与供应；开发者默认后端；公开的端到端系统可用性。

### 15. 给产品与采购团队的竞争框架

先定义三年工作负载与失败成本，再建立硬约束：ISA/OS、内存、GPU API、NPU、扩展、功耗、地区和服务。满足硬约束的候选才进入性能测试。把一次性硬件、迁移工程、软件许可、功耗、停机、备件和退出成本合成 TCO。

PoC 使用真实数据和版本，至少持续一周，模拟升级与恢复。端侧设备测电池、噪声和休眠；工作站测长任务与认证；AI集群测多节点效率与故障；嵌入式测温度和生命周期。厂商结果可指导用例，不能替代验收。

最终应保留第二来源或迁移路径。CPU/GPU/NPU生态快速变化，单一供应商可能在某代领先、下一代落后。采用标准格式、容器、跨后端框架和清晰接口，让技术选择可逆，是比猜中一次赢家更可靠的战略。

---

## 路线图、关键风险与验证议程

### 1. 路线图的证据纪律

本章只记录 AMD 正式产品页、新闻稿、投资者活动和 SEC 文件公开的名称、定位与窗口。新闻稿说“计划”“预计”不等于量产，客户宣布测试不等于一般可用，产品页上线也不证明所有地区有库存。工程样品、数据库泄露、运输清单、论坛和匿名消息不作为事实。

路线图每一项分四级：已正式宣布；已披露合作伙伴/送样；具体产品或云实例已发布；广泛可买且支持矩阵完成。随着状态变化，来源账本和正文应更新日期。没有官方规格时，禁止从代号推断节点、核心、缓存、功耗或价格。

### 2. 截至快照日已确认的消费线

Ryzen 9000/9000X3D 与 Threadripper 9000 已形成 Zen 5 桌面和工作站组合；Ryzen AI 300/Max/400覆盖移动、紧凑工作站和部分桌面；Radeon RX 9000 已于 2025 年以 RDNA 4 发布。[A002][A010][A014] 这些是已发布产品，不是未来路线。

2026-03-02 官方稿把 Ryzen AI 400 扩展到更多消费者与商用 AI PC，包含桌面/工作站选项，架构组合仍为 Zen 5、RDNA 3.5、XDNA 2、NPU最高 50 TOPS。[A081] 因此可以写“产品形态扩展”，不能把 400 名称解释为 XDNA 3或 RDNA 4。

RX 9000 的上市得到 2025 年 10-K 对 Radeon 游戏 GPU需求的确认；具体地区库存和 AIB型号是动态渠道事实。[A001] 新驱动可能扩展 FSR/ROCm，仍以支持页逐版本判断。未正式公布的下一代 Radeon 规格不写入。

### 3. 数据中心正式方向

AMD 已发布 5th Gen EPYC 9005 与 Instinct MI350。[A032][A034] Advancing AI 2025 披露开放 AI 平台并预览 Helios 方向。[A086] 公司在 2025 年 10-K 表述 Instinct 路线采用年度节奏；这是一项公司战略目标，不是每年所有 SKU 都按同一时间一般可用。[A001]

Helios 的意义是 EPYC、Instinct、Pensando、机架互连与 ROCm共同设计。对路线验收应看：最终 GPU/CPU/NIC数量与功耗；机架供电和液冷；OEM/ODM系统；ROCm容器；RCCL多节点效率；可服务性和交付。只有发布会框图不足以证明生产成熟。

Financial Analyst Day 是 AMD 正式路线图入口，可作为后续 Zen、Instinct和平台名称的证据。[A065] 本报告不复述无法从稳定文档核对的幻灯片细节。若未来页面原地更新，应保留活动 PDF或视频时间戳。

### 4. 架构后续如何写

正式后续 Zen 披露可以记录代际目标、产品市场和时间范围，但在白皮书/数据表前不推断 IPC、频率或 CCD布局。微架构“完成设计”不等于产品出货；服务器、桌面和移动落地时间可能不同。所谓 Zen 6/后续名称必须以 AMD活动资料为准。

RDNA 后续同样谨慎。AMD 可以调整消费独显策略、把图形和计算 IP 融合或更名；除非正式说明，不把招聘和驱动字符串写成产品。已确认 RDNA 4 的光追/AI方向可分析，下一代 CU数和显存保持未知。

CDNA/Instinct 的年度节奏在官方 AI 活动披露后可记录系列名称和平台目标，但峰值、HBM、机架数量只在产品页出现后使用。UDNA 等统一架构概念若正式披露，应区分长期方向与具体可买 GPU，避免把愿景当 SKU。

XDNA 后续需要同时看硅和软件。新 NPU 即使提高 TOPS，也要等 Ryzen AI Software、Windows执行提供者和 OEM设备。路线图验收指标是受支持模型覆盖、端到端能耗和正式矩阵，而不是峰值。

### 5. 制程与先进封装风险

AMD 依赖外部代工，先进节点容量、良率、价格和地缘风险会影响产品。chiplet 允许计算用先进节点、IOD用成熟节点，提高组合灵活性；但多裸片增加封装、互连、测试和基板要求。chiplet 生态白皮书将互操作、隔离与恢复列为关键。[A030]

3D V-Cache 和 HBM 依赖先进键合/封装。堆叠缓存需要控制热路径，HBM GPU需要大中介层与多堆栈。AI需求可能同时争夺 HBM和封装产能，供应不足会限制 Instinct，即使 GPU裸片有产能。

消费者可能看到的影响是高端产品延迟、价格上涨或 SKU 分仓。数据中心客户还要等待整机、液冷、网卡和光模块。路线图评估必须覆盖整条供应链，不用“已流片”代替可交付系统。

### 6. 功耗、散热与机架约束

制程和架构提高每瓦，但总功耗可能因更多核心和带宽上升。桌面 170W CPU、350W Threadripper和高 TBP GPU要求主板、PSU和散热；笔记本/掌机受到皮肤温度和电池；AI机架受到数据中心电力和冷却水。

峰值模式可能提高短跑，却降低每瓦、噪声和可靠性。OEM为营销设高功耗，轻薄机身无法长期维持。验收必须热浸，记录频率、功耗、温度和风扇。不同设备相同 SKU的离散正是平台风险。

机架级 AI增长若受电网和冷却限制，市场会从“每 GPU性能”转向“每机架/每兆瓦 token”。AMD 需协同 GPU、HBM、互连和软件降低通信/空闲。大容量 HBM减少 GPU数可能节能，低利用率则浪费。

### 7. 软件执行风险

ROCm 的最大风险不是缺少某个峰值，而是长尾库、框架版本、安装和回归。官方矩阵证明支持边界会随版本变；Windows部分组件仍不同于 Linux。[A038][A039] 年度硬件节奏会给软件验证施压，新 GPU若等待 kernel支持就无法兑现。

HIP迁移降低源代码成本，却难处理 CUDA专有库和手写内核。AMD需要上游框架默认支持、优化常用模型、稳定容器和诊断。客户 PoC成功后仍可能在升级、故障和大规模通信遇到问题。

Ryzen AI生态依赖 Microsoft 与 ISV。NPU硬件存在但应用未启用会造成消费者“看得到、用不到”。AMD已明确 OEM/ISV enablement要求。[A004] 风险指标是实际应用节点覆盖与默认启用，不是合作伙伴 logo 数量。

图形驱动要同时支持旧游戏、新游戏、多个 Windows版本和大量 AIB/OEM。FSR 4的游戏集成和质量影响 RDNA 4价值。快速更新与稳定之间需分支、回滚和清晰 release notes。

### 8. 产品命名与兼容性风险

移动 Ryzen历史上数字和后缀可能混合架构、年份和定位，消费者容易把较大数字当新核心。Ryzen AI新命名改善 AI识别，又有 Max、PRO、HX等组合。产品矩阵必须给架构、核心、内存和 TDP，不只列名称。

桌面插槽承诺也有边界。物理 AM5兼容需 BIOS、供电和主板厂支持；新 CPU可能在旧板上缺少 I/O或内存特性。升级前查 CPU support list和最低 BIOS，准备旧 CPU或 Flashback避免无法启动。

ROCm gfx target与营销名不同，同一产品家族可能多个 target。软件包若没包含目标代码会报错，即使驱动识别。开发者应记录 PCI ID/gfx，而用户只按“RX 7000支持”会误判。[A039]

### 9. 供应链与库存

PC、显卡和嵌入式存在库存周期。疫情、加密或需求预测会让渠道提前拉货，随后去库存。收入下降可能是客户消化库存，不等于终端份额同幅下降；收入增长也可能包含 ASP和补库。10-K管理层解释比单一渠道报价可靠。[A001]

AMD依赖少数制造、封装和内存供应商。自然灾害、质量、物流和地缘冲突会影响。多产品共享先进产能可灵活配置，也会产生内部机会成本：高毛利 Instinct需求强时，消费 GPU供给策略可能变化。

假货、翻新和灰色渠道是消费风险。用户应从授权渠道、核对序列和保修；服务器/嵌入式客户应有供应链追踪。报告不把第三方泄露库存当路线证据。

### 10. 政策、出口与地区风险

2025年 MI308出口管制导致约4.4亿美元相关库存和费用，证明高端 AI GPU可因政策突然改变可售市场。[A001] AMD可能开发合规 SKU，但性能、上市和许可必须以正式文件为准。地区 SKU不能直接映射全球版本。

出口限制还可能影响客户数据中心建设、HBM与网络设备；政府补贴和本地制造政策改变成本。企业采购应把许可、交货目的地、再出口和软件更新纳入合同，不能只看订货时合规。

嵌入式、通信和国防产品也受更严格规则。Xilinx/Pensando客户遍布全球，合规会增加销售周期。路线图的“全球发布”不保证所有地区同时可买。

### 11. 安全风险

CPU推测执行、固件、管理处理器、GPU驱动和 DPU都扩大攻击面。安全公告可能要求微码/BIOS/驱动更新并影响性能。企业需要资产清单、漏洞响应和可回滚固件，不能只依赖出厂 Secure Boot。

AMD PRO/EPYC提供内存加密和可信执行等能力，具体功能依代际/SKU。[A078] 机密虚拟机还依 hypervisor、证明和密钥服务。消费端全盘加密依 TPM、OS和恢复密钥。硬件特性不替代配置。

AI模型和数据可能留在 HBM/显存，共享环境需隔离和清理。DPU承担安全策略后成为高价值控制点，固件与管理平面必须签名、分权和审计。开源软件还要管理供应链依赖。

### 12. 并购整合风险

Xilinx和 Pensando扩展 AMD能力，也带来工具、销售、文化和会计整合。Vitis/Vivado、ROCm、GPUOpen、Ryzen AI Software若长期割裂，客户难形成统一开发；强行统一又可能破坏成熟流程。成功应以跨产品工具和联合客户部署验证。[A067][A001]

10-K中收购相关无形资产摊销和 All Other费用会影响 GAAP利润。财务评价要区分运营增长与会计摊销，也不能忽略收购价需要未来现金流回报。技术协同不是自动的。

### 13. 竞争响应风险

Intel可能通过制程、封装、OEM渠道和 NPU改进收复 PC/服务器；NVIDIA可用 CUDA、网络和整机保持 AI/图形优势；Apple垂直整合提高端侧能效；Qualcomm以 Arm改变 Windows功耗；云厂商自研 ASIC减少通用 GPU需求。[A069][A070][A071][A072]

AMD不能只用一个维度应对。更强 CPU若 NPU应用不足，AI PC仍弱；更大 HBM若集群软件差，Instinct利用低；开放工具若缺支持，企业成本高。竞争会迫使 AMD在硬件速度、软件成熟、价格和供应间分配有限资源。

竞争也可能扩大市场。多家 NPU促使 Windows/应用采用标准接口，多家 AI GPU推动框架可移植，Ultra Ethernet扩大开放网络。AMD的机会是让标准成为默认，风险是标准最低公分母无法发挥硬件。

### 14. 购买者与开发者风险矩阵

消费者最高风险是买到内存不足或 OEM散热差的不可升级设备；缓解是按具体机型测长时、一次买够焊接内存、确认退换。游戏玩家风险是显存/驱动/游戏支持；缓解是目标游戏矩阵。创作者风险是插件与认证；缓解是供应商支持页和项目 PoC。

开发者风险是后端锁定和版本回归；缓解是标准模型格式、抽象设备层、跨后端 CI和固定容器。企业工作站风险是停机；缓解是认证驱动和备件。AI集群风险是扩展效率与供应；缓解是小规模真实模型 PoC、故障注入和多供应商设计。

嵌入式风险是生命周期与认证；缓解是正式供货承诺、最后采购和软件维护合同。DPU/FPGA风险是专业人才和工具锁定；缓解是模块化接口、源代码和培训。

### 15. 未来十二个月的验证议程

1. 每季度存档 Ryzen/Radeon/Threadripper/EPYC/Instinct规格页，记录新增/下架 SKU。
2. 跟踪 Ryzen AI 400实际 OEM设备、内存配置、NPU默认应用和地区可买状态，而非只看发布。
3. 逐 ROCm版本记录 Radeon/Ryzen/Instinct矩阵、Windows组件和主流框架 wheel。
4. 验证 RX 9000在目标游戏的 FSR 4、光追、帧时间和显存，不汇总无条件厂商倍数。
5. 跟踪 Helios从宣布到 OEM系统、机架规格、云实例和集群基准的状态。
6. 用 10-Q/10-K更新分部收入、库存、出口费用和研发，不依赖市场传闻。
7. 仅从 AMD Investor Relations和 Newsroom接收路线图变更。[A080]

### 16. 决策原则

路线图不能替代当前产品。若今天需要部署，按今天正式支持和可交付选；未来升级只赋有限权重。等待新代也有机会成本，首发采用有固件/软件风险。用可逆性管理：标准接口、可退换、可扩展内存/存储、容器和迁移测试。

风险并不意味着回避 AMD。它意味着把多产品体系的优势与相应依赖同时计价。最好的 AMD 方案通常是任务、内存、互连和软件恰好命中其架构优势；最差方案是只因一个峰值或路线图名称购买，再让 OEM、驱动或应用成为未知。

---

## 引用与复核指南

### 引用格式

正文使用 `[A001]` 至 `[A080]` 的稳定标识，完整条目位于 `sources-amd.md`。标识表达“该来源能支持紧邻事实”，不表达对整段所有推论的背书。一个段落若混合规格、市场数据和推断，应分别在相应句子后引用。

### 来源优先级

优先顺序为：AMD/SEC 原始申报；AMD 产品规格与架构文档；AMD 官方软件文档和源码仓库；平台拥有者的终端规格；公开标准组织；最后才是完整披露测试方法的第三方测量。本报告没有把搜索摘要、聚合参数站、泄露或论坛消息列为路线图证据。[A078][A080]

### 时间与版本

网页会原地更新，因此引用条目记录访问日 2026-08-04。软件支持结论必须同时给出文档版本或“latest 页面在快照日的状态”。若读者在以后看到不同矩阵，应以当时版本归档重现本报告，而不是假定现在页面仍代表当时。

### 厂商测试

AMD 新闻稿和产品页中的相对性能属于厂商实验。只有当比较系统、内存、驱动、应用、分辨率、功耗模式、样本与统计方法足够清楚时，才能复述具体比例；否则只用来确认产品定位。理论 TOPS/TFLOPS 还必须标注精度、稀疏、矩阵/向量和 boost 条件。

### 争议处理

当产品页、规格数据库和开发文档不一致时，先判断它们是否描述不同 SKU、封装或软件版本。仍冲突时，在正文并列陈述并标注日期，不自行“平均”。路线图和上市状态分为已宣布、已送样、合作伙伴系统已宣布、一般可用四级，不能互换。

### 复核入口

读者可运行：

```bash
bash scripts/assemble_report.sh
bash scripts/check_report.sh
bash scripts/audit_citations.sh
perl scripts/check_duplicates.pl
bash scripts/check_links.sh
```

合并版由源章节生成；任何直接编辑都会在下次装配时被覆盖。

---

## AMD 平台来源账本

> 全部链接访问于 2026-08-04。`用途` 只说明本报告如何使用该来源；厂商性能数据不自动等于独立结论。

- [A001] AMD, “2025 Form 10-K,” 2026-02-04. https://ir.amd.com/financial-information/sec-filings/content/0000002488-26-000018/amd-20251227.htm — 分部、收入、风险与业务定义。
- [A002] AMD, “Ryzen Processors for Desktops.” https://www.amd.com/en/products/processors/desktops/ryzen.html — Ryzen 9000/X3D 平台与代表规格。
- [A003] AMD, “Processor Specifications.” https://www.amd.com/en/products/specifications/processors.html — 逐 SKU 核心、频率、TDP、内存和 I/O。
- [A004] AMD, “Ryzen AI.” https://www.amd.com/en/products/processors/consumer/ryzen-ai.html — Ryzen AI Max/400/300 与 OEM/ISV 限制。
- [A005] AMD, “Ryzen AI 300 Series Announcement,” 2024-06-02. https://www.amd.com/en/newsroom/press-releases/2024-6-2-amd-unveils-next-gen-zen-5-ryzen-processors-to-p.html — Zen 5、RDNA 3.5、XDNA 2 首发口径。
- [A006] AMD, “Expanded AI PC Portfolio,” 2025-01-06. https://www.amd.com/en/newsroom/press-releases/2025-1-6-amd-announces-expanded-consumer-and-commercial-ai-.html — Ryzen AI Max 和 Ryzen AI 300 扩展。
- [A007] AMD, “Ryzen 9 9950X.” https://www.amd.com/en/products/processors/desktops/ryzen/9000-series/amd-ryzen-9-9950x.html — 16 核 Zen 5 桌面 SKU。
- [A008] AMD, “Ryzen 7 9700X.” https://www.amd.com/en/products/processors/desktops/ryzen/9000-series/amd-ryzen-7-9700x.html — 8 核 65W 桌面 SKU。
- [A009] AMD, “Ryzen 7 9800X3D.” https://www.amd.com/en/products/processors/desktops/ryzen/9000-series/amd-ryzen-7-9800x3d.html — 第二代 3D V-Cache SKU。
- [A010] AMD, “Ryzen Threadripper Workstation Processors.” https://www.amd.com/en/products/processors/workstations/ryzen-threadripper.html — 9000/PRO、TRX50/WRX90。
- [A011] AMD, “Ryzen Laptop Processors.” https://www.amd.com/en/products/processors/laptop/ryzen.html — 移动家族与 OEM 产品入口。
- [A012] AMD, “Graphics Specifications.” https://www.amd.com/en/products/specifications/graphics.html — Radeon 逐 SKU 规格数据库。
- [A014] AMD, “Radeon RX 9070 Series,” 2025-02-28. https://www.amd.com/en/newsroom/press-releases/2025-2-28-amd-unveils-next-generation-amd-rdna-4-architectu.html — RDNA 4、FSR 4 与上市信息。
- [A015] AMD, “Radeon RX 7900 XTX.” https://www.amd.com/en/products/graphics/desktops/radeon/7000-series/amd-radeon-rx-7900xtx.html — RDNA 3 chiplet 独显规格。
- [A016] AMD, “Radeon PRO Professional Graphics.” https://www.amd.com/en/products/graphics/workstations.html — 专业显卡组合与认证定位。
- [A017] AMD, “AMD Advantage.” https://www.amd.com/en/gaming/advantage.html — CPU/GPU/OEM 协同计划。
- [A019] AMD, “Ryzen Embedded.” https://www.amd.com/en/products/embedded/ryzen.html — 嵌入式 Ryzen 产品线。
- [A021] Microsoft, “Xbox Series X Specifications.” https://www.xbox.com/en-US/consoles/xbox-series-x — Xbox 终端配置。
- [A023] AMD, “5th Gen EPYC Architecture White Paper.” https://www.amd.com/content/dam/amd/en/documents/epyc-business-docs/white-papers/5th-gen-amd-epyc-processor-architecture-white-paper.pdf — Zen 5/5c、IOD、内存与安全。
- [A024] AMD, “Zen 3 Core Architecture.” https://www.amd.com/en/technologies/zen-core.html — Zen 代际与核心复合体说明。
- [A029] AMD, “3D V-Cache Technology.” https://www.amd.com/en/products/processors/technologies/3d-v-cache.html — 堆叠缓存原理与产品入口。
- [A030] AMD, “AMD Chiplet Ecosystem White Paper,” 2024-12. https://www.amd.com/content/dam/amd/en/documents/solutions/technologies/chiplet-architecture-white-paper.pdf — chiplet 互操作、隔离与恢复。
- [A032] AMD, “EPYC 9005 Series.” https://www.amd.com/en/products/processors/server/epyc/9005-series.html — 5th Gen EPYC 产品组合。
- [A033] AMD, “EPYC Processor Specifications.” https://www.amd.com/en/products/specifications/server-processor.html — 服务器逐 SKU 数据。
- [A034] AMD, “Instinct MI350 Series.” https://www.amd.com/en/products/accelerators/instinct/mi350.html — CDNA 4、HBM3E 与数据类型。
- [A035] AMD, “Instinct MI300X.” https://www.amd.com/en/products/accelerators/instinct/mi300/mi300x.html — CDNA 3、192GB HBM3 与 OAM 平台。
- [A036] AMD, “CDNA Architecture.” https://www.amd.com/en/technologies/cdna.html — 数据中心计算架构定位。
- [A037] AMD, “ROCm Documentation.” https://rocm.docs.amd.com/en/latest/ — ROCm 组件与入口。
- [A038] AMD, “ROCm Compatibility Matrix.” https://rocm.docs.amd.com/en/latest/compatibility/compatibility-matrix.html — 数据中心 GPU、OS 与版本支持。
- [A039] AMD, “Use ROCm on Radeon and Ryzen: Compatibility.” https://rocm.docs.amd.com/projects/radeon-ryzen/en/latest/docs/compatibility/compatibility.html — 消费 GPU/APU 支持矩阵。
- [A040] AMD, “HIP Documentation.” https://rocm.docs.amd.com/projects/HIP/en/latest/ — HIP 语言、运行时与移植。
- [A041] AMD, “Ryzen AI Software.” https://ryzenai.docs.amd.com/en/latest/ — XDNA 模型部署工具链。
- [A042] AMD, “Ryzen AI Software GitHub.” https://github.com/amd/RyzenAI-SW — 示例、版本与开源组件。
- [A043] AMD, “ROCm GitHub.” https://github.com/ROCm/ROCm — ROCm 开源仓库入口。
- [A044] Microsoft, “DirectML.” https://learn.microsoft.com/en-us/windows/ai/directml/dml — Windows GPU ML API 与执行模型。
- [A045] AMD, “XDNA Linux Driver.” https://github.com/amd/xdna-driver — XDNA 内核驱动与支持设备。
- [A046] AMD, “GPUOpen.” https://gpuopen.com/ — 图形开发工具与开放 SDK。
- [A048] AMD, “FSR Technology.” https://www.amd.com/en/products/graphics/technologies/fidelityfx/super-resolution.html — FSR 代际与支持边界。
- [A049] AMD, “Software: Adrenalin Edition.” https://www.amd.com/en/products/software/adrenalin.html — 消费显卡驱动功能。
- [A050] Linux Kernel, “AMDGPU Driver.” https://docs.kernel.org/gpu/amdgpu/index.html — Linux 内核驱动接口。
- [A051] AMD, “Ryzen AI Max+ 395.” https://www.amd.com/en/products/processors/laptop/ryzen/ai-300-series/amd-ryzen-ai-max-plus-395.html — 共享内存、CPU、GPU 与 NPU 代表 SKU。
- [A052] PCI-SIG, “PCI Express.” https://pcisig.com/pci-express — PCIe 标准与代际入口。
- [A053] CXL Consortium, “Compute Express Link.” https://computeexpresslink.org/about-cxl — CXL 协议定义。
- [A054] AMD, “Smart Access Memory.” https://www.amd.com/en/gaming/technologies/smart-technologies.html — Resizable BAR/SAM 平台功能。
- [A057] AMD, “Versal.” https://www.amd.com/en/products/adaptive-socs-and-fpgas/versal.html — Versal 自适应 SoC 架构定位。
- [A058] AMD, “FPGAs.” https://www.amd.com/en/products/adaptive-socs-and-fpgas/fpga.html — FPGA 产品体系。
- [A059] AMD, “Embedded Products.” https://www.amd.com/en/products/embedded.html — 嵌入式 CPU、GPU 与自适应计算入口。
- [A063] AMD, “Pensando Software.” https://www.amd.com/en/solutions/data-center/networking.html — 数据中心网络与软件定位。
- [A065] AMD, “Financial Analyst Day 2025.” https://ir.amd.com/news-events/financial-analyst-day — 正式战略和路线图入口。
- [A067] AMD, “AMD Completes Acquisition of Xilinx,” 2022-02-14. https://www.amd.com/en/newsroom/press-releases/2022-2-14-amd-completes-acquisition-of-xilinx.html — 自适应计算并购与业务组合。
- [A069] Intel, “Core Ultra Processors.” https://www.intel.com/content/www/us/en/products/details/processors/core-ultra.html — x86 消费端竞品产品体系。
- [A070] Apple, “M4.” https://www.apple.com/newsroom/2024/05/apple-introduces-m4-chip/ — 统一内存 SoC 竞品参照。
- [A071] Qualcomm, “Snapdragon X Elite.” https://www.qualcomm.com/products/mobile/snapdragon/pcs-and-tablets/snapdragon-x-elite — Windows on Arm 竞品参照。
- [A072] NVIDIA, “GeForce RTX 50 Series.” https://www.nvidia.com/en-us/geforce/graphics-cards/50-series/ — 消费独显竞品参照。
- [A073] Sony, “PlayStation 5 Pro Technical Seminar.” https://blog.playstation.com/2024/09/10/welcome-playstation-5-pro-the-most-visually-impressive-way-to-play-games-on-playstation/ — 半定制终端图形目标。
- [A074] Microsoft, “Xbox Velocity Architecture.” https://news.xbox.com/en-us/2020/07/14/a-closer-look-at-xbox-velocity-architecture/ — 半定制 SoC、存储与软件协同。
- [A075] Valve, “Steam Deck Tech Specs.” https://www.steamdeck.com/en/tech — AMD APU 掌机整机规格。
- [A076] ASUS, “ROG Ally X Tech Specs.” https://rog.asus.com/gaming-handhelds/rog-ally/rog-ally-x-2024/spec/ — Ryzen Z1 Extreme 掌机 OEM 配置。
- [A077] Framework, “Laptop 13 Ryzen AI 300.” https://frame.work/products/laptop13-diy-amd-ai300 — 可维修 OEM 与移动平台案例。
- [A078] AMD, “AMD Secure Technology.” https://www.amd.com/en/products/processors/technologies/pro-technologies.html — 安全、管理与 PRO 定位。
- [A080] AMD, “Newsroom.” https://www.amd.com/en/newsroom.html — 上市、合作与路线图公告索引。
- [A081] AMD, “AMD Gives Consumers and Businesses More AI PC Options,” 2026-03-02. https://www.amd.com/en/newsroom/press-releases/2026-3-2-amd-gives-consumers-and-businesses-more-ai-pc-opti.html — Ryzen AI 400 桌面与工作站正式扩展。
- [A082] AMD, “Software Optimization Guide for AMD Family 10h and 12h Processors,” Rev. 3.13, 2011-02. https://www.amd.com/content/dam/amd/en/documents/archived-tech-docs/software-optimization-guides/40546.pdf — Family 10h/12h 缓存、内存与指令优化。
- [A083] AMD, “Software Optimization Guide for AMD Family 15h Processors,” Rev. 3.08, 2014-01. https://www.amd.com/content/dam/amd/en/documents/archived-tech-docs/software-optimization-guides/47414_15h_sw_opt_guide.pdf — Bulldozer 家族模块资源和软件优化。
- [A084] AMD, “Software Optimization Guide for AMD Family 17h Processors,” Rev. 3.01. https://docs.amd.com/v/u/en-US/55723_3.01 — Zen 家族缓存、预取、NUMA 与数据布局。
- [A085] AMD, “AMD Expands Leadership Data Center Portfolio … at Data Center and AI Technology Premiere,” 2023-06-13. https://www.amd.com/en/newsroom/press-releases/2023-6-13-amd-expands-leadership-data-center-portfolio-with-.html — MI300、EPYC 和软件生态的正式活动框架。
- [A086] AMD, “AMD Unveils Vision for an Open AI Ecosystem … at Advancing AI 2025,” 2025-06-12. https://www.amd.com/en/newsroom/press-releases/2025-6-12-amd-unveils-vision-for-an-open-ai-ecosystem-detai.html — MI350、ROCm、开放机架与 Helios 预览。
